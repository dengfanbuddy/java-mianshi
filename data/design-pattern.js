window.QB = window.QB || {};
window.QB["design-pattern"] = {
  id: "design-pattern",
  name: "设计模式",
  icon: "🧩",
  desc: "游戏服务器高频设计模式专项：单例/工厂/策略/模板方法/观察者/责任链/状态机/命令/享元等，全部落到游戏 Handler 分发、渠道接入、活动框架、技能 Buff 系统、GM 指令等候选人真实场景，拒绝纯概念背诵。",
  questions: [
    {
      id: "design-pattern-01",
      level: 1,
      q: "单例模式有哪几种写法？DCL 为什么要加 volatile？游戏服里的配置表管理器适合用哪种单例？",
      point: "考察对 DCL 指令重排与 volatile 语义的理解，以及单例在游戏服并发读写下的真实风险",
      approach: "先给结论：生产只用饿汉/静态内部类、DCL+volatile、枚举三种。讲 DCL 时重点拆解 new 的三步与重排序风险，说明 volatile 的作用。再结合配置表管理器讲选型理由，最后主动指出单例不保证字段并发安全、热更需 AtomicReference 整体替换，展现实战深度。别只背五种写法。",
      a: "核心结论：生产环境只用三种——饿汉式/静态内部类（简单场景）、DCL + volatile（懒加载）、枚举（最安全）；游戏服配置表管理器用静态内部类或容器托管（Spring 单例）最合适。\n1. 饿汉式：类加载即初始化，线程安全但可能浪费（没用也创建）。\n2. DCL 双重检查锁：两次判空 + synchronized。必须加 volatile 的原因：instance = new ConfigManager() 不是原子操作，分「分配内存 → 初始化对象 → 赋值引用」三步，JIT 可能重排序成「分配 → 赋值 → 初始化」，另一个线程拿到的是「半初始化对象」。volatile 禁止指令重排并保证可见性。\n3. 静态内部类：利用类加载机制（类初始化有 JVM 锁保证），懒加载 + 无线程安全问题 + 无锁开销，最优雅的写法。\n4. 枚举单例：Effective Java 推荐，天然防反射攻击和序列化破坏。\n5. 游戏服落地：配置表管理器 GameConfigManager 全局唯一、启动时加载，用静态内部类或干脆交给 Spring 容器管理（@Component 默认单例）；注意单例在游戏服的真正风险不是创建，而是「多线程读写」——单例只保证一个实例，不保证实例字段的并发安全，配置热更时要考虑读写切换（如 AtomicReference 整体替换配置对象）。",
      followups: [
        {
          q: "反射和序列化怎么破坏单例？枚举为什么能防住？",
          a: "反射可通过 setAccessible 调私有构造器创建新实例；反序列化 readObject 也会生成新对象。枚举由 JVM 保证实例唯一，反射创建枚举实例会被禁止，序列化按名字还原，天然免疫。"
        },
        {
          q: "Spring 的单例 Bean 和单例模式是一回事吗？",
          a: "不是一回事。Spring 单例是容器作用域概念，保证每个容器内同 Bean 名一个实例；单例模式是类加载器级的实例唯一。Spring 单例靠容器管理，不防反射，但更可注入、便于测试替换。"
        },
        {
          q: "配置表热更新时，直接改单例里的 Map 有什么并发风险？",
          a: "读线程可能读到改了一半的 Map，遍历时抛 ConcurrentModificationException。正确做法：构建好新配置对象后用 AtomicReference 整体替换引用，读线程始终拿到完整一致的旧版或新版快照。"
        }
      ],
      memory: "DCL 不加 volatile = 装修队「钥匙先交给业主（引用赋值），家具后搬（初始化）」，别人开门进去看到空房子。配置表单例 = 全公司一本员工手册，热更不是涂改手册（线程不安全），而是整本换新版（AtomicReference 替换）。",
      tags: ["单例", "DCL", "volatile", "游戏实战", "基础"]
    },
    {
      id: "design-pattern-02",
      level: 1,
      q: "简单工厂、工厂方法、抽象工厂有什么区别？游戏里的「奖励发放」适合用哪种？",
      point: "考察能否分清三种工厂的抽象层次，以及用注册表加策略落地奖励系统的工程能力",
      approach: "先用一句话概括三者抽象层次递进，再各举一个特征。重点落第 4 点：奖励发放用「简单工厂+注册表」混合方案，讲清 Map 按 type 查表发放，本质是工厂叠加策略。最后主动提 Spring 的 FactoryBean/BeanFactory 作为工业级例子加分。别停留在概念对比，面试官要听落地。",
      a: "核心结论：三者的抽象层次递进——简单工厂是「一个工厂造一切」，工厂方法是「一种产品一个工厂」，抽象工厂是「一族产品一套工厂」。\n1. 简单工厂：一个静态方法按类型参数返回不同对象（RewardFactory.create(type)）。优点是简单；缺点是每加一种奖励要改工厂类，违反开闭原则。\n2. 工厂方法：定义工厂接口，每种产品一个具体工厂类（GoldRewardFactory、ItemRewardFactory）。新增奖励 = 新增工厂类，不改老代码，符合开闭原则；代价是类变多。\n3. 抽象工厂：工厂生产「一族相关产品」，如跨平台 UI 库造一套按钮+文本框。游戏里的例子：不同渠道服的整套接入对象（登录器 + 支付器 + 分享器）由渠道工厂统一产出。\n4. 游戏奖励发放落地：奖励类型几十种（金币/钻石/道具/经验/体力），用「简单工厂 + 注册表」的混合最实用——每种奖励一个 RewardHandler，启动时按 type 注册进 Map<Integer, RewardHandler>，发放时 rewardHandlerMap.get(type).grant(player, params)。本质是把简单工厂的 if-else 换成查表，再叠加策略模式。\n5. 面试加分点：Spring 里 FactoryBean、BeanFactory 就是工厂模式的工业级实现，可以主动提。",
      followups: [
        {
          q: "开闭原则是什么？简单工厂为什么违反它？",
          a: "开闭原则：对扩展开放、对修改关闭。简单工厂每加一种产品都要改工厂类的 if-else，属于修改已有代码，违反开闭；工厂方法新增产品只新增类，不改老代码，符合开闭。"
        },
        {
          q: "Map 注册表方案和 switch-case 比，除了优雅还有什么工程收益？",
          a: "新增奖励只加 Handler 类不动分发逻辑，改动面小、回归风险低；注册表可在启动期校验重复 type 提前报错；每个 Handler 可独立单测；配合 Spring 可自动收集注册，零配置扩展。"
        },
        {
          q: "抽象工厂在游戏跨平台（安卓/iOS/小程序）接入上怎么用？",
          a: "每个平台是一族相关产品：登录器+支付器+推送器+分享器。定义抽象渠道工厂声明四个创建方法，各平台工厂各产一族。切换平台只换工厂，整套对象配套产出，避免跨平台对象混搭错配。"
        }
      ],
      memory: "简单工厂 = 大食堂一个窗口啥都卖（加菜要改窗口）；工厂方法 = 每个菜一个专窗（加菜开新窗）；抽象工厂 = 连锁套餐店（麦当劳一套：汉堡+薯条+可乐）。奖励发放 = 按奖券类型去对应窗口兑奖（Map 查表）。",
      tags: ["工厂", "开闭原则", "奖励系统", "游戏实战", "基础"]
    },
    {
      id: "design-pattern-03",
      level: 1,
      q: "策略模式解决什么问题？你们卡牌游戏接了 N 个渠道 SDK，登录/支付代码怎么避免 if-else 地狱？",
      point: "考察用策略模式消除分支堆积的能力，以及渠道 SDK 登录支付接入的真实封装经验",
      approach: "先讲没有策略时 if-else 的痛点：方法膨胀、回归测试爆炸。再给接口抽象+Map 查表的改造方案，结合登录/支付两个渠道场景说明统一出入参。加分项是 Spring 按 Bean 名自动注入 Map 的零配置注册。最后辨析策略与工厂的分工。别只答 Comparator 这种教科书例子。",
      a: "核心结论：策略模式 = 把「会变的行为」抽象成接口，运行期按标识选择具体实现，消灭 if-else 分支堆积，新增渠道 = 新增类而非改代码。\n1. 没有策略模式时：if (channel == \"huawei\") { ... } else if (channel == \"xiaomi\") { ... }，渠道越多方法越长，任何渠道改动都要动核心流程，回归测试范围爆炸。\n2. 策略模式改造：定义 ChannelLoginStrategy 接口（verify(token) -> LoginResult），每个渠道一个实现类；用 Map<String, ChannelLoginStrategy> 持有全部策略，登录请求进来按渠道标识 strategyMap.get(channel).verify(token)。\n3. 游戏项目落地细节：渠道 SDK 的验证逻辑各不相同（华为的 OAuth、应用宝的票据校验、官方服的账号密码），但对外统一为「传入凭证、返回 channelUid + 扩展字段」；支付同理抽象 ChannelPayStrategy（下单、回调验签、查询订单）。\n4. Spring 加持的优雅做法：每个策略实现类加 @Component(\"huawei\")，构造器注入 Map<String, ChannelLoginStrategy>（Spring 自动按 Bean 名装配成 Map），新增渠道 = 新增一个类，零配置注册。\n5. 和简单工厂的关系：策略模式关注「行为可替换」，工厂关注「对象创建」；实际项目里两者叠加——工厂按渠道名拿出对应策略。",
      followups: [
        {
          q: "策略模式在 JDK 里有哪些经典应用？（Comparator 是最好答的）",
          a: "最经典是 Comparator：排序算法固定、比较行为可替换，Collections.sort(list, comparator) 就是策略注入。还有线程池的 RejectedExecutionHandler 拒绝策略、Spring 的 ResourceLoader 等。"
        },
        {
          q: "Spring 是怎么把同类型 Bean 注入成 Map 的？key 是什么？",
          a: "Spring 发现注入点是 Map<String, T> 时，会把容器中所有 T 类型 Bean 按 Bean 名（默认首字母小写类名，或 @Component 指定名）为 key 装配成 Map。所以 @Component(\"huawei\") 的实现自动进 Map，新增渠道零配置。"
        },
        {
          q: "渠道特有参数（如华为需要 accessToken + unionId 两个字段）怎么在统一接口里传递？",
          a: "用通用参数容器：接口方法收 Map<String,String> 或带扩展字段 map 的统一 Credential 对象，各策略内部取自己需要的 key。更严格的做法是每渠道定义参数子类并做反序列化校验，兼顾类型安全。"
        }
      ],
      memory: "策略模式 = 机场安检通道：国内/国际/外交/员工通道规则不同（策略），旅客按机票类型走对应通道（Map 查表），新开一条通道不影响其他通道。if-else 地狱 = 所有旅客挤一个门，安检员现场翻手册判断。",
      tags: ["策略模式", "渠道接入", "游戏实战", "if-else", "基础"]
    },
    {
      id: "design-pattern-04",
      level: 1,
      q: "模板方法模式是什么？你们的活动框架/消息处理流程里哪里用了「固定骨架 + 可变钩子」的思路？",
      point: "考察对「骨架固定、钩子可变」复用方式的理解，及在 Handler 体系与活动框架中的应用",
      approach: "先定义模板方法并点出好莱坞原则。再讲两个落地：Handler 基类的 handle() 骨架和活动生命周期骨架，说明差异步骤如何抽象。接着与策略模式对比（继承复用 vs 组合替换），体现选型判断力。最后举 JDK/Spring 例子。注意主动强调骨架方法声明 final 的用意，这是细节加分项。",
      a: "核心结论：模板方法 = 父类定义算法骨架（final 方法固定步骤顺序），子类重写抽象钩子方法填充差异步骤——好莱坞原则「别打电话给我们，我们会打给你」。\n1. 结构：父类 final 的 templateMethod() 里固定调用顺序：step1() → step2() → step3()，其中差异步骤声明为 abstract 或空实现（钩子）让子类覆盖。\n2. 游戏服消息处理落地：所有业务 Handler 基类 BaseHandler 定义 handle() 骨架：校验登录态 → 反序列化参数 → before() 钩子（如校验活动时间）→ doHandle() 抽象方法（子类写业务）→ after() 钩子（如打点日志）→ 统一异常捕获回错误包。几十个协议 Handler 共享同一套健壮性逻辑。\n3. 活动框架落地：BaseActivity 定义生命周期骨架：onStart（开启校验）→ onReward（奖励发放，子类实现）→ onEnd（结算清理）。双倍掉落、限时商店、累计充值等活动只写差异逻辑。\n4. 和策略模式的对比：模板方法是「继承维度的复用」（骨架在父类），策略是「组合维度的替换」（行为在接口实现）；模板方法继承层次深了就僵，现代代码更倾向「策略 + 组合」，但骨架流程稳定的场景模板方法依然最直白。\n5. JDK/Spring 里的例子：AbstractList、HttpServlet 的 service() 调 doGet/doPost、JdbcTemplate（回调接口填差异）。",
      followups: [
        {
          q: "钩子和抽象方法在模板方法里分别承担什么角色？",
          a: "抽象方法是必须由子类实现的差异步骤（如 doHandle），是骨架的必填项；钩子是有默认实现（通常为空）的可选覆盖点（如 before/after），子类按需增强。一个保骨架完整，一个提供扩展弹性。"
        },
        {
          q: "模板方法和建造者模式都涉及「步骤」，区别是什么？",
          a: "模板方法关心算法步骤的复用：骨架在父类、差异在子类，强调流程顺序不变；建造者关心复杂对象的组装：多参数链式构建加 build 校验，产物是一个对象。一个是行为复用，一个是对象构建。"
        },
        {
          q: "为什么模板方法的骨架方法要声明 final？",
          a: "final 防止子类重写骨架、打乱步骤顺序，保证控制反转不被破坏。子类只能填钩子、不能改流程，父类里的健壮性逻辑（参数校验、异常兜底）才不会被绕过。"
        }
      ],
      memory: "模板方法 = 简历模板：姓名、经历、自我评价的版式固定（骨架），内容每个人填自己的（钩子）。游戏 Handler 骨架 = 登机流程：值机→安检→登机固定，但商务舱有快速通道（钩子覆盖），没人能跳过安检（final 骨架）。",
      tags: ["模板方法", "活动框架", "Handler", "游戏实战", "基础"]
    },
    {
      id: "design-pattern-05",
      level: 1,
      q: "观察者模式（发布订阅）在游戏服务器里怎么用？游戏内事件总线和 Spring 事件机制分别怎么落地？",
      point: "考察用事件机制解耦业务动作与衍生逻辑的能力，以及同步异步、异常隔离等工程细节",
      approach: "先讲痛点：升级触发多系统导致耦合。再给事件总线方案：事件基类+监听注册+按类型分发。重点讲同步异步的取舍（单线程玩家模型同步即可，耗时监听异步化）和 Spring 事件、@TransactionalEventListener 的落地。最后主动讲注意点：监听器顺序、异常隔离、环形事件，这是区分背书与实战的地方。",
      a: "核心结论：观察者模式 = 主题维护观察者列表，状态变化时逐一通知；游戏服用它解耦「业务动作」和「衍生逻辑」（成就、任务、统计）。\n1. 游戏服典型痛点：玩家升级这个事件会触发——任务进度更新、成就检查、排行榜刷新、全服公告（如首个满级）。如果升级逻辑里直接调各系统，耦合死且越加越多。\n2. 事件总线落地：定义 GameEvent 基类和 @EventListener 注解，玩家行为产生处 fireEvent(new LevelUpEvent(player))，各系统注册监听各自关心的事件类型，总线按事件类型分发。升级系统从此不认识成就系统。\n3. 同步还是异步：游戏服单线程玩家模型下，同步事件即可（玩家逻辑线程内顺序触发，无线程安全问题）；耗时监听器（写日志、推 BI）异步化——丢进 Disruptor 队列或 @Async，避免拖慢主流程。\n4. Spring 事件：ApplicationEventPublisher.publishEvent() + @EventListener，GM 后台/购买服里天然可用；@TransactionalEventListener 还能保证「事务提交后再发事件」——充值成功才发奖励通知，避免事务回滚但奖励已发的脏事件。\n5. 注意点：监听器执行顺序（@Order）、异常隔离（一个监听器抛异常不能炸掉整个事件）、环形事件（A 事件触发 B，B 又触发 A 的死循环）。",
      followups: [
        {
          q: "观察者模式和 MQ 发布订阅有什么区别和适用边界？",
          a: "观察者是进程内调用，延迟低、强一致但限于同一 JVM；MQ 发布订阅跨进程、可削峰解耦、天然异步，但有延迟和运维成本。游戏内逻辑解耦用事件总线，跨服跨系统（如充值通知 BI）用 MQ。"
        },
        {
          q: "同步事件里一个监听器抛异常，会波及事件源业务吗？怎么隔离？",
          a: "会：同步事件里异常会冒泡到事件源，拖垮主业务。隔离方式：总线分发时对每个监听器 try-catch 记日志、不中断后续监听器；非关键监听器异步化；关键监听器（如扣资源）留在主流程同步执行。"
        },
        {
          q: "@TransactionalEventListener 解决什么问题？",
          a: "解决「事务回滚但事件已发」的脏事件问题。普通监听在方法内即触发，若事务随后回滚，监听器已做的动作（如发奖通知）无法撤销。该注解默认在事务提交后才触发，保证事件基于已落库的事实。"
        }
      ],
      memory: "观察者 = 班级群公告：老师（事件源）只发一条「明天春游」（升级事件），带书包的、买零食的、请假的各干各的（监听器），老师不用挨个通知。异步监听器 = 群里@家委会稍后统计人数，不耽误发公告。",
      tags: ["观察者", "事件总线", "游戏实战", "Spring事件", "基础"]
    },
    {
      id: "design-pattern-06",
      level: 1,
      q: "责任链模式是什么？Netty Pipeline、登录校验链、敏感词过滤链是怎么体现这个模式的？",
      point: "考察对责任链两种形态及中断/传递语义的理解，结合 Netty 与业务校验链的实战经验",
      approach: "先给定义并区分纯与不纯两种形态，指出工程里不纯的更常见。三个落地案例各讲一段：Netty 双向链与 fireChannelRead 传递语义、登录校验链的可插拔节点、敏感词链的「便宜先跑」排序。最后补工程要点：链的组装方式、中断语义约定、顺序控制，并列举 Servlet Filter 等同款思想扩大战果。",
      a: "核心结论：责任链 = 把处理者串成链，请求沿链传递，每个节点决定「处理 + 传递」或「中断」，新增处理节点不改链路结构。\n1. 两种经典形态：纯责任链（一个节点处理完就结束，如报销审批）和不纯责任链（每个节点都处理一部分再往下传，如过滤器链）。工程里后者更常见。\n2. Netty Pipeline：ChannelPipeline 是一条双向责任链——入站事件从 head 流向 tail（解码 → 协议分发 → 业务），出站事件从 tail 流向 head（编码 → 写出）。每个 ChannelHandler 是链上节点，ctx.fireChannelRead() 显式传递，不传就中断。游戏服的「解码器 → 日志记录 → 协议路由」就是靠它串起来的。\n3. 登录校验链：玩家登录要过一串校验——版本号校验 → 封号校验 → 防沉迷校验 → 白名单/维护校验 → 区服人数上限校验。每个校验器实现 LoginChecker 接口（check() 返回通过/拒绝原因），链路按序执行，任一节点拒绝即中断。新增「渠道未成年宵禁」校验只加一个节点。\n4. 敏感词过滤链：基础词库匹配 → 变体字归一化 → 组合词检测 → 语义级检测（调外部服务），性能上「便宜的先跑、贵的后跑」，任一节点命中即拦截。\n5. 实现要点：链的组装方式（数组遍历 / next 指针 / Netty 的双向链表）；节点的中断与继续语义要约定清楚；@Order 控制顺序。Servlet Filter、Spring Interceptor、Sentinel 的 ProcessorSlot 都是同款思想。",
      followups: [
        {
          q: "Netty 的 Pipeline 为什么设计成双向链表而不是数组？",
          a: "Pipeline 要支持运行期动态增删 Handler（如按需装载加密层），链表增删 O(1) 且天然维护前驱后继；数组插入要搬移元素。双向结构还区分入站/出站两个传播方向，找下一个对应方向的 Handler 直接走指针。"
        },
        {
          q: "责任链里某个节点执行很慢，会对整条链造成什么影响？怎么优化？",
          a: "同步执行下慢节点会阻塞整条链，后续节点全部排队，请求延迟被拉长。优化：慢节点（如语义级检测）异步化，快速节点先跑；给节点加耗时监控与超时；必要时拆成快慢两条链。"
        },
        {
          q: "责任链和装饰器模式结构上很像，语义上有什么区别？",
          a: "结构都是持有同接口对象层层传递。语义不同：责任链节点是处理者，各自处理请求的一部分并可中断流程；装饰器是增强者，在被装饰者调用前后叠加功能、不改请求路由。一个分而治之，一个层层加料。"
        }
      ],
      memory: "责任链 = 机场过检流程：值机查证件 → 安检查行李 → 边检查签证 → 登机口查机票，每个关口查一项、不合格就地拦下，新增「核酸检查」就是中间插一个关口。Netty Pipeline = 双向传送带，进站出站各走一边。",
      tags: ["责任链", "Netty", "登录校验", "游戏实战", "基础"]
    },
    {
      id: "design-pattern-07",
      level: 1,
      q: "代理模式的三种形态（静态代理、JDK 动态代理、CGLIB）有什么区别？MyBatis Mapper 和 RPC stub 是怎么用代理的？",
      point: "考察三种代理的实现原理差异，以及 MyBatis、RPC 如何用代理把底层细节藏进调用",
      approach: "先一句话概括代理本质：横切逻辑的附着点。再按静态代理→JDK 动态代理→CGLIB 的顺序讲实现机制与限制（接口要求、final 限制、SpringBoot 2.x 默认）。然后落到 MyBatis Mapper 和 RPC stub 两个「代理变魔术」案例。最后提自己项目里的代理应用。别混淆 JDK 代理与 CGLIB 的前提条件，这是常见扣分点。",
      a: "核心结论：代理 = 不直接访问目标对象，通过代理对象在前后插入横切逻辑；JDK 代理基于接口反射，CGLIB 基于字节码继承，静态代理手写最直白但不可扩展。\n1. 静态代理：手写代理类实现同一接口，内部持有目标对象。一个接口一个代理类，类爆炸，但逻辑最透明。\n2. JDK 动态代理：Proxy.newProxyInstance + InvocationHandler，运行期生成实现指定接口的代理类。前提是目标必须有接口；调用走反射，性能略低。\n3. CGLIB：ASM 字节码生成目标类的子类，拦截方法调用。不需要接口，但不能代理 final 类/方法；SpringBoot 2.x 起 AOP 默认用它。\n4. MyBatis Mapper：Mapper 接口没有实现类，SqlSession.getMapper() 返回的就是 JDK 动态代理对象；调用接口方法时，代理把「接口全限定名 + 方法名」映射到 Mapper XML/注解的 SQL，交给 Executor 执行——代理模式把「接口调用」魔术般变成「SQL 执行」。\n5. RPC stub：Dubbo 消费端注入的接口引用也是代理——本地调用被代理拦下，转成网络请求发给 Provider，结果再转回返回值。调用方感觉在调本地方法，网络细节全被代理藏掉，这就是「远程调用本地化」。\n6. 游戏服自己的应用：导表工具生成的配置访问层、协议层的统一日志/耗时统计，也可以用代理在不动业务代码的情况下加横切逻辑。",
      followups: [
        {
          q: "CGLIB 为什么不能代理 final 方法？",
          a: "CGLIB 通过生成目标类的子类并重写方法来拦截调用，final 方法不能被子类重写、final 类不能被继承，拦截逻辑插不进去，所以无法代理。这也是 Spring AOP 对 final 方法静默失效的原因。"
        },
        {
          q: "JDK 动态代理生成的类名长什么样？怎么打断点调试代理逻辑？",
          a: "类名形如 jdk.proxy1.$Proxy0（JDK8 是 com.sun.proxy.$Proxy0）。调试把断点打在 InvocationHandler 的 invoke 实现里；JDK8 还可加 -Dsun.misc.ProxyGenerator.saveGeneratedFiles=true 导出字节码反编译查看。"
        },
        {
          q: "MyBatis 为什么选 JDK 代理而不是 CGLIB？",
          a: "Mapper 天生就是接口、没有实现类，JDK 动态代理正好基于接口工作，无需字节码生成，依赖少、机制简单。CGLIB 需要继承具体类，Mapper 没有父类可继承，反而不适用。"
        }
      ],
      memory: "代理 = 房产中介：买家（调用方）不直接接触房东（目标对象），中介带看、砍价、办手续（横切逻辑）。JDK 代理要求房东有「房产证」（接口），CGLIB 直接克隆一个「房东替身」（子类）。Mapper 接口 = 只写了门牌号没盖房，代理现盖。",
      tags: ["代理", "JDK动态代理", "CGLIB", "MyBatis", "基础"]
    },
    {
      id: "design-pattern-08",
      level: 2,
      q: "状态模式/状态机在游戏服务器里有哪些必用场景？和 if-else 判断状态相比，状态机的工程价值是什么？",
      point: "考察能否把状态与迁移规则显式建模，理解状态机对非法迁移的结构性杜绝价值",
      approach: "先抛核心结论：状态机的价值是结构性杜绝非法迁移。再列举玩家、订单、活动、副本四类必用场景。然后批判 if-else 写法的状态散落问题，给出「枚举+迁移表+上下文」三件套实现。最后用订单状态机加乐观锁防重复发货的实战收尾。注意说清完整状态模式与轻量迁移表的取舍，避免过度设计嫌疑。",
      a: "核心结论：状态模式把「状态」和「状态间的迁移规则」显式建模为对象/表，代替散落的 if-else 判断，核心收益是「非法迁移被结构性地杜绝」而非靠人肉小心。\n1. 游戏必用场景：\n   - 玩家状态：离线/在线/战斗中/死亡/挂机，每个状态允许的操作不同（死亡不能交易、战斗中不能切换场景）；\n   - 订单状态：待支付/已支付/已发货/已关闭，支付回调只在待支付状态生效（这是幂等的重要一环）；\n   - 活动状态：未开启/进行中/领奖期/已结束，玩家请求进来先过状态门；\n   - 副本/战斗状态机：匹配中/加载中/战斗中/结算中。\n2. if-else 写法的问题：状态判断散落在几十个方法里（if (player.isDead()) return;），新增状态要翻遍所有调用点，漏一处就是线上 bug；状态迁移没有集中定义，「从死亡直接变在线」这种非法跳变无法被系统性拦截。\n3. 状态机实现三件套：状态枚举 + 迁移表（当前状态 × 事件 → 下一状态，用 Map 或二维表配置）+ 状态上下文（持有当前状态，提供 fire(event) 统一入口，非法迁移直接拒绝并告警）。\n4. 和状态模式的区别：状态模式（State Pattern）是每种状态一个类、行为写在状态类里；轻量场景用「枚举 + 迁移表」就够，行为复杂的（如战斗 AI）才上完整状态类体系。\n5. 游戏服经验：购买服订单状态机救过我们——渠道重复回调时，状态已是「已发货」的订单直接幂等返回，不会重复发货；这就是状态机 + 乐观锁（update ... where status = 待支付）的组合拳。",
      followups: [
        {
          q: "状态迁移表放代码里和放配置表里各有什么利弊？",
          a: "放代码里：类型安全、可单测、随版本可追溯，但改迁移要发版；放配置表：运营可调整、热更灵活，但缺编译期校验，需启动期校验表合法性（非法状态、孤立状态）。核心迁移规则建议放代码，参数化的放配置。"
        },
        {
          q: "状态机和数据库乐观锁怎么配合保证并发下状态不错乱？",
          a: "状态迁移用条件更新实现乐观锁：update order set status=? where id=? and status=旧值，影响行数为 0 说明已被并发修改，本次迁移失败。入口先按状态机校验迁移合法性，再用乐观锁提交，双重保证不错乱。"
        },
        {
          q: "什么是分层状态机（HFSM）？游戏 AI 为什么需要它？",
          a: "HFSM 允许状态内部嵌套子状态机，父状态的公共行为被子状态继承。游戏 AI 行为复杂（战斗内又分追击/施法/逃跑），平铺会状态爆炸；分层后公共迁移（如受击打断）只在父状态定义一次，结构清晰可维护。"
        }
      ],
      memory: "if-else 状态判断 = 全城没有红绿灯，每个路口靠司机自己判断该不该走；状态机 = 红绿灯 + 车道线，红灯结构性地不能走（非法迁移被拒）。订单状态机 = 快递状态：已签收的包裹，重复扫码只会提示「已签收」，不会送第二次。",
      tags: ["状态机", "状态模式", "订单", "游戏实战"]
    },
    {
      id: "design-pattern-09",
      level: 2,
      q: "命令模式是什么？GM 指令系统、协议请求处理、操作回放/撤销，分别怎么用它设计？",
      point: "考察理解命令模式「请求对象化」带来的排队、审计、撤销、回放能力及游戏落地",
      approach: "先点明本质：请求对象化带来时间维度的能力（排队、记录、撤销、回放）。再按 GM 指令、协议处理、回放撤销三个场景讲落地，突出审计与幂等因此有了附着点。最后与策略模式辨析「换算法」vs「行为对象化」。答题时强调命令加队列的组合是游戏服请求串行化的基础，体现体系感。",
      a: "核心结论：命令模式 = 把「请求」封装成对象（携带参数 + 接收者 + 执行方法），让请求可以被排队、记录、撤销、回放，而不只是立即执行的函数调用。\n1. 结构四角色：Command 接口（execute/undo）、ConcreteCommand（绑定接收者和参数）、Invoker（调用/排队命令）、Receiver（真正干活的人）。\n2. GM 指令系统落地：GM 后台发的每条指令（发补偿、封号、改数值）封装成 GmCommand 对象，经统一通道进入游戏服后——鉴权 → 记审计日志（谁在什么时间对哪个玩家执行了什么，可追责）→ 排队执行 → 回执结果。命令对象化让「审计、重试、幂等」都有了附着点。\n3. 协议请求处理：客户端每个协议请求本质就是命令——协议号定位 ConcreteCommand（Handler），参数是命令字段，玩家是接收者。配合命令队列（Disruptor RingBuffer）实现「请求排队、按玩家串行」，这就是命令模式 + 队列的经典组合。\n4. 撤销/回放：命令对象可记录执行前快照，支持 undo——GM 误发全服补偿，补偿命令带 undo 就能精准回收；战斗回放/操作录像则是把命令序列持久化后重新 execute 一遍（帧同步游戏就是这么做的）。\n5. 和策略模式的辨析：策略是「同一个行为换算法」，命令是「把行为本身变成可传递的对象」——命令多了时间维度（可延迟、可存储、可回放）。",
      followups: [
        {
          q: "命令的幂等性怎么保证？GM 补发命令重试会不会发两次？",
          a: "每条命令分配唯一 commandId，执行前先查执行记录，已执行过直接返回上次结果。GM 补发重试按 commandId 去重，再配合状态机校验（已发货的补偿单拒绝重复执行），不会发两次。"
        },
        {
          q: "宏命令（组合多个命令）怎么做？",
          a: "宏命令 = 组合模式用在命令上：MacroCommand 持有命令列表，execute() 顺序执行子命令，undo() 逆序撤销。注意宏不是天然事务——中间失败要逆序补偿已执行的子命令，或先预校验全部可执行再统一执行。"
        },
        {
          q: "命令模式和消息队列里的「消息」有什么异同？",
          a: "相同点：都是请求对象化、可排队异步处理、可持久化。不同点：命令通常在进程内，强调 execute/undo 语义与接收者绑定；消息是跨进程数据载体，强调路由与投递语义。实践中常串联：消费消息后再构造命令执行。"
        }
      ],
      memory: "命令模式 = 餐厅点菜单：顾客说的话（请求）写在单子上（对象化），单子可以排队（厨师按序做）、留底（审计追责）、退菜（undo）、照着单子原样再做一桌（回放）。GM 指令 = 必须有单据的操作，口头打招呼不算数。",
      tags: ["命令模式", "GM系统", "回放", "游戏实战"]
    },
    {
      id: "design-pattern-10",
      level: 2,
      q: "享元模式是什么？游戏地图格子、配置表对象、字符串常量池是怎么用它省内存的？",
      point: "考察对内在/外在状态拆分的理解，及配置表共享、地图格子等游戏内存优化实践",
      approach: "先给定义：拆内在/外在状态、共享不变部分。用围棋和 JDK 例子快速建立直觉，再重点讲两个游戏落地：地图格子共享地形类型、配置表不可变共享加背包只存 configId。强调享元必须不可变这一前提。最后背熟与对象池的区别——这是高频追问。讲解时给出内存量级对比更有说服力。",
      a: "核心结论：享元模式 = 把对象的「内在状态（不变、可共享）」和「外在状态（变化、场景相关）」拆开，内在状态共享同一份实例，用极少对象支撑海量细粒度元素。\n1. 经典例子：围棋棋盘 361 个交叉点，棋子对象只共享「黑/白」两个实例，位置（外在状态）由棋盘坐标提供——否则每盘棋 new 几百个对象。JDK 的 Integer 缓存（-128~127）、String 常量池就是享元。\n2. 游戏地图落地：MMORPG 地图百万级格子，地形类型（草地/水面/山地）是内在状态共享享元对象（含贴图、通行规则），每个格子只存「类型引用 + 坐标 + 动态占用状态」。内存从「格子数 × 完整对象」降到「格子数 × 引用 + 类型数 × 完整对象」。\n3. 配置表落地：策划 Excel 导出的 10 万条道具配置，加载后是不可变共享对象（所有玩家的「铁剑」指向同一个 ItemConfig），玩家背包只存 configId + 数量 + 绑定状态等实例数据。配合不可变设计，天然线程安全，多线程读无需加锁。\n4. 工程注意点：享元对象必须不可变，否则共享 = 互相污染；外在状态要由客户端传入而不是存在享元里；享元工厂 + 缓存池管理复用。\n5. 和对象池的区别（高频追问）：享元是「同一个对象被多个地方同时共享使用」，对象池是「对象在不同时间被复用（同一时刻只服务一方）」——连接池、线程池是后者。",
      followups: [
        {
          q: "享元对象为什么必须不可变？如果配置要热更新怎么办？",
          a: "共享意味着一处改、处处可见，可变享元会让所有使用者互相污染。配置热更不能改原对象：构建新配置对象后整体替换引用（AtomicReference），旧对象等无引用后由 GC 回收，读侧始终拿到一致快照。"
        },
        {
          q: "Integer 缓存池的范围为什么是 -128~127？能调吗？",
          a: "因为该范围整数使用最频繁，JVM 规范要求缓存此区间。可用 -XX:AutoBoxCacheMax 调大上界（如 10000），下界 -128 固定。注意只有自动装箱/Integer.valueOf 走缓存，new Integer 不走。"
        },
        {
          q: "享元模式和单例模式都在「减少对象数量」，本质区别是什么？",
          a: "单例解决类级实例唯一，全局一个实例，关注点是控制数量为一；享元解决细粒度对象共享，实例可有多个（每种内在状态一个），关注点是海量对象场景的内存复用。单例是全局唯一，享元是池化共享集合。"
        }
      ],
      memory: "享元 = 共享单车：车（内在状态）全城共享，你在哪骑、骑多久（外在状态）是你自己的事。每个玩家一辆私有自行车 = 内存爆炸。对象池 = 共享单车调度站，享元 = 骑行这个行为本身。",
      tags: ["享元", "内存优化", "配置表", "游戏实战"]
    },
    {
      id: "design-pattern-11",
      level: 2,
      q: "建造者模式解决什么问题？游戏里的邮件对象、奖励包对象为什么适合 Builder？Lombok @Builder 有什么坑？",
      point: "考察对 Builder 适用场景（多可选参数、构建校验）的判断及 Lombok 实战踩坑经验",
      approach: "先讲 Builder 解决的两个痛点：重叠构造器和半构建状态。再用邮件、奖励包两个游戏例子展示链式构建与 build() 统一校验的价值。Lombok 的坑是重头戏，按序列化、默认值、继承、toBuilder 四个坑讲，体现真用过。最后说清与工厂的分工。别把 StringBuilder 当标准答案举。",
      a: "核心结论：建造者模式 = 把「多参数、可选参数多、构建有顺序/校验要求」的对象构建过程从构造器里解放出来，链式调用可读、可校验、可复用。\n1. 解决的痛点： telescoping constructor（构造函数 5 个重载，参数从 3 个排到 12 个，调用方根本记不清第 7 个 boolean 是什么）； setter 方式则对象处于「半构建」状态被使用，且无法做整体校验。\n2. 游戏邮件对象落地：邮件有标题、内容、发件人、附件列表、过期时间、类型、语言版本……十几个字段大多可选：Mail.builder().title(\"补偿\").content(...).attach(itemId, count).expireDays(7).build()，build() 时统一校验（附件超 10 个拒绝、过期时间必须为正）。\n3. 奖励包落地：RewardPack.builder().gold(100).diamond(50).item(1001, 3).build()，链式累加，比 Map 拼装可读性强得多，且编译期类型安全。\n4. Lombok @Builder 的坑：\n   - 和 @Data 联用时默认无全参构造，某些序列化框架（Jackson 反序列化）需要配合 @NoArgsConstructor + @AllArgsConstructor + @Jacksonized；\n   - 默认值字段要加 @Builder.Default，否则 builder 不设值就是 null 而不是默认值；\n   - 父类字段不被继承，需要 @SuperBuilder；\n   - toBuilder = true 才有「以旧对象为底改几个字段」的能力。\n5. 和工厂的分工：工厂解决「创建哪种对象」，建造者解决「这个复杂对象怎么一步步装出来」。",
      followups: [
        {
          q: "为什么 build() 里做校验比 setter 后调 validate() 更安全？",
          a: "setter 方式对象在构建过程中可能被其他地方读取，处于不一致中间态，validate 靠调用方自觉容易漏；build() 校验保证对象一旦产出必然合法，不存在半构建对象外流，问题在构建期暴露而非运行期。"
        },
        {
          q: "Builder 模式在高频创建对象的热路径上（如战斗伤害事件）能用吗？",
          a: "谨慎使用。Builder 每次构建多一个中间对象，高频热路径会增加 GC 压力。热路径建议复用对象（对象池）或直接构造，Builder 留给低频复杂对象（邮件、配置、奖励包），并注意校验开销。"
        },
        {
          q: "JDK 里 StringBuilder 是建造者模式吗？",
          a: "严格说不是。建造者强调分步构建复杂对象、最后 build 出成品且过程可校验；StringBuilder 只是可变字符串的追加容器，没有 build 校验语义，也不隐藏构建过程，toString 只是取结果，更像可变累加器。"
        }
      ],
      memory: "建造者 = 奶茶店点单：「大杯 + 半糖 + 去冰 + 加珍珠 + 加椰果」链式说完，最后店员确认下单（build 校验）。传统构造器 = 报菜名式一口气念 12 个参数，念错一位就做错。",
      tags: ["建造者", "Builder", "Lombok", "游戏实战"]
    },
    {
      id: "design-pattern-12",
      level: 2,
      q: "装饰器模式和继承有什么区别？游戏里的「伤害加成叠加」「Buff 属性修正」能用装饰器实现吗？",
      point: "考察运行期组合增强与编译期继承的辨析，及伤害计算链等游戏数值场景的应用",
      approach: "先用附魔类爆炸的例子讲清继承的局限，引出装饰器「同一接口+层层包裹」。Java IO 一句话带过，重点落在伤害计算器装饰链的实现与顺序敏感性。关键加分点：主动指出需动态增删的 Buff 系统更适合修饰器列表而非装饰器，体现模式边界感。最后与代理模式辨析语义差异。",
      a: "核心结论：装饰器 = 运行期动态地给对象层层包装增强（组合 + 同一接口），继承是编译期静态扩展；需要「多种增强自由组合叠加」时，装饰器避免类爆炸。\n1. 继承的问题：假设武器有「火焰」「冰冻」「剧毒」三种附魔，组合要 7 个子类，N 种附魔要 2^N - 1 个类——类爆炸。装饰器每种附魔一个装饰类，运行期自由包裹：new PoisonEnchant(new FireEnchant(baseWeapon))。\n2. 结构：装饰器实现与被装饰者相同的接口，内部持有一个同接口对象（Component），方法里「先调被装饰者 + 再加自己的料」。Java IO 是教科书案例：new BufferedInputStream(new FileInputStream(...)) 层层套娃。\n3. 游戏伤害计算落地：伤害计算器接口 DamageCalculator，基础实现算裸伤害，装饰器依次叠加「暴击判定」「属性克制」「Buff 加成」「护盾减免」——new BuffDecorator(new CritDecorator(new BaseDamage()))，每种修正独立成类可测试，顺序可配。注意顺序敏感（先暴击后减免和先减免后暴击结果不同），这正是策划配表要定义的。\n4. Buff 属性修正的适配性：严格说 Buff 系统更适合「修饰器列表 + 遍历聚合」（属性计算器持有 BuffModifier 列表，按优先级排序依次 apply），因为 Buff 要支持动态增删（装饰器拆层很麻烦）。装饰器适合「相对固定的增强链」，动态增删场景用「责任链/修饰器列表」更顺手——面试能讲出这个边界是加分项。\n5. 和代理模式的辨析：结构上几乎一样，语义不同——代理控制「访问」（能不能调、怎么调），装饰器增强「功能」（调了再加料）。",
      followups: [
        {
          q: "为什么 Java IO 用装饰器而不是直接设计一个 BufferedFileInputStream？",
          a: "因为缓冲、加密、压缩、按行读等增强可以自由组合，每种组合一个类会类爆炸（2^N 种）。装饰器让每个增强独立成类，使用者按需层层包裹，这说明装饰器适合多种正交增强自由叠加的场景。"
        },
        {
          q: "装饰器链条顺序敏感在游戏数值里意味着什么？怎么让顺序可配置？",
          a: "顺序敏感意味着结果依赖计算顺序（先暴击后减免不等于先减免后暴击），必须由策划显式定义。做法：每个修正器配 priority 字段，装配时按优先级排序，顺序进配置表而非写死代码，调数值公式不用发版。"
        },
        {
          q: "MyBatis 的缓存（Cache 的层层装饰）是怎么用这个模式的？",
          a: "二级缓存用装饰器层层包装：基础 PerpetualCache 外按需套 LruCache（淘汰）、SerializedCache（序列化）、ScheduledCache（定时刷新）、SynchronizedCache（线程安全）等，每种能力一个装饰类，配置标签决定套哪几层。"
        }
      ],
      memory: "装饰器 = 穿衣服：内衣 → 毛衣 → 羽绒服，每层保暖叠加（功能增强），冷了就加一件（运行期动态），热了脱一件。继承 = 出生就定死的「抗寒体质」，想要「抗寒+防火」体质得重新生一个。",
      tags: ["装饰器", "继承", "Buff系统", "伤害计算", "游戏实战"]
    },
    {
      id: "design-pattern-13",
      level: 2,
      q: "门面模式（Facade）是什么？游戏服对外提供的接口聚合、跨系统组合操作适合怎么用？",
      point: "考察用门面收口跨系统编排的能力，及防止门面膨胀为上帝类的架构意识",
      approach: "先给定义：统一高层入口、隐藏子系统协作细节。用创角五系统编排、GM 封号、登录 Facade 三个例子展示「复杂流程一个方法收口」。再与中介者辨析单向与双向。收益与风险并重讲，重点是按业务域拆分门面避免上帝类。整体要体现你设计过对外接口的经验，而非背定义。",
      a: "核心结论：门面 = 为一组复杂子系统提供统一的高层入口，调用方只和门面打交道，不感知内部协作细节，降低耦合和误用概率。\n1. 结构：Facade 类持有多个子系统引用，把「组合多个子系统调用」的复杂流程封装成一个简单方法。\n2. 游戏服落地场景：\n   - 「创建角色」接口内部实际要：账号服校验 → 玩家数据服建档 → 背包服初始化背包 → 任务服初始化新手任务 → 邮件服发欢迎邮件。客户端只调 RoleFacade.createRole()，五个系统的编排细节全藏在门面后；\n   - GM 后台的「账号封禁」按钮：更新账号状态 + 踢下线 + 通知客服系统 + 记审计，一个 Facade 方法收口；\n   - 登录服的 LoginFacade：渠道验签 + token 签发 + 区服分配 + 登录日志，对外一个接口。\n3. 和中介者模式的区别：门面是「单向的」——调用方通过门面访问子系统，子系统之间依然可能互相调用；中介者是「双向的」——所有子系统只和中介者通信，子系统互相不直接说话。\n4. 收益：接口稳定（子系统重构不影响调用方）、事务边界清晰（门面方法天然是事务/补偿编排的落点）、新人友好（不用读懂五个系统才能写一个功能）。\n5. 风险：门面容易长成「上帝类」——什么都往里塞，要按业务域拆分多个门面（RoleFacade、PayFacade、GmFacade），而不是一个 GameFacade 包打天下。",
      followups: [
        {
          q: "微服务架构里的「BFF 层 / 聚合服务」和门面模式是什么关系？",
          a: "BFF/聚合服务就是门面在微服务下的形态：为客户端聚合并编排多个微服务调用，屏蔽内部拓扑。区别是 BFF 通常按端拆分，且要处理跨进程调用的超时、降级、补偿，比进程内门面复杂。"
        },
        {
          q: "门面方法里组合调用五个系统，其中第三个失败了怎么办？",
          a: "这是分布式事务问题，按一致性要求选方案：能收口到一个库的用本地事务；跨系统用补偿（对前两步做逆向操作）或 Saga 编排（每步配补偿动作）；可容忍的最终一致场景用失败重试加对账。门面方法是编排与补偿逻辑的天然落点。"
        },
        {
          q: "门面模式和适配器模式都在「包一层」，目的有什么不同？",
          a: "门面为简化：调用方不变，把多个子系统的复杂协作收敛成简单入口；适配器为兼容：双方接口不一致，适配器做翻译转换让协作成为可能。一个降低复杂度，一个消除不兼容，结构上都是包一层。"
        }
      ],
      memory: "门面 = 酒店前台：客人只说「我要入住」，前台背后协调房态、押金、早餐券、行李员（子系统），客人不用挨个部门跑。上帝类门面 = 一个前台管全城的酒店、医院、派出所——迟早累死。",
      tags: ["门面", "Facade", "接口聚合", "游戏实战"]
    },
    {
      id: "design-pattern-14",
      level: 2,
      q: "适配器模式是什么？接入第三方支付渠道、对接老系统接口时怎么用它？类适配器和对象适配器有什么区别？",
      point: "考察用适配器隔离接口差异的系统集成经验，及对象适配优先于类适配的判断",
      approach: "先给定义：接口翻译、最不侵入的缝合手段。重点讲支付渠道接入（统一 PayService，各渠道 Adapter 做方言翻译）和老系统对接（XML 转 DTO）两个实战。类适配器与对象适配器对比简明带过。最后主动抛出「适配器/门面/代理/装饰器四象限辨析」，这是面试官最爱追问的，主动讲显得体系完整。",
      a: "核心结论：适配器 = 把不兼容的接口包装成目标接口，让「接口不一致」的双方能协作，是系统集成时最不侵入的缝合手段。\n1. 两种形态：对象适配器（组合：适配器持有被适配者实例，推荐）和类适配器（多继承/实现+继承，Java 里少用，侵入性强）。\n2. 支付渠道接入落地：我们购买服对内的统一接口是 PayService（createOrder / verifyCallback / queryOrder），但微信、支付宝、苹果 IAP、各渠道 SDK 的入参出参千差万别。每个渠道写一个 Adapter 实现 PayService，内部把统一参数翻译成渠道方言、把渠道返回翻译成统一订单模型。主流程只依赖 PayService，渠道差异被隔离在适配层。\n3. 老系统对接落地：GM 后台重构时，老 PHP 系统的玩家查询接口返回 XML，新系统要 JSON DTO——写一个 LegacyPlayerAdapter，内部调老接口、解析 XML、转成新 DTO，新代码完全不用感知老系统的存在，后续老系统下线只删适配器。\n4. JDK/框架里的例子：Arrays.asList()（数组适配成 List）、InputStreamReader（字节流适配成字符流）、SpringMVC 的 HandlerAdapter（把各种形态的 Handler 适配成统一调用）。\n5. 和门面/代理/装饰器的四象限辨析（面试高频）：适配器改「接口签名」让双方兼容；门面简化「复杂度」；代理控制「访问」；装饰器增强「功能」。结构上都是包一层，意图完全不同。",
      followups: [
        {
          q: "为什么对象适配器优于类适配器？",
          a: "对象适配器用组合持有被适配者，符合组合优于继承：可以适配被适配者的整个子类体系、不受 Java 单继承限制、耦合更松。类适配器靠继承绑死一个具体类，侵入性强且无法适配接口族。"
        },
        {
          q: "防腐层（ACL, Anti-Corruption Layer）和适配器模式是什么关系？",
          a: "ACL 是 DDD 的战略概念，指在两个限界上下文之间建立隔离层，防止外部模型污染内部领域模型。适配器是 ACL 最常见的战术实现——每个外部接口一个适配器翻译协议并转换模型，外部变化只影响适配层。"
        },
        {
          q: "渠道接口升级（V1 到 V2）时，适配器层怎么平滑过渡？",
          a: "新增 V2 适配器实现同一统一接口，按配置或灰度开关路由；观察期双通道对账（同时调 V1/V2 比对结果）；全量稳定后下线 V1。渠道差异始终隔离在适配层内，主流程代码零改动。"
        }
      ],
      memory: "适配器 = 出国转换插头：国标电器（统一接口）插不进欧标插座（渠道 SDK），转换头一插就能用，电器和插座都不用改。门面是酒店前台（简化流程），适配器是插头（兼容接口）——一个管「省事」，一个管「能插」。",
      tags: ["适配器", "支付渠道", "系统集成", "游戏实战"]
    },
    {
      id: "design-pattern-15",
      level: 2,
      q: "组合模式是什么？技能树、装备套装、背包分组、组织架构这种树形结构怎么用它统一处理？",
      point: "考察对「叶子与容器统一接口」的理解，及技能树、套装等树形结构的游戏落地",
      approach: "先给定义：单个对象与集合同一接口，递归统一处理。讲清三角色后，用技能树、装备套装、背包分组、Buff 树四个例子证明树形结构在游戏里的普遍性。注意点部分（透明式 vs 安全式、引用泄漏、递归深度）是深度体现，别漏「新增节点类型不改遍历算法」这个核心收益。最后举 JDK 例子收尾。",
      a: "核心结论：组合模式 = 让「单个对象」和「对象集合」实现同一接口，调用方用统一方式处理树形结构的任意节点，递归操作不用区分叶子和容器。\n1. 结构三角色：Component 抽象（统一接口）、Leaf 叶子、Composite 容器（持有 children，方法里遍历 children 递归调用）。\n2. 游戏落地场景：\n   - 技能树：技能节点和技能分支统一抽象 SkillNode（isUnlocked() / getCost()），分支节点的解锁条件 = 所有前置子节点已解锁，递归计算整棵树的加点总消耗；\n   - 装备套装：单件装备和套装统一为 EquipmentComponent，套装的「属性总加成」= 遍历子装备聚合 + 套装额外词条；\n   - 背包分组：背包 → 标签页 → 格子，统计「总负重/总价值」一个 getWeight() 递归搞定；\n   - Buff 树：复合 Buff（如「狂暴 = 攻击提升 + 移速提升」）和原子 Buff 统一 apply/remove 接口。\n3. 收益：新增一种节点类型（如新的分组层级）不需要改遍历算法；客户端代码面对任意深度树都一视同仁。\n4. 注意点：\n   - 透明式（叶子也有 add/remove 空实现）vs 安全式（只有容器有），透明式客户端无脑统一但要处理叶子的非法调用；\n   - 树形结构在游戏服里要注意引用泄漏——节点从树中移除后，子树引用要清干净；\n   - 深树递归注意栈深度，必要时改迭代 + 显式栈。\n5. JDK 例子：AWT 的 Component/Container、JSF 的 UIComponent，以及文件系统抽象（文件/文件夹）。",
      followups: [
        {
          q: "透明式和安全式组合模式各有什么取舍？",
          a: "透明式：叶子和容器接口完全一致（叶子 add/remove 空实现或抛异常），客户端无需区分节点类型，但要容忍对叶子的非法调用；安全式：只有容器有子节点管理方法，类型安全但客户端要先判类型再操作，失去统一性。"
        },
        {
          q: "技能树的前置依赖如果是「环形」会发生什么？怎么防？",
          a: "环形前置会导致解锁判断死循环或永远无法解锁。防护：配置加载期做拓扑排序或 DFS 检测环，发现环直接报错拒绝加载；运行期递归遍历带 visited 集合防死循环。环检测应放在导表工具里，早于服务器启动。"
        },
        {
          q: "组合模式和访问者模式经常搭档出现，它们怎么配合？",
          a: "组合负责组织结构（树），访问者负责在结构上施加操作：遍历在树里，具体操作（导出 JSON、算总战力、校验配置）放访问者，新增一种操作只加一个访问者类、不改节点类。配合点是 accept(visitor) 方法挂在 Component 接口上。"
        }
      ],
      memory: "组合模式 = 军队编制：士兵（叶子）和军团（容器）都能执行「报数」（统一接口），军团报数 = 各连队报数之和（递归聚合）。统帅不用关心下面是一个兵还是一个师，一声令下统一执行。",
      tags: ["组合模式", "树形结构", "技能树", "游戏实战"]
    },
    {
      id: "design-pattern-16",
      level: 3,
      q: "综合运用题：设计游戏服的协议分发框架——客户端发来「协议号 + 二进制数据」，要路由到对应 Handler 执行。请说明你会组合哪些设计模式，各自解决什么点。",
      point: "考察模式组合的系统设计能力：能否讲清每个模式在分发框架中解决的具体问题",
      approach: "这题考体系化表达，按链路顺序讲：注解+工厂做注册路由、策略做分发、责任链做前置校验、模板方法做 Handler 骨架、命令做请求对象化入队、AOP 做横切。每个模式必须说清「解决什么问题、不用它会怎样」。建议主动画链路图收尾，并强调模式是为问题服务的组合拳——这才是面试官真正想听的，别把答案变成模式名罗列。",
      a: "核心结论：一个生产级协议分发框架至少组合 5 个模式：注解 + 工厂（注册）、策略（分发）、责任链（前置处理）、模板方法（Handler 骨架）、命令（请求对象化），模式是为问题服务的组合拳而非点缀。\n1. 注册与路由（注解 + 工厂 + 享元思想）：自定义注解 @Protocol(msgId = 1001) 标在 Handler 类上；启动时扫描指定包，构建 Map<Integer, Handler> 路由表（享元：协议号到 Handler 单例的映射共享一份）。新增协议 = 新建一个带注解的类，零配置。\n2. 分发（策略模式）：Dispatcher 收到解码后的请求，按协议号从路由表取 Handler 执行——本质是按 key 选策略，替代 switch 地狱。\n3. 前置处理（责任链）：请求进 Handler 前先过一条处理器链——登录态校验 → 频率限制（防协议轰炸）→ 黑白名单 → 参数校验，任一节点拦截即终止，节点可插拔、顺序可配。\n4. Handler 骨架（模板方法）：BaseHandler.handle() 固定流程：解析参数 → 权限钩子 → doHandle() 抽象业务 → 统一异常处理回错误码 → 打点日志。业务子类只写 doHandle。\n5. 请求对象化（命令模式）：请求封装成 RequestContext（玩家、协议号、参数、时间戳），投入 Disruptor 队列按玩家串行消费——请求可排队、可追踪、可统计。\n6. 横切增强（代理/AOP）：给特定 Handler 加耗时监控、参数日志，用代理或注解切面，不侵入业务代码。\n7. 面试呈现技巧：画「客户端 → 解码 → 责任链 → Dispatcher(查表) → Handler(模板方法) → 队列(命令)」的链路图，每个节点说出模式名 + 解决的问题 + 不用它会怎样——这才是面试官想听的「模式服务于问题」。",
      followups: [
        {
          q: "路由表构建为什么必须在所有 Handler 装配完成之后？Spring 里用什么扩展点保证时机？",
          a: "因为 Handler 依赖其他 Bean（Service、配置），过早注册会拿到未注入完整的实例。扩展点：实现 ApplicationListener<ContextRefreshedEvent> 或 SmartInitializingSingleton，容器就绪后再扫包构建路由表。"
        },
        {
          q: "协议号冲突（两个 Handler 标了同一个 msgId）怎么在启动期就炸出来？",
          a: "注册路由表时 put 前先 containsKey 检查，发现重复 msgId 直接抛异常让启动失败（fail fast），并打印冲突的两个类名。启动期炸出来远好于线上随机路由错。还可按业务模块分配协议号段预防冲突。"
        },
        {
          q: "如果要做协议的热更新（不停服新增 Handler），这套框架要改哪里？",
          a: "路由表要支持运行期增删（读写锁或 CopyOnWrite 保护）；新 Handler 通过热部署、Groovy 脚本或独立类加载器加载，校验协议号无冲突、依赖可注入后再注册。生产上更务实的做法是灰度服滚动更新，热更只用于紧急协议。"
        }
      ],
      memory: "协议分发 = 医院分诊台全流程：挂号处按科室建索引（工厂注册），分诊台按症状分科室（策略分发），进门先量体温查医保（责任链前置），每个科室问诊流程固定（模板方法），病历单全程跟着病人走（命令对象）。模式 = 各岗位的办事章法，拼起来才是医院。",
      tags: ["L3", "综合运用", "协议分发", "游戏实战"]
    },
    {
      id: "design-pattern-17",
      level: 3,
      q: "压力题：设计模式经常被批「过度设计」。你 10 年经验，怎么把握「用模式」和「过度设计」的度？见过哪些模式滥用？",
      point: "考察工程成熟度：能否用「痛点是否真实」的标准权衡模式使用，并识别模式滥用",
      approach: "先亮判断标准：痛点真实存在才上模式。用「三问」框架展开：变化真实吗、复杂度付得起吗、团队接得住吗，配 Rule of Three。然后举真实滥用案例（单例滥用、为模式而模式、过度分层、接口执念）。游戏服务实经验（热路径做减法）和收尾金句是记忆点。姿态要务实，但别显得为不用模式找借口。",
      a: "核心结论：模式是「对重复出现的痛点的命名解法」，判断标准就一条——痛点是否真实存在；为「想象中的变化」提前上模式，就是过度设计。\n1. 判断框架（三问）：\n   - 变化点真实吗？「以后可能接入其他渠道」如果有明确计划（下个季度接 iOS），提前抽象策略合理；如果只是「万一呢」，先写 if-else，第二次变化出现时再重构（Rule of Three：第三次重复才抽象）；\n   - 复杂度付得起吗？为一个只被调用两次的流程引入工厂 + 策略 + 模板方法三层抽象，读代码的人要跳五个文件才能看懂逻辑，维护成本超过收益；\n   - 团队接得住吗？再优雅的设计，团队新人看不懂、改不动，就是负资产。\n2. 见过的真实滥用：\n   - 单例滥用：什么工具类都静态单例，状态互相污染，测试无法隔离；\n   - 为模式而模式：三行业务硬套策略工厂，类数量是业务代码的十倍；\n   - 过度分层：Controller → Facade → Service → Manager → Helper → Dao，每层只做透传；\n   - 接口执念：只有一个实现的接口满天飞（IUserService/UserServiceImpl），没有多态场景就是噪音。\n3. 游戏服的务实经验：热路径（战斗结算、状态同步）优先直白高效，模式带来的间接层在高频路径上是真开销；低频管理路径（GM、活动配置）优先可扩展可维护，模式随便用。「性能敏感处做减法，变化频繁处做抽象」。\n4. 重构观：模式更多时候是「重构的目标」而不是「设计的起点」——先写能跑的直白代码，痛点出现了按《重构》手法演进到位，比一上来摆满模式更专业。\n5. 收尾金句：设计模式是武器库不是军规，十年经验和五年经验的差别不在于多会背 23 种模式，而在于知道什么时候不用。",
      followups: [
        {
          q: "YAGNI 原则是什么？和开闭原则冲突时怎么取舍？",
          a: "YAGNI：你不会需要它——不为想象中的需求写代码。与开闭冲突时：有明确计划的变化提前抽象（符合开闭），纯猜测的变化遵守 YAGNI 先简单实现，变化真实发生时再重构到开闭。取舍依据是变化的确定性和时间窗。"
        },
        {
          q: "怎么判断一个抽象是不是「错误的抽象」？错误的抽象和不抽象哪个危害大？",
          a: "错误抽象的特征：只有一两种实现却硬抽接口、调用方被迫理解多余概念、新增常见需求反而要改抽象层。错误抽象危害更大——不抽象只是重复，错误抽象是误导加枷锁，且调用方已依赖、更难拆。重复的代码比错误的抽象便宜。"
        },
        {
          q: "游戏热路径上，接口多态调用的真实开销有多大？JIT 怎么优化它？",
          a: "接口调用走 invokeinterface，理论上有虚表查找开销，但 JIT 有内联优化：单态/双态调用点直接内联成静态调用，开销接近零；只有真正多态（多实现频繁切换）才退化。热路径上多态通常不是瓶颈，真正的开销是间接层阻碍内联和额外对象分配。"
        }
      ],
      memory: "过度设计 = 为「可能下雨」背着全套潜水服出门。三问判断法 = 雨真的会下吗（变化真实）？潜水服多重（复杂度成本）？同行的人会帮忙背吗（团队接受度）？模式是药，先确诊再开方，Rule of Three = 症状出现三次才下诊断。",
      tags: ["L3", "过度设计", "重构", "工程观"]
    },
    {
      id: "design-pattern-18",
      level: 3,
      q: "设计题：游戏要高频上限时活动（双倍掉落/累计充值/限时商店/节日副本），用模板方法 + 策略 + 观察者 + 状态机设计一个配置化活动框架，讲清每个模式的位置。",
      point: "考察多模式协同的框架设计能力：状态机、模板、策略、观察者在活动框架中的分工",
      approach: "先立总纲：新活动只写差异、复用骨架，四个模式各管一层。按状态机→模板方法→策略→观察者逐层讲位置与解决的问题，再用配置化收尾（策划自助配活动、程序零发版）。兜底设计（开关、落盘、跨天处理）是拉开差距的部分，要主动讲。全程保持「模式服务于问题」的叙述线，别变成模式罗列。",
      a: "核心结论：活动框架的目标是让新活动「只写差异、复用骨架」，四个模式各管一层——模板方法管生命周期骨架，策略管玩法差异，观察者管事件触发，状态机管活动生命周期流转。\n1. 状态机（生命周期控制）：活动状态枚举「未开启 → 预热 → 进行中 → 领奖期 → 已结束」，迁移由时间驱动（定时器触发状态切换事件）。玩家所有活动请求先过状态门——进行中才能参与、领奖期只能领奖，非法请求结构性被拒，不用每个 Handler 手写 if 判断。\n2. 模板方法（活动骨架）：BaseActivity 抽象类定义 final 生命周期：onStart（加载配置、初始化排行/进度）→ onPlayerAction（参与资格校验 → doAction() 抽象玩法 → 记录进度 → 触发事件）→ onEnd（结算、发奖、清理）。通用逻辑（资格校验、进度持久化、打点）父类收口。\n3. 策略模式（玩法差异）：双倍掉落、累计充值、限时商店的「参与逻辑、奖励计算」各不相同，抽象 ActivityPlayStrategy，每个活动类型一个策略实现。策划配置表里 activityType 字段决定装配哪个策略——新活动类型 = 新策略类 + 新配置行。\n4. 观察者（事件驱动）：活动进度不该硬编码监听「玩家充值/击杀/合成」等几十个业务事件。业务侧 fireEvent(RechargeEvent)，活动框架的事件监听层把事件路由给关心它的活动（双倍掉落监听 KillEvent，累计充值监听 RechargeEvent）。业务系统和活动系统彻底解耦——加活动不改业务代码。\n5. 配置化收尾：活动时间、参数、奖励全走配置表（导表工具生成），策划自助配活动，程序零发版上线。配置热更用「整对象替换」保证原子性。\n6. 兜底设计：活动开关（出事故配置层一键关闭）、进度数据落盘策略、跨天/跨服边界处理——面试官深挖时这些才是拉开差距的部分。",
      followups: [
        {
          q: "活动状态的迁移由谁驱动？服务器重启时正好跨状态切换点怎么办？",
          a: "迁移由时间驱动：定时器周期检查活动配置的开始/结束时间触发状态切换。重启时按「当前时间 vs 配置时间表」重算应有状态并直接对齐（状态机幂等重放），再补必要的结算/初始化逻辑，保证重启不丢状态切换。"
        },
        {
          q: "几十个活动同时监听充值事件，怎么控制监听器的执行开销？",
          a: "事件路由层按事件类型维护订阅表，一次事件只分发给订阅了该类型的活动而非全量遍历；每个活动监听逻辑做短路（未开启直接跳过）；进度更新聚合批量落盘而非逐条写库；必要时事件入队异步消费，主线程只投递不执行。"
        },
        {
          q: "同一个玩家在同一活动里的并发请求（客户端连点领奖按钮）框架层面怎么防？",
          a: "框架层用玩家维度串行化兜底：同一玩家的请求进入其专属逻辑线程/队列（Disruptor 按 playerId 哈希分发），天然串行。再加业务幂等：领奖校验奖励状态（已领取直接返回），必要时加乐观锁，连点不会重复发奖。"
        }
      ],
      memory: "活动框架 = 商场运营部：状态机是商场营业时间牌（开门才能进、打烊只能退），模板方法是每次促销的 SOP（布场→活动→撤场），策略是每次促销的具体玩法（满减/抽奖/秒杀各一套），观察者是客流传感器（顾客进门自动触发欢迎语）。策划 = 商场企划，填表就能开活动，不用找工程队（程序）改造商场。",
      tags: ["L3", "活动框架", "综合运用", "游戏实战"]
    }
  ]
};
