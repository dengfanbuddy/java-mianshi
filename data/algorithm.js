window.QB = window.QB || {};
window.QB["algorithm"] = {
  id: "algorithm",
  name: "数据结构与算法",
  icon: "🧮",
  desc: "成都游戏公司笔试上机必考：手写 LRU、链表反转、二分变体、TopK、排序复杂度、跳表/红黑树思想、限流算法手写、海量数据处理、一致性哈希，全部按面试手写标准准备并结合游戏场景。",
  questions: [
    {
      id: "algorithm-01",
      level: 1,
      q: "时间复杂度怎么分析？O(1)、O(log n)、O(n)、O(n log n)、O(n²) 各举一个游戏业务里的真实例子。",
      a: "核心结论：复杂度分析看「输入规模 n 增长时，基本操作次数的增长趋势」，抓主导项、忽略常数；面试要能从代码结构直接读出复杂度。\n1. 分析规则：顺序语句取最大；循环看次数（两层嵌套通常 O(n²)，但内层边界固定时是 O(n)）；二分/折半类是 O(log n)；递归画递归树（如归并 T(n) = 2T(n/2) + O(n) = O(n log n)）。\n2. 游戏业务例子：\n   - O(1)：按玩家 ID 从 ConcurrentHashMap 取玩家对象；\n   - O(log n)：在有序战力数组里二分定位某分数的排名；\n   - O(n)：遍历在线玩家列表做全服广播；\n   - O(n log n)：排行榜排序（ZSet 内部跳表维护是 O(log n) 单点插入，但全量排序是 n log n）；\n   - O(n²)：新手写法「每个玩家遍历所有玩家算距离做 AOI」——万人同屏就是 10^8，必须换九宫格索引。\n3. 均摊复杂度：ArrayList 的 add 是 O(1) 均摊（扩容 O(n) 摊薄到每次插入）；HashMap get 是 O(1) 均摊（rehash 同理）——面试提「均摊」显专业。\n4. 空间复杂度同步回答：递归深度（栈空间）、辅助数组，写算法时主动说「时间 O(n)、空间 O(1)，原地操作」。\n5. 实战敏感度：游戏服单玩家逻辑帧预算可能只有几毫秒，任何 O(n²) 出现在热路径都是事故；面试主动把复杂度和帧预算挂钩，直接区别于纯刷题选手。",
      point: "考察能否从代码结构直接读出复杂度，并把复杂度与游戏服帧预算挂钩，而非只背定义。",
      approach: "先给结论「看增长趋势、抓主导项」，再给四条分析规则（顺序取最大、循环看次数、二分对数、递归画树）。每档复杂度配一个游戏业务实例证明实战感，主动补均摊复杂度和空间复杂度，收尾挂钩帧预算。坑：别把嵌套循环一律说成 O(n²)，内层边界固定时是 O(n)；别忘了答空间复杂度。",
      followups: [
        { q: "O(n²) 的双重循环，什么情况下其实可以算 O(n)？", a: "当内层边界是常数或总迭代次数与 n 线性相关时。如内层固定跑 10 次，或双指针类结构两指针总移动 2n 次。判断标准是数基本操作总次数，而不是数循环层数。" },
        { q: "为什么说 HashMap 的 get 是「均摊 O(1)」而不是「最坏 O(1)」？最坏是什么？", a: "扩容 rehash 要迁移全部桶，单次 O(n)，摊薄到 N 次插入后每次 O(1)，故称均摊。最坏是所有 key 哈希碰撞挤在一个桶成链，get 退化 O(n)，Java 8 树化后降为 O(log n)。" },
        { q: "递归求斐波那契的复杂度是多少？怎么从 O(2^n) 优化到 O(n) 再到 O(log n)？", a: "朴素递归 O(2^n)，递归树每层节点翻倍。记忆化或自底向上 DP 降到 O(n)；再用矩阵快速幂 [[1,1],[1,0]]^n 可达 O(log n)，这是面试标准三连问。" }
      ],
      memory: "复杂度 = 餐厅出菜速度和客流的关系：O(1) 自助餐（人再多拿菜一样快），O(log n) 查字典点菜（每次翻一半），O(n) 传菜员逐桌问，O(n²) 每桌都和每桌拼桌社交。游戏服帧预算 = 上菜超时顾客就摔门（玩家卡顿流失）。",
      tags: ["复杂度", "基础", "游戏实战"]
    },
    {
      id: "algorithm-02",
      level: 1,
      q: "手写一个 LRU 缓存（get/put 都是 O(1)），讲清数据结构选型和每一步操作。笔试必考题。",
      a: "核心结论：LRU = HashMap 负责 O(1) 定位 + 双向链表负责 O(1) 维护访问顺序，头部是最新、尾部是最老，淘汰尾部。\n1. 数据结构选型原因：要 O(1) 查找只有哈希表；要 O(1) 「移动节点到头部 + 删除尾部」只有双向链表（单链表删除要 O(n) 找前驱）。两者合体：Map<key, Node>，Node 里存 key、value、prev、next（Node 必须存 key，否则淘汰尾节点时无法反查删 Map 项）。\n2. get(key)：Map 查不到返回 -1；查到则把该节点「摘除 → 插入头部」，返回 value。\n3. put(key, value)：已存在 → 更新值 + 移到头部；不存在 → 新建节点插头部 + 入 Map；size 超容量 → 删尾节点 + Map 里 remove 尾节点 key。\n4. 手写要点（面试官盯着看的细节）：用虚拟头尾哨兵节点（dummy head/tail），消除空指针判断，代码量减半；「摘除节点」和「插入头部」抽成私有方法；每个指针操作都要 prev/next 双向更新。\n5. Java 取巧版与追问应对：继承 LinkedHashMap（accessOrder = true，重写 removeEldestEntry）三行搞定，但笔试要主动说「这是取巧版，我手写原生版」，然后写双向链表版。\n6. 游戏服落地：本地缓存玩家最近访问的配置/会话，LRU 淘汰冷数据；追问并发——手写版线程不安全，游戏服要么按玩家逻辑线程隔离（单线程内用），要么分段锁，要么直接用 Caffeine。",
      point: "考察数据结构组合设计能力：哈希表定位加双向链表维护顺序，以及手写时的工程细节。",
      approach: "先一句话给方案「HashMap + 双向链表」，讲清选型原因（为什么单链表不行）。按 get、put 两条路径讲操作，手写时用哨兵节点、抽私有方法体现工程素养。主动提 LinkedHashMap 取巧版再写原生版，收尾谈并发与游戏服落地。坑：淘汰尾节点时忘记从 Map 删 key；Node 漏存 key。",
      followups: [
        { q: "Node 里为什么还要存 key？只存 value 不行吗？", a: "淘汰尾节点时手里只有 Node，必须靠 Node.key 反查删除 HashMap 中的对应项；只存 value 就删不掉 Map 项，容量会泄漏，缓存永远清不干净。" },
        { q: "并发环境下这个 LRU 会出什么问题？怎么改成分段锁版本？", a: "get 和 put 都改链表指针，并发下指针错乱甚至成环死循环。分段锁：按 key 哈希分 N 段，每段独立小 LRU 加独立锁，冲突降 N 倍；或按玩家逻辑线程隔离单线程用，或直接上 Caffeine。" },
        { q: "LRU 和 LFU 的区别？LFU 怎么实现 O(1)？", a: "LRU 按最近使用时间淘汰，LFU 按访问频次淘汰。LFU O(1) 做法：Map 存 key 到节点，再加 freq 到双向链表的分层 Map，更新时节点从旧频次链表移到新频次链表头部，淘汰 minFreq 链表尾部。" }
      ],
      memory: "LRU = 办公桌 + 文件柜索引：HashMap 是索引卡（按编号秒查文件在哪），双向链表是桌面上的文件摞（用过的放最上面 moveToHead，桌面满了最下面那份扔掉 removeTail）。哨兵节点 = 桌面两头的挡板，永远不动，省得每次都担心文件摞是空的。",
      tags: ["LRU", "手写", "双向链表", "笔试必考"]
    },
    {
      id: "algorithm-03",
      level: 1,
      q: "快速排序和归并排序的原理、复杂度、稳定性分别是什么？为什么说快排是工程排序之王？",
      a: "核心结论：快排 = 分治 + 原地分区（平均 O(n log n)，最坏 O(n²)，不稳定）；归并 = 分治 + 合并有序段（稳定 O(n log n)，但要 O(n) 额外空间）；工程选快排赢在缓存友好和原地。\n1. 快排：选 pivot，一趟 partition 把小于 pivot 的放左、大于的放右，递归左右两段。平均 O(n log n)；最坏（每次 pivot 是极值，如有序数组选首元素）退化 O(n²)——工程上用随机 pivot 或三数取中规避。空间 O(log n)（递归栈）。不稳定（partition 交换会打乱相等元素相对顺序）。\n2. 归并：递归拆到单个元素，两两合并有序数组。任何情况 O(n log n)，稳定，但要 O(n) 辅助数组。\n3. 快排为什么是工程之王：原地分区顺序访问内存，CPU 缓存命中率高；归并每次合并要开临时数组，缓存不友好且分配开销大。实测快排常数小得多。Java 基本类型排序用双轴快排（DualPivotQuicksort），对象排序用 TimSort（归并改良，保稳定性）。\n4. 稳定性业务意义：排行榜「同分先到达的排前面」——如果排序算法稳定，按分数排序前先按时间排即可保留次序；不稳就要把「分数 + 时间戳」合成复合 key 再排（ZSet 的 score 设计就是这个思路）。\n5. 追问储备：堆排（O(n log n) 原地但不稳定，缓存不友好用得少）；计数/桶排（数据范围小时 O(n)，如年龄、等级段统计）；TimSort 思想（利用数据已有序性，实测最快）。",
      point: "考察对排序复杂度与稳定性的精确理解，以及工程选型（缓存友好、JDK 实现）的见识。",
      approach: "先给对比结论：快排原地不稳定、归并稳定费空间。分别讲原理、复杂度和最坏情况，回答「工程之王」时落到缓存命中率和常数因子。主动讲 JDK 实现（基本类型双轴快排、对象 TimSort），用排行榜同分案例讲稳定性的业务意义。坑：忘提快排最坏 O(n²) 及规避手段；说不出稳定性是什么。",
      followups: [
        { q: "快排最坏 O(n²) 怎么避免？三数取中和随机化各是什么思路？", a: "随机化：随机选元素与首元素交换再分区，把最坏情况压成极低概率；三数取中：取首中尾三元素中位数做 pivot，对近似有序数组效果好。两者都把人为构造的最坏输入化解为概率问题。" },
        { q: "Java 的 Arrays.sort 对 int[] 和 Object[] 用的算法为什么不同？", a: "基本类型用双轴快排：无稳定性要求，双 pivot 分区减少比较次数、缓存友好；对象数组用 TimSort（归并改良）：必须保稳定性，且利用真实数据已有的有序片段 run 加速。" },
        { q: "一亿个玩家的等级数据（1~200 级）排序，你会选什么算法？", a: "计数排序。值域只有 200，开 200 个桶的计数数组，一遍统计、一遍展开，O(n) 碾压任何比较排序的 n log n。识别信号：数据范围远小于数据量。" }
      ],
      memory: "快排 = 军训排队：随便挑个标杆（pivot），比他矮的站左边高的站右边，左右两队各自再来一遍。归并 = 两副各自理好的扑克牌合成一副（合并有序段）。稳定性 = 同分数的人谁先报名谁排前，报名顺序不能被打乱。",
      tags: ["排序", "快排", "归并", "稳定性", "基础"]
    },
    {
      id: "algorithm-04",
      level: 1,
      q: "手写二分查找，并讲清楚它的经典变体：游戏排行榜「找到第一个大于等于某分数的位置」怎么写？",
      a: "核心结论：二分的难点从来不在思想而在边界——区间定义（闭区间还是左闭右开）必须全程一致，mid 取法防溢出，变体题统一套「找第一个满足条件的位置」模板。\n1. 标准版（闭区间 [left, right]）：while (left <= right)，mid = left + (right - left) / 2（防 (left + right) 溢出）；目标小于 mid 则 right = mid - 1，大于则 left = mid + 1，相等返回。\n2. 变体「第一个 >= target 的位置」（lower_bound）：while (left < right)，nums[mid] >= target 则 right = mid（mid 可能是答案，区间不能丢），否则 left = mid + 1（mid 一定不是）；退出时 left 即答案。核心口诀：「谁可能是答案，区间就保留谁」。\n3. 游戏落地：排行榜数组按分数降序，玩家问「我这个分数能排多少名」——转成「第一个分数 <= 我的分数的位置」；等级经验表查「当前经验对应多少级」——找最后一个 <= 当前经验的配置行。导表工具生成的配置查表大量用二分。\n4. 二分的本质扩展：二分不只能查有序数组，还能「二分答案」——当答案单调可判定（如「最小的背包容量能装下所有道具」「最长的活动周期不超预算」），判定函数单调就能对答案空间二分。面试说出这层直接升档。\n5. 笔试注意：写完主动过边界用例——空数组、单元素、目标不存在、重复元素全相同；这是面试官判断「手写熟练度」的观察点。",
      point: "考察二分边界控制的严谨性和「二分答案」思维层次，而非只会默写标准版。",
      approach: "先点破「难点在边界不在思想」，讲清区间定义全程一致和防溢出 mid 写法。给出「第一个 >= target」模板和口诀「谁可能是答案，区间就保留谁」，结合排行榜与经验表讲落地，主动升维到二分答案。坑：写完不过边界用例；区间定义中途切换导致死循环或漏解。",
      followups: [
        { q: "为什么 mid 要写成 left + (right - left) / 2？Java 里 int 会溢出，那 C++ 呢？", a: "防止 left + right 直接相加溢出 int 上限变负数，导致数组越界或死循环。C++ 的 int 同样溢出，写法一致；Java 还可用无符号右移 (left + right) >>> 1 规避。" },
        { q: "lower_bound 和 upper_bound 的区别？怎么用它们求某个值出现的次数？", a: "lower_bound 找第一个 >= target 的位置，upper_bound 找第一个 > target 的位置。出现次数 = upper_bound(target) 减 lower_bound(target)，两次 O(log n) 搞定有序数组频次统计。" },
        { q: "「二分答案」的判定函数需要满足什么性质才能二分？", a: "单调性：答案 x 可行则比 x 大（或小）的都可行，形成不可行到可行的单调分界。有单调性才能对答案空间二分，每轮用判定函数验证一侧，把搜索问题转成判定问题。" }
      ],
      memory: "二分 = 翻字典：闭区间版是「每次把目标页撕掉一半」；变体版口诀「可能是答案的半边留着」= 找第一个姓张的，看到张就往前翻（保留当前页），看到李就往后翻（当前页扔掉）。边界 bug = 字典多翻一页或少翻一页，口诀一致就永不翻错。",
      tags: ["二分查找", "手写", "排行榜", "游戏实战"]
    },
    {
      id: "algorithm-05",
      level: 1,
      q: "手写反转单链表（迭代法和递归法都要会），并讲讲链表相关的兄弟题型。",
      a: "核心结论：反转链表是指针操作的基本功验收题，迭代法三个指针（prev/curr/next）滚一遍是标准答案，递归法考察对递归栈的理解。\n1. 迭代法：prev 初始 null，循环里① 暂存 next = curr.next（先保住后继，否则断链丢失）；② curr.next = prev（掉头）；③ prev = curr、curr = next（双指针前进）。curr 为 null 时 prev 就是新头。时间 O(n) 空间 O(1)。\n2. 递归法：reverseList(head) = 假设 head.next 之后的子链已反转好（返回值 newHead 透传），只需 head.next.next = head（后继指回自己）、head.next = null（防成环）。空间 O(n)（递归栈），游戏服热路径别用深递归。\n3. 高频踩坑点：不暂存 next 就掉头（链断丢数据）；返回错节点（返回 curr 是 null，要返回 prev）；循环结束条件写错。\n4. 兄弟题型串讲（面试官顺势追问）：\n   - 找链表中点/倒数第 K 个：快慢指针，快指针先走 K 步或两倍速；\n   - 判环 + 找环入口：Floyd 快慢指针，相遇后一个回头部同速走，再相遇即入口（要会说数学原理：2(a+b) = a+b+n(b+c) 推出 a = c）；\n   - 合并两个有序链表：哑节点 + 双指针，和归并排序的合并段同款。\n5. 游戏服联系：Netty Pipeline 本质是双向链表；手写 LRU 里的双向链表操作（摘除/插头）就是反转链表的进阶版，两题一起准备事半功倍。",
      point: "考察指针操作基本功与边界严谨性，迭代与递归两种思维都要求过关。",
      approach: "先声明这是基本功验收题。迭代法按「暂存 next、掉头、双指针前进」三步讲清顺序原因；递归法讲「假设子问题已完成」的思维。主动列三个高频踩坑点展示踩过坑，串讲兄弟题型（中点、判环、合并有序链表）展示体系化。坑：返回 curr 而不是 prev；不暂存 next 就掉头。",
      followups: [
        { q: "递归版反转链表的空间复杂度是多少？什么场景下会爆栈？", a: "O(n)，每层递归压一层栈帧。链表长度到几万层就可能 StackOverflowError（JVM 默认栈约 1M）。游戏服热路径或数据量不可控的场景必须用迭代版。" },
        { q: "怎么只反转链表的中间一段（第 m 到 n 个节点）？", a: "分段处理：先走 m-1 步定位前驱 guard，对 m 到 n 段做 n-m 次标准掉头，最后拼接三段——guard.next 接新段头、旧段头接原 n 的后继。配 dummy 头节点统一 m 等于 1 的边界。" },
        { q: "双向链表反转和单链表反转差在哪？", a: "双向链表每个节点要同时交换 prev 和 next 两个指针，每步是一次 swap 而非单纯掉头；好处是 prev 天然暂存无断链风险，代价是指针操作翻倍、容易漏改一头导致遍历错乱。" }
      ],
      memory: "迭代反转 = 铁路调头：三个人协作——next 先去前面占住下一节车厢（暂存），curr 摘钩掉头接上前一节（curr.next = prev），然后三人整体前移。先占位再掉头，顺序反了车厢就丢了（断链）。",
      tags: ["链表", "手写", "反转链表", "笔试必考"]
    },
    {
      id: "algorithm-06",
      level: 1,
      q: "栈和队列分别适合什么场景？怎么用两个栈实现一个队列？括号匹配为什么天然用栈？",
      a: "核心结论：栈是「后进先出」处理嵌套/回退类问题，队列是「先进先出」处理排队/缓冲类问题；两栈实现队列的关键是「倒栈只在输出栈空时做」，保证均摊 O(1)。\n1. 栈的典型场景：括号匹配、表达式求值、浏览器前进后退、函数调用栈、递归改迭代、单调栈（下一个更大元素）。游戏服例子：技能连招的嵌套状态回退、协议解析的嵌套结构。\n2. 队列的典型场景：消息缓冲、BFS 层序遍历、线程池任务队列、生产者消费者。游戏服例子：登录排队系统（开服限流排队进游戏）、Disruptor 的 RingBuffer 本质也是环形队列。\n3. 两栈实现队列：in 栈负责入队（push 直接进 in），out 栈负责出队——出队时若 out 非空直接 pop；若 out 为空，把 in 全部倒入 out 再 pop。均摊 O(1)：每个元素「进 in、出 in、进 out、出 out」各一次共 4 次操作，摊到每次出队是常数。追问陷阱：「每次出队都倒栈」就退化成 O(n) 了。\n4. 括号匹配为什么用栈：括号是严格嵌套结构，最近的左括号必须最先被匹配——天然 LIFO。遍历遇到左括号入栈，右括号弹出栈顶比对，不匹配或栈空即非法，结尾栈必须为空。\n5. 扩展储备：单调栈求「每个元素右侧第一个更大值」（维护递减栈）；用队列实现栈（每次入队后把前面元素重新排到队尾）；循环队列用数组实现时「牺牲一个格子区分空满」还是「加 size 计数」。",
      point: "考察对 LIFO 与 FIFO 语义的场景匹配能力，以及两栈实现队列的均摊分析。",
      approach: "先用一句话区分两者语义，各给两三个场景（含游戏服例子：登录排队、Disruptor）。两栈队列重点讲「懒惰倒栈」和均摊 O(1) 的论证过程。括号匹配点出「最近先匹配等于 LIFO」的本质，收尾给单调栈等扩展。坑：把均摊 O(1) 说成每次 O(1)；每次出队都倒栈导致退化。",
      followups: [
        { q: "两栈实现队列为什么「倒栈要懒惰」？均摊分析怎么做？", a: "只有 out 栈空时才把 in 整体倒入 out，保证每个元素只经历进 in、出 in、进 out、出 out 四次操作，N 次操作总成本 4N，摊到每次 O(1)。若每次都倒，单次出队 O(n) 整体退化。" },
        { q: "循环队列数组实现，队空和队满的区分条件有哪两种方案？", a: "方案一牺牲一个存储格：(rear + 1) % capacity == front 判满，rear == front 判空；方案二额外维护 size 计数器，size 为 0 空、等于 capacity 满。前者省维护，后者空间利用满。" },
        { q: "单调栈适合解决哪一类问题？能现场说一个模板吗？", a: "解决「每个元素左或右侧第一个更大或更小值」类问题。模板（求右侧第一个更大）：维护递减栈，新元素比栈顶大则栈顶出栈、新元素即其答案，随后新元素入栈。每元素进出栈各一次，O(n)。" }
      ],
      memory: "栈 = 单手洗盘子：最后放上去的最先洗（括号嵌套：最里面的最先闭合）。两栈实现队列 = 两个垃圾桶倒替：扔垃圾进 A 桶，要取时把 A 整桶扣进 B 桶再取——B 桶没倒空绝不重复倒（懒惰倒栈），否则白费劲。",
      tags: ["栈", "队列", "手写", "基础"]
    },
    {
      id: "algorithm-07",
      level: 1,
      q: "哈希表思想解决什么问题？两数之和怎么写？游戏里「统计当前在线的工会数」「道具去重」怎么用哈希做？",
      a: "核心结论：哈希表 = 用空间换时间，把「查找/计数/去重」从 O(n) 降到 O(1)，套路只有三招：存出现、存位置、存计数。\n1. 两数之和（LeetCode 1）：暴力 O(n²) 双重循环；哈希版一次遍历——对每个 num 查 target - num 是否在 Map 里，在则返回两个下标，不在则把 num 和下标存进 Map。O(n) 时间 O(n) 空间。注意「先查后存」，避免同一个元素被自己匹配（如 target = 6、num = 3 用两次）。\n2. 三招套路：\n   - 存出现（Set 去重/判重）：背包发奖前去重，Set 存 itemId 判断重复道具；\n   - 存位置（Map 存下标）：两数之和、滑窗里存字符最后出现位置；\n   - 存计数（Map 做频率统计）：统计在线玩家的工会分布 Map<guildId, count>，一行 merge(guildId, 1, Integer::sum)。\n3. 游戏实战例子：全服广播前统计「当前在线玩家分属多少不同工会」——Set<guildId> 一遍遍历 O(n)；「判断玩家今日奖励是否重复领取」——Set 存 (playerId, activityId) 组合 key；敏感词预处理的词库本身就是哈希集合。\n4. 哈希的代价与追问：哈希冲突（链地址/开放寻址）、扩容 rehash 开销、最坏 O(n)（哈希碰撞攻击）；游戏服热路径要预估容量避免频繁 rehash（new HashMap<>(expectedSize / 0.75 + 1)）。\n5. 进阶关联：哈希思想是海量数据题的总开关——bitmap、布隆过滤器、分治哈希切分都是它的衍生，为后续 L2 题埋伏笔。",
      point: "考察「空间换时间」的哈希三招套路化应用，以及对冲突、扩容代价的认识。",
      approach: "先给总纲「存出现、存位置、存计数」三招。两数之和演示一次遍历并强调先查后存的细节，用工会统计、道具去重两个游戏例子各对应一招。主动谈哈希冲突与 rehash 代价、预估容量，收尾把哈希升为海量数据题的总开关。坑：两数之和先存后查导致元素自匹配。",
      followups: [
        { q: "两数之和如果数组有序，有更省空间的做法吗？", a: "对撞双指针：left 指头、right 指尾，和小了 left 右移，大了 right 左移，O(n) 时间 O(1) 空间。前提是有序；无序数组先排序要 O(n log n) 不划算，仍选哈希。" },
        { q: "HashMap 的容量为什么要预估算好？rehash 一次的开销有多大？", a: "默认负载因子 0.75，超限就翻倍扩容并把全部桶重新散列，一次 rehash 是 O(n) 全量迁移，热路径上就是毛刺。初始化用 expectedSize / 0.75 + 1 预估容量，避免多次 rehash。" },
        { q: "组合 key（playerId + activityId）怎么设计才不会有拼接歧义？", a: "别用简单字符串相加，12 拼 3 与 1 拼 23 会撞车。方案：加分隔符且保证字段不含分隔符；或位运算合成 long（playerId 左移 32 位或上 activityId）；或定义小对象重写 equals 和 hashCode，最稳妥。" }
      ],
      memory: "哈希三招 = 前台登记表：存出现 = 签到表（来过没），存位置 = 座位表（坐哪了），存计数 = 打卡次数统计。两数之和 = 进店先看「搭档是不是已经在等了」（先查后存），别把自己登记两遍。",
      tags: ["哈希", "两数之和", "手写", "游戏实战"]
    },
    {
      id: "algorithm-08",
      level: 2,
      q: "跳表（SkipList）的原理是什么？如果让你手写一个跳表的 insert，核心步骤是什么？为什么 Redis ZSet 选它而不是红黑树？",
      a: "核心结论：跳表 = 有序链表 + 多层随机索引，用「以空间换时间 + 概率平衡」达到 O(log n) 的增删查，实现远比平衡树简单，且天然支持高效范围查询。\n1. 结构：底层是完整有序链表；每个节点以概率 p（Redis 是 1/4）向上晋升一层索引，层数期望 O(log n)。查找从头节点最高层开始，「能往右就往右，不能就下一层」，像高速公路-国道-县道分级赶路。\n2. 手写 insert 核心四步：\n   - 随机生成新节点层数（抛硬币直到反面，概率 p 晋升）；\n   - 从最高层往下走，记录每一层「插入位置的前驱节点」到 update[] 数组（这是手写题的关键数据结构）；\n   - 逐层做标准链表插入：newNode.next = update[i].next; update[i].next = newNode；\n   - 更新节点数；若值已存在则直接更新（按语义）。\n3. 为什么 ZSet 用跳表而非红黑树（高频深挖）：\n   - 范围查询（ZRANGEBYSCORE）跳表 O(log n) 定位后顺序遍历即可；红黑树要找起点再中序遍历，实现繁琐；\n   - 实现简单：跳表插入只动相邻指针，红黑树要旋转 + 变色，代码量数倍且难写对；\n   - 并发友好：跳表可以做无锁/细粒度锁（插入只影响局部），红黑树旋转波及范围大；\n   - 内存实际更省：跳表每节点期望 1/(1-p) 个指针（p=1/4 时约 1.33），红黑树每节点固定 2 个指针 + 颜色位。\n4. 复杂度：增删查期望 O(log n)，空间 O(n)。「期望」二字是概率结构的标准表述。\n5. 游戏落地：排行榜（ZSet）、延迟队列（时间轮之外的方案：score = 执行时间戳的跳表，到期的批量取头部）。",
      point: "考察概率型平衡结构的原理理解与手写能力，以及与红黑树的工程选型对比。",
      approach: "先给「有序链表加多层随机索引」定义，用分级路网比喻讲查找路径。手写 insert 突出 update 前驱数组这个关键数据结构。背熟 ZSet 选跳表的四点理由（范围查询、实现简单、并发友好、内存），给游戏落地场景。坑：insert 忘记记录每层前驱；把「期望 O(log n)」说成保证。",
      followups: [
        { q: "跳表的层数晋升概率 p 设大设小各有什么影响？", a: "p 大：索引层多、查找更快，但内存大、插入维护层数多；p 小：省内存但索引稀疏、查找变慢。Redis 取 1/4，每节点期望约 1.33 个指针，是时间与空间的平衡点。" },
        { q: "跳表和 B+ 树都是多层索引，本质区别是什么？各自适合内存还是磁盘？", a: "跳表是内存结构：随机指针跳转、概率平衡，追求实现简单；B+ 树为磁盘而生：多叉矮胖、一个节点对齐一个磁盘页，把 IO 次数压到最少。指针跳转对内存廉价，对磁盘是随机 IO 灾难。" },
        { q: "跳表怎么做并发安全的插入？", a: "插入只影响相邻节点，天然适合细粒度锁：对要修改的每层前驱逐层加锁，验证前驱关系没变再链接；也可无锁化，用 CAS 做链表插入、失败重试，ConcurrentSkipListMap 就是这个思路。" }
      ],
      memory: "跳表 = 城市分级路网：高速（顶层索引）跨城、国道（中层）跨区、县道（底层）到户。找路先上高速，快到了逐级下道（能往右就往右，不能就下楼）。盖楼（晋升层数）靠抛硬币，不用像红黑树那样请专业施工队（旋转变色）精确找平。",
      tags: ["跳表", "手写", "Redis", "ZSet", "L2"]
    },
    {
      id: "algorithm-09",
      level: 2,
      q: "红黑树的核心性质是什么？它靠哪两种操作维持平衡？为什么工程上（HashMap、TreeMap、epoll）偏爱红黑树而不是 AVL？",
      a: "核心结论：红黑树 = 用五条性质约束的「近似平衡」二叉搜索树，靠变色和旋转维持平衡，牺牲一点平衡度换取插入删除时更少的调整次数。\n1. 五条性质（要能背）：\n   - 每个节点非红即黑；\n   - 根是黑色；\n   - 叶子（NIL 空节点）是黑色；\n   - 红节点的两个孩子必须黑（不能有两个连续红）；\n   - 任意节点到其每个叶子的所有路径，黑节点数相同（黑高一致）。\n   推论：最长路径（红黑交替）不超过最短路径（全黑）的 2 倍，这就是「近似平衡」的含义，保证树高 O(log n)。\n2. 两种平衡操作：变色（叔叔是红的场景，父叔爷一起变色，问题向上冒）和旋转（左旋/右旋调整结构，叔叔是黑的场景）。插入最多两次旋转，删除最多三次——这是它比 AVL 强的硬指标。\n3. 为什么工程偏爱红黑树而非 AVL：AVL 严格平衡（左右子树高差 <= 1），查找略快；但插入删除时 AVL 可能要 O(log n) 次旋转连锁调整，红黑树最多两三次。工程场景「增删查混合」，红黑树综合成本更低。所以 TreeMap、HashMap 树化、Linux epoll 的 fd 集合、CFS 调度器全用红黑树。\n4. 和 B+ 树的辨析（承接 mysql 分类）：红黑树是内存二叉平衡树；B+ 树是多叉、矮胖、节点对齐磁盘页，为减少磁盘 IO 而生。一个管内存，一个管磁盘。\n5. 面试话术：笔试不会让你手写红黑树（插入删除的分支情况太繁琐），但性质、旋转思想、与 AVL/跳表/B+ 树的选型对比必须张口就来。",
      point: "考察对「近似平衡」取舍思想的理解，以及与 AVL、B+ 树的选型辨析，而非手写旋转。",
      approach: "先定义「近似平衡加变色旋转两种操作」，五条性质背熟并推出最长不超过最短两倍的推论。用「插入最多两次旋转」的硬指标对比 AVL，列举 TreeMap、epoll 等工程采用案例，主动辨析 B+ 树（内存对磁盘）。坑：试图现场推插入分支细节陷入泥潭，明确说笔试不考手写。",
      followups: [
        { q: "为什么「最长路径不超过最短路径两倍」就能保证 O(log n)？", a: "最短路径全黑至少为黑高 h，最长红黑交替至多 2h，树高不超过 2 倍黑高。黑高由性质五约束、n 个节点时 h 是 O(log n)，所以树高 O(log n)，查找代价保持对数级。" },
        { q: "HashMap 为什么链表长度到 8 才树化、到 6 才退化回链表？中间留个缓冲带是为什么？", a: "泊松分布下链表长 8 概率极低，说明哈希已失效，换树保 O(log n)。8 与 6 之间留缓冲带是为防抖：若阈值相同，频繁增删会在边界反复树化退化，来回转换成本高。" },
        { q: "epoll 为什么用红黑树管理 fd，而不用哈希表？", a: "fd 集合要高效增删查且支持有序遍历，红黑树都是稳定 O(log n) 且保序；哈希表虽均摊 O(1)，但无序、扩容有毛刺，内核场景更看重稳定有界的单次操作时间。" }
      ],
      memory: "红黑树 = 小区限高令：不要求每栋楼一样高（非严格平衡），但「最高不超过最矮两倍」（黑高一致 + 红不相邻），用刷漆（变色）和微调地基（旋转）维持，比 AVL 那种「毫米级找平」省事得多——城管（工程）都喜欢这种够用就好的规矩。",
      tags: ["红黑树", "AVL", "HashMap", "L2"]
    },
    {
      id: "algorithm-10",
      level: 2,
      q: "手写一个令牌桶限流器（单机版），并对比滑动窗口、漏桶的实现思路。GM 后台接口和登录服各自适合哪种？",
      a: "核心结论：令牌桶 = 按固定速率发牌，请求持牌通行、桶空拒绝，允许攒牌应对突发；手写关键是「惰性补充令牌」——用时间差计算，不开后台线程。\n1. 令牌桶手写要点：字段 capacity（桶容量）、rate（每秒发牌数）、tokens（当前牌数）、lastTime（上次补充时间）。tryAcquire() 里：先按「(now - lastTime) * rate / 1000」惰性补充令牌（封顶 capacity），再判断 tokens >= 1 则扣减放行，否则拒绝。无需定时器，线程安全用 synchronized 或 CAS 即可。\n2. 漏桶对比：桶以固定速率「漏水」（处理请求），流入超限则溢出拒绝。区别：漏桶强制平滑输出（突发被削平成匀速），令牌桶允许突发（攒的牌一次用光）。\n3. 滑动窗口对比：把固定窗口（每分钟 N 次）按时间片细分（如 6 个 10 秒格子），窗口随时间滑动，统计当前窗口内各格子之和。解决固定窗口的「临界突刺」问题（窗口边界瞬间两倍流量），精度换内存。\n4. 游戏场景选型：\n   - 登录服开服洪峰：令牌桶——允许玩家适度突发涌入（攒牌期空闲攒下的容量），又能把长期速率压在游戏服承载内，配合排队页体验更好；\n   - GM 后台危险操作接口：固定窗口或滑动窗口就够（内部系统，防手滑狂点）；\n   - 对下游第三方渠道回调（支付验签调用渠道接口）：漏桶——对方限流严格，必须匀速。\n5. 工程关联：Guava RateLimiter 就是令牌桶（SmoothBursty + SmoothWarmingUp）；Sentinel 支持匀速排队（漏桶思想）；分布式限流用 Redis + Lua（计数在 Redis，Lua 保证「读-判-写」原子）——为 L3 深挖题埋点。",
      point: "考察限流算法手写实现（惰性补充）与按业务场景选型的判断力。",
      approach: "先给令牌桶语义：攒牌、允许突发。手写突出「时间差惰性补充、不开后台线程」，再对比漏桶（强制匀速）与滑动窗口（解决临界突刺）。按登录服、GM 后台、第三方回调三个场景给选型并讲理由，关联 Guava、Sentinel 和 Redis 分布式方案。坑：用定时线程补令牌；三种算法区别说混。",
      followups: [
        { q: "令牌桶「惰性补充」相比定时线程补充有什么优势？", a: "省一个后台线程，无线程生命周期管理和泄漏问题；计算只在请求到达时发生，空闲零开销；天然适配单机无状态。定时线程还要处理补充精度与竞争，复杂度更高。" },
        { q: "RateLimiter 的预热模式（SmoothWarmingUp）解决什么问题？", a: "冷启动保护：系统刚启动时缓存为空、连接池未热，瞬间满速流量会打垮服务。预热模式让令牌发放速率从慢到快爬升到目标值，给下游一个热身窗口。" },
        { q: "滑动窗口格子分多少合适？精度和内存怎么权衡？", a: "格子越多越接近真实流量曲线、临界突刺越小，但内存和求和开销越大。经验值 6 到 10 格（1 分钟窗分 6 个 10 秒格），突刺上限约为配额的格数分之一，按业务容忍度定。" }
      ],
      memory: "令牌桶 = 游乐场发手环：工作人员按固定速度往盒子里放手环（rate），游客拿手环进场，没手环就等；闲时手环能攒着（允许突发）。漏桶 = 商场旋转门：不管外面多少人挤，门就按固定速度转（强制匀速）。滑动窗口 = 查考勤，最近 60 分钟打卡次数，每分钟翻一页。",
      tags: ["限流", "令牌桶", "手写", "游戏实战", "L2"]
    },
    {
      id: "algorithm-11",
      level: 2,
      q: "TopK 问题有哪几种解法？全服 100 万玩家算战力 Top100，流式更新场景你会怎么做？",
      a: "核心结论：TopK 三板斧——全量排序 O(n log n)、大小为 K 的小顶堆 O(n log K)、快速选择 O(n) 平均；流式/实时场景用小顶堆或分层缓存。\n1. 小顶堆（标准答案）：维护容量 K 的小顶堆，遍历数据，元素比堆顶大就替换堆顶并调整。堆顶是「当前 TopK 里的最后一名」，新元素只要赢过它就能进榜。O(n log K)，空间 O(K)。100 万玩家 Top100：log100 ≈ 7 次比较，远快于全量排序。\n2. 快速选择（QuickSelect）：快排的 partition 思想，pivot 归位后只递归包含第 K 大的那一半，平均 O(n)。适合一次性离线计算、可以改动原数组的场景（注意它不稳定且会打乱数组）。\n3. 游戏排行榜真实方案（超越八股的回答）：\n   - 实时榜：ZSet（跳表，score = 战力），玩家战力变化 O(log n) 原地更新，查 Top100 和自己排名都是 O(log n)，根本不用自己维护堆——「知道什么时候不用手写算法」是更高段位；\n   - 离线计算（如跨服赛前海选）：分片 + 堆——各区服本地算出 TopK，汇总节点合并 K 路有序序列（归并 + 堆），避免全量数据传输；\n   - 流式场景：小顶堆常驻内存，新数据流入即比较，随时可取当前 TopK。\n4. 同分处理：堆元素要带 tie-breaker（如达到时间戳、playerId），比较器先比分数再比时间——呼应排序稳定性问题。\n5. 变形追问储备：第 K 大（堆大小 K 即可）、中位数（两个堆：大顶堆存小半 + 小顶堆存大半，平衡两堆大小）、K 个有序链表合并（小顶堆按链表头组织）。",
      point: "考察按场景选择 TopK 解法的能力，以及「知道何时不用手写算法」的工程段位。",
      approach: "先列三板斧（全量排序、小顶堆、快速选择）及各自复杂度。小顶堆讲清「守门员」直觉，快选讲 partition 单侧递归。关键升维：游戏真实场景用 ZSet 或分片加归并，说明何时放弃手写，最后补同分 tie-breaker。坑：小顶堆大顶堆用反；忽略同分处理。",
      followups: [
        { q: "小顶堆求 TopK 大为什么用小顶堆而不是大顶堆？", a: "小顶堆堆顶是堆里最小元素即「第 K 名守门员」，新元素只需跟它比：赢了进堆淘汰守门员，输了直接跳过。大顶堆堆顶是最大值，无法判断新元素是否够格进 TopK。" },
        { q: "双堆求中位数怎么保持两堆平衡？", a: "大顶堆存较小一半、小顶堆存较大一半。新元素先入对应堆，再调整使两堆大小差不超过 1（多的一边弹顶给另一边）。中位数：总数奇取大堆顶，偶取两顶均值。插入 O(log n) 查询 O(1)。" },
        { q: "ZSet 实时榜和堆方案各自的适用边界是什么？", a: "ZSet：数据动态增删改、随时查任意排名和范围，O(log n) 原地更新，首选；堆：一次性离线计算、流式只进不出、无外部存储依赖，或要定制复杂比较规则。跨服合并用分片堆加 K 路归并。" }
      ],
      memory: "小顶堆 = 班级光荣榜（只贴 100 名）：榜尾是「守门员」，新同学分数只要赢过守门员就上榜、守门员下榜。快速选择 = 军训教官喊「比张三高的出列」把队伍分两半，只在一半里继续找，另一半不用管。双堆求中位数 = 天平两端各放一半人，随时知道分界线是谁。",
      tags: ["TopK", "堆", "排行榜", "游戏实战", "L2"]
    },
    {
      id: "algorithm-12",
      level: 2,
      q: "海量数据题套路：10 亿个玩家 ID（long 型）里找出重复的 ID，内存只有 1G，怎么做？bitmap 和哈希分治分别怎么玩？",
      a: "核心结论：海量数据题就三个套路——bitmap（存在性压缩）、哈希分治（化大为小）、外部排序/堆（全局有序），先算内存账再选方案。\n1. 先算账：10 亿个 long 原样存要 8G，放不进 1G——必须换表示法或切分。\n2. bitmap 方案（判重/判存在首选）：ID 范围有限时，一个 bit 表示一个 ID 是否出现。若 ID 范围 40 亿内，需要 40 亿 bit ≈ 500MB，放得下。遍历两遍：第一遍置位，第二遍发现已置位即重复。Java 用 BitSet 或 long[] 手算位运算（word = id >>> 6，bit = 1L << (id & 63)）。游戏落地：签到、DAU 去重、活动参与去重——Redis Bitmap 就是现成方案。\n3. 哈希分治方案（ID 范围太大或要精确统计次数时）：hash(id) % 1000 把数据切到 1000 个小文件，重复 ID 必然落在同一文件（哈希的确定性），逐个小文件加载进内存用 HashMap 统计。化大为小、分而治之，这是所有「装不下」问题的通用钥匙。\n4. 变种套路备查：\n   - 找出现次数 TopK：分治 + 各小文件内统计 + 小顶堆汇总；\n   - 两个大文件找交集：各自哈希分治到对应小文件对，再对内比较；\n   - 排序：外排序（分段内排 + 多路归并）。\n5. 精度可降级场景：只要「是否存在过」且允许误判，布隆过滤器比 bitmap 更省（应对 ID 范围稀疏巨大的情况）——但删除困难、有误判率，要能说清代价。\n6. 面试加分：主动追问面试官「ID 范围多大？要不要精确？要不要支持删除？」——海量题本质是约束选择题，问清约束比直接给方案更像老手。",
      point: "考察「先算账再选方案」的海量数据方法论，bitmap 与哈希分治两个核心武器。",
      approach: "先算内存账：10 亿 long 要 8G 放不下。讲 bitmap（位压缩、两遍扫描、Java 位运算细节），讲哈希分治（重复 ID 必同文件的确定性论证）。列变种套路（TopK、交集、外排序），结尾反客为主追问约束：范围多大、要不要精确、要不要删除。坑：不分析 ID 范围直接上 bitmap。",
      followups: [
        { q: "bitmap 一位表示一个 ID，ID 范围是 0~100 亿但总量只有 10 亿，还适合 bitmap 吗？", a: "不适合直接开：100 亿 bit 约 1.25G 超内存，且位图稀疏浪费。可换布隆过滤器（允许误判）、哈希分治，或分段 bitmap 分段加载处理。bitmap 的前提是值域压缩后放得下。" },
        { q: "哈希分治时如果某个小文件还是装不下内存（哈希倾斜）怎么办？", a: "二次分治：对超限文件换一个哈希函数再切一轮，直到单片可入内存。预防：分桶数按总数据量除以单片容量再乘 2 到 3 倍冗余预估；倾斜严重说明哈希函数分布差，可换 MurMurHash。" },
        { q: "布隆过滤器的误判率怎么算？为什么不能直接删除元素？", a: "误判率约 (1 - e 的 -kn/m 次) 的 k 次方，m 位数、n 元素、k 个哈希函数，m/n 取 10 时约 1%。不能删：一个 bit 可能被多个元素共享，清零会误删其他元素；要删用计数布隆，代价是数倍内存。" }
      ],
      memory: "海量三套路 = 图书馆搬家：bitmap = 不搬书，只在清单上打勾（一位一本）；哈希分治 = 按书号尾数分到 1000 个纸箱，同号必同箱（重复必相遇），一箱一箱处理；外排序 = 每箱先内部理好，再多箱合并。搬不动就不搬、就拆，别硬扛。",
      tags: ["海量数据", "bitmap", "分治", "游戏实战", "L2"]
    },
    {
      id: "algorithm-13",
      level: 2,
      q: "手写一个简化版线程池（核心线程 + 任务队列 + 拒绝策略），说说 ThreadPoolExecutor 的执行流程在你的简化版里怎么体现。",
      a: "核心结论：简化线程池三要素——Worker 线程组、阻塞任务队列、拒绝策略；核心流程「任务入队 → Worker 循环取任务执行」，讲清它和 ThreadPoolExecutor 四步流程的对应关系。\n1. 简化版骨架：\n   - 构造时启动 coreSize 个 Worker 线程；\n   - execute(task)：队列没满则入队，满了走拒绝策略（直接抛异常）；\n   - Worker 是 Runnable，run() 里 while 循环 BlockingQueue.take() 取任务执行（take 阻塞即线程空闲等待）；\n   - shutdown() 中断所有 Worker。\n2. 和 ThreadPoolExecutor 四步流程的对应（面试官真正想听的）：\n   - 提交任务：当前线程数 < 核心数 → 直接新建 Worker 执行；\n   - 核心数已满 → 入队；\n   - 队列已满且 < 最大线程数 → 新建非核心 Worker；\n   - 全满 → 拒绝策略。\n   简化版把「先扩线程再入队」简化成「只有固定核心 + 入队 + 拒绝」——能讲出这个差异说明真懂而不是背的。\n3. 手写细节：BlockingQueue 直接用 ArrayBlockingQueue（有界，防 OOM）；Worker 循环要捕获任务异常，否则一个任务抛异常线程就死了（线程数悄悄减少，这是真实 bug 高发点）；shutdown 要区分「立即停止」和「跑完队列再停」。\n4. 游戏服落地：登录服的异步任务池（写日志、通知）就是固定核心 + 有界队列 + 自定义拒绝（满了降级为丢弃 + 告警）；Disruptor 则是把这个模型做到极致（无锁队列 + 固定消费者组）。\n5. 追问储备：为什么阻塞队列用 take 而不是 poll + sleep（空转烧 CPU）；Worker 怎么复用（线程不退出、循环取任务，这就是「池」的本质）。",
      point: "考察对线程池「复用加有界加拒绝」本质的理解，及与 ThreadPoolExecutor 流程的对照能力。",
      approach: "先给三要素骨架：Worker 线程组、阻塞队列、拒绝策略，讲 execute 与 Worker 循环。关键加分：主动对照 JDK 四步流程说出简化差异。强调任务异常吞线程、队列必须有界两个实战坑，最后游戏服落地。坑：Worker 循环不捕异常；用无界队列；说不清线程复用原理。",
      followups: [
        { q: "Worker 线程数悄悄减少可能是什么原因？怎么防御？", a: "任务抛未捕获异常，run 方法退出，Worker 线程死亡且无人察觉。防御：Worker 循环内 try-catch 包住 task.run，记录日志继续循环；JDK 线程池可用 Future.get 暴露异常或 afterExecute 钩子。" },
        { q: "为什么任务队列必须有界？无界队列（LinkedBlockingQueue 默认）在生产上有什么后果？", a: "无界队列下生产快于消费时任务无限堆积直至 OOM，且拒绝策略永远触发不到、最大线程数形同虚设（队列不满就不扩线程）。有界队列加拒绝策略才能把压力显性化、可控降级。" },
        { q: "线程池的「线程复用」到底是怎么实现的？", a: "Worker 线程启动后不退出，run 里 while 循环从阻塞队列 take 任务，执行完一个取下一个；无任务时 take 阻塞挂起不耗 CPU。池的本质就是线程生命周期与任务解耦、反复借用。" }
      ],
      memory: "线程池 = 餐厅：Worker 是固定编制的厨师，任务队列是点菜单传送带（必须有界——单子堆到天花板就拒单，不能无限接），厨师循环「取单 → 做菜 → 再取单」（线程复用）。一道菜做糊了（任务异常）不能让厨师辞职（线程死掉），记个差评继续干。",
      tags: ["线程池", "手写", "并发", "L2"]
    },
    {
      id: "algorithm-14",
      level: 2,
      q: "二叉树的前中后序遍历（递归 + 迭代）和层序遍历怎么写？「中序遍历 BST 得到升序」在业务里有什么应用？",
      a: "核心结论：遍历四件套——递归版三行代码，迭代版用栈模拟递归栈，层序用队列（BFS）；手写题的真正考点是「递归转迭代」的思维。\n1. 递归版：前序「根左右」、中序「左根右」、后序「左右根」，每种三行——判空返回、按序访问。要能做到三十秒默写。\n2. 迭代版（重点）：\n   - 前序：栈压右再压左（栈是 LIFO，先出左）；\n   - 中序：一路向左压栈到底，弹出访问，转向右子树；\n   - 层序：队列 BFS，每层记录 size 可分层输出（打印工会组织树按层级展示）。\n3. 中序遍历 BST = 升序序列：这是 BST 的身份证性质。应用：TreeMap/TreeSet 的有序遍历（按分数升序取配置）、校验一棵树是不是 BST（中序结果是否严格递增）、找 BST 第 K 小（中序数到第 K 个）。\n4. 游戏/工程联系：\n   - 表达式树求值（后序遍历，先算子表达式）；\n   - 技能树的依赖检查（DFS 判环）；\n   - 序列化/反序列化二叉树（前序 + 空标记）；\n   - MongoDB/文档树的字段遍历也是树的 DFS。\n5. 高频变形储备：二叉树最大深度（DFS 一行：max(左, 右) + 1）；最近公共祖先（递归：左右子树各找到一个即当前节点是 LCA）；镜像翻转（递归交换左右孩子）。\n6. 迭代版记忆锚点：中序迭代模板「curr 不空就压栈往左走，空了弹出访问并右拐」——背这个口诀比背代码稳。",
      point: "考察递归转迭代的思维与手写熟练度，以及 BST 中序升序性质的活学活用。",
      approach: "递归版三行秒写展示熟练度。迭代版重点讲中序口诀「压栈向左、弹出访问、右拐」，层序讲队列加 size 分层技巧。用 TreeMap、BST 校验、第 K 小讲中序性质的业务应用，补 LCA、镜像等变形储备。坑：迭代版压栈顺序搞反；判 BST 只比相邻节点。",
      followups: [
        { q: "怎么判断一棵二叉树是不是 BST？只比较每个节点和左右孩子够吗？", a: "不够。左子树所有节点都必须小于根，仅比直接孩子会漏掉深层违规（根的左子树里藏着比根大的节点）。正解：中序遍历验证严格递增，或递归传 min、max 边界逐层收紧。" },
        { q: "Morris 遍历怎么做到 O(1) 空间？代价是什么？", a: "利用树中空闲右指针做线索：左子树最右节点的右指针暂指回当前节点，访问后恢复，替代栈记录回头路。代价：遍历中临时改树结构需恢复、代码难写难懂，工程少用，面试说清原理即可。" },
        { q: "后序遍历用栈迭代为什么是四件套里最难写的？", a: "根要左右都访问完才能处理，从右子树回来和从左子树下去到同一节点时行为不同，需区分第几次见到该节点。常用技巧：记录 lastVisit 判断右子树已访问，或用根右左遍历后逆序取巧等价。" }
      ],
      memory: "遍历 = 视察校区：前序是校长每到一处先讲话再逛（根先），中序是先逛左边再讲话（左根右），层序是一层一层查教室（BFS 队列）。迭代版 = 校长带着记事本（栈），走到哪记到哪，不会再忘了回头。中序 BST 升序 = 按学号排队的人从左到右点名，天然有序。",
      tags: ["二叉树", "遍历", "手写", "L2"]
    },
    {
      id: "algorithm-15",
      level: 2,
      q: "滑动窗口和双指针思想是什么？手写「无重复字符的最长子串」。游戏敏感词过滤为什么用 AC 自动机而不是逐个词 indexOf？",
      a: "核心结论：滑动窗口 = 双指针维护一个「动态区间」，右指针扩张、左指针收缩，把两层暴力 O(n²) 压到 O(n)；AC 自动机 = 多模式串一次扫描全部命中，是敏感词过滤的工业标准。\n1. 滑动窗口模板（无重复字符最长子串）：Set 存窗口内字符，right 右扩——字符不在 Set 就加入并更新最大长度；在 Set 里则 left 右收缩逐个移除直到冲突消除。每个字符进出窗口各一次，O(n)。模板口诀：「右扩记账，冲突左缩」。\n2. 双指针家族备查：对撞指针（有序数组两数之和、回文判断）、快慢指针（链表判环、去重）、滑动窗口（子串/子数组问题）——识别信号是「区间/成对/连续」字样。\n3. 敏感词过滤为什么不能用 indexOf 逐个词扫：词库 5 万个词，每条消息 100 字，逐个词 indexOf 是 5 万 × O(n) = 灾难；聊天频道每秒几千条消息直接打爆 CPU。\n4. AC 自动机原理：把全部敏感词建成一棵 Trie（前缀树，共享前缀省空间），再给每个节点建 fail 指针（失配时跳到「当前串的最长可匹配后缀」对应节点，类似 KMP 的 next 数组）。构建一次，之后每条消息只扫描一遍 O(n)，所有命中一次性出来，与词库规模无关。\n5. 游戏工程细节：变体字对抗（先归一化：繁简转换、去空格符号、谐音映射，再过 AC）；热词热更新（Trie 重建太贵，用双 buffer——新词库后台建树，AtomicReference 原子切换）；命中后的策略分级（替换星号/拦截/人工复审）。\n6. 关联储备：KMP（单模式匹配 O(n+m)，next 数组是 fail 指针的祖宗）；Trie 的其他用途（协议号路由、ID 前缀检索）。",
      point: "考察滑动窗口模板化手写能力，以及多模式串匹配（AC 自动机）的工程认知。",
      approach: "先给窗口口诀「右扩记账、冲突左缩」，默写无重复字符模板并证 O(n)。点出双指针家族的识别信号。算 indexOf 方案的账（5 万词乘 O(n)）引出 AC 自动机，讲 Trie 加 fail 指针原理，补游戏工程细节：归一化对抗变体、双 buffer 热更新。坑：左缩用 if 不用 while。",
      followups: [
        { q: "滑动窗口里左指针收缩时为什么要用 while 而不是 if？", a: "一个冲突字符可能需要移出窗口里多个元素才能消除，必须持续收缩直到冲突字符完全离开窗口，if 只移一次窗口内仍有重复。while 保证循环不变式：窗口内始终无重复。" },
        { q: "AC 自动机的 fail 指针和 KMP 的 next 数组是什么关系？", a: "同源思想：失配时利用已匹配信息避免回退主串。next 数组针对单模式串，fail 指针是它在 Trie 树上的推广——指向当前已匹配串最长后缀对应的节点，让多模式共享失配跳转。" },
        { q: "敏感词库热更新除了双 buffer 还有什么方案？各有什么取舍？", a: "增量构建：直接在 Trie 上加词，读多写少用读写锁，但 fail 指针重建麻烦；分段词库：热词小库高频重建加基础大库低频重建，查询时查两库。双 buffer 简单费内存，增量省内存但有锁竞争。" }
      ],
      memory: "滑动窗口 = 毛毛虫爬行：头往前探（右扩），身体里的食物重复了就尾巴往前收（左缩），整个身体（窗口）扫过一遍就算完。AC 自动机 = 机场安检门：5 万个违禁品特征全部预装进机器（Trie），人过一遍门（扫一遍）全部特征同时比对；indexOf 逐个词 = 保安拿着 5 万张照片挨个对人脸，队伍排到法国。",
      tags: ["滑动窗口", "双指针", "AC自动机", "敏感词", "游戏实战", "L2"]
    },
    {
      id: "algorithm-16",
      level: 3,
      q: "手写一致性哈希（含虚拟节点），并说明它在游戏分服路由、缓存节点扩容时是怎么做到「数据迁移最小化」的。",
      a: "核心结论：一致性哈希 = 把节点和数据都映射到同一个哈希环上，数据顺时针找第一个节点归属；增删节点只影响环上相邻区间，迁移量约 1/N，完胜取模方案的全量重排。\n1. 手写三件套（Java 版骨架）：\n   - TreeMap<Long, String> ring 存「哈希值 → 节点」（TreeMap 提供 ceilingEntry 找顺时针第一个，天然适配环）；\n   - addNode(node)：对每个虚拟节点（如 node + \"#VN\" + i，100~200 个）算哈希放入 ring；\n   - getNode(key)：key 算哈希，ceilingEntry 找到即返回；没找到（超出最大哈希）则取 ring.firstEntry()——这就是「环」的收尾相接。\n2. 虚拟节点解决什么：物理节点少（3~5 台）时哈希分布不均，有的节点环上占大半（数据倾斜）；每个物理节点虚拟化成上百个虚拟点打散在环上，分布趋于均匀，且扩容后迁移量平滑。虚拟节点数越多越均匀，代价是路由表内存。\n3. 迁移最小化的数学解释：环上加一个节点，只「接管」它逆时针方向相邻节点的一段区间，其他 N-1 个节点的数据纹丝不动——平均迁移 1/N；普通取模 hash % N 扩容到 N+1 时几乎所有 key 的归属都变（命中率雪崩）。\n4. 游戏落地：\n   - 分服/跨服路由：玩家 ID 一致性哈希到战斗节点，加机器只迁移环上一小段玩家；\n   - 缓存层：Redis 代理（twemproxy/codis 的核心思想）用它做节点路由，扩容缓存不引发全量穿透；\n   - 注意边界：游戏分服其实是「显式分片」（开服规划好的区服），一致性哈希更多用在「无状态节点层」——战斗服进程、缓存节点。\n5. 追问储备：哈希函数选择（MurMurHash/FNV，要均匀且快）；节点故障 = 删除节点，其数据顺时针倒给下一个节点（天然故障转移）；有状态服务用它要小心「归属漂移期间的脏写」，要配合路由版本号/优雅迁移。",
      point: "考察手写能力（TreeMap 环）与对迁移最小化的数学理解、虚拟节点的作用。",
      approach: "先给环形映射加顺时针归属的定义，手写三件套突出 TreeMap.ceilingEntry 和环收尾相接。用「加节点只影响相邻区间、迁移 1/N」对比取模的全量重排，讲虚拟节点解决数据倾斜。游戏落地时主动说边界：显式分片与无状态节点层之别。坑：忘记 ceilingEntry 为空时回环首。",
      followups: [
        { q: "TreeMap 为什么是一致性哈希的最佳载体？ceilingEntry 起什么作用？", a: "TreeMap 是有序 Map，哈希环本质是有序哈希值空间：ceilingEntry 一步找到顺时针第一个不小于该哈希的节点，O(log n) 完成定位；找不到时取 firstEntry 实现环的收尾相接，其他结构要自己维护有序性。" },
        { q: "虚拟节点数量怎么定？100 个和 1000 个各有什么影响？", a: "数量越多分布越均匀、扩容迁移越平滑，但路由表内存和 addNode 成本线性增长。经验值每物理节点 100 到 200 个虚拟点已足够均匀，1000 以上收益递减，属于内存换均匀度。" },
        { q: "一致性哈希和 Redis Cluster 的 slot 分片（16384 槽）思想有什么异同？", a: "同：都把 key 空间预切分为固定单元路由，增删节点只迁移部分单元。异：slot 把环上连续区间离散成 16384 个槽，路由表是槽到节点的显式映射，迁移以槽为单位、元数据可管理；一致性哈希是隐式区间、节点即边界。slot 更利于集群治理。" }
      ],
      memory: "一致性哈希 = 圆桌分蛋糕：节点是围桌坐的人，每块蛋糕（key）顺时针分给遇到的第一个人。加一个人坐下，只从左手边邻居那分走一小块（迁移 1/N）；取模分蛋糕 = 每加一个人全部蛋糕重新切（全量迁移）。虚拟节点 = 每个大胃王在桌上占 100 个座位（打散均匀）。",
      tags: ["一致性哈希", "手写", "L3", "游戏实战"]
    },
    {
      id: "algorithm-17",
      level: 3,
      q: "深挖限流：单机令牌桶你已经会了，现在要求「全服共享的限量资格」（如全服限购 1000 份礼包、每个 IP 每分钟限 10 次登录），分布式限流怎么实现？Redis + Lua 脚本写一个。",
      a: "核心结论：分布式限流 = 把计数状态放共享存储（Redis），用 Lua 把「读取-判断-更新」三步原子化，单机算法思想不变，原子性保障升级。\n1. 为什么不能各节点本地限流：10 个登录服节点各自令牌桶，总限流目标 100 次/分钟，每个节点配 10 次——但流量不均（某节点被打了 50 次其他空闲）就超限或不达标；全服限购更是必须全局唯一计数。\n2. 滑动窗口的 Redis + Lua 实现（最经典）：key = 限流标识，ZSet 存请求记录（score = 时间戳，member = 唯一请求 ID）。Lua 脚本原子执行四步：\n   - ZREMRANGEBYSCORE 移除窗口外旧记录；\n   - ZCARD 统计当前窗口请求数；\n   - 小于阈值则 ZADD 当前请求并返回 1（放行），否则返回 0（拒绝）；\n   - PEXPIRE 设置 key 过期兜底清理。\n   整个脚本在 Redis 单线程内原子执行，并发安全无锁。\n3. 令牌桶的 Redis 版：Hash 存 tokens 和 lastTime，Lua 里做惰性补充 + 扣减，逻辑和单机版一致，只是状态搬家。\n4. 游戏落地：\n   - 全服限购：其实更简单——DECR 原子扣减库存（返回值 < 0 说明抢完，INCR 回补），Lua 里「查资格 + 扣库存」原子防超卖，这是我们秒杀/限量礼包的标准方案；\n   - 登录防爆破：IP 维度滑动窗口限流；\n   - 降级预案：Redis 挂了限流怎么办？fail-open（放行，防误杀正常玩家）还是 fail-close（拒绝，防刷）要按业务定——支付类 fail-close，登录类 fail-open + 本地兜底限流。\n5. 追问储备：集群下 key 热点（限购 key 全打在一个 slot，要拆桶：stock:1~stock:16 分桶扣减）；时钟一致性（用 Redis 的 TIME 命令而不是客户端时间）；Guava RateLimiter 和这套方案的组合（本地削峰 + 全局兜底两层）。",
      point: "考察把单机限流升级为分布式的核心洞察：共享状态加 Lua 原子化，以及降级预案。",
      approach: "先讲本地限流为何失效：流量不均、全局计数必须唯一。给 ZSet 滑动窗口的 Lua 四步流程，提令牌桶 Redis 版只是状态搬家。游戏落地：限购 DECR 防超卖、登录 IP 限流。必答 fail-open 与 fail-close 决策框架，埋热 key 拆桶、时钟源等深挖点。坑：Lua 里用客户端时间；无 Redis 宕机预案。",
      followups: [
        { q: "ZSet 滑动窗口里 member 为什么要带唯一 ID 而不是纯时间戳？", a: "同一毫秒可能有多个请求，纯时间戳做 member 会被 ZSet 判重覆盖，统计数偏少、放行超量。member 用时间戳加 UUID 或自增序列，保证每个请求唯一落座，计数才准确。" },
        { q: "限流 Redis 宕机，fail-open 和 fail-close 怎么决策？", a: "按误伤用户与被刷损失谁更严重：支付、限购类 fail-close，宁可拒绝也要防超卖防刷；登录、聊天类 fail-open 先放行加本地兜底限流，Redis 故障不能演变为全服不可用。故障后果定策略。" },
        { q: "全服限购的库存 key 是超级热 key，怎么拆？拆完怎么保证不超卖？", a: "拆桶：stock 拆成 stock:1 到 stock:16，请求哈希到各桶分别 DECR，总库存预先分配。不超卖靠每桶 Lua 内查加扣原子执行，扣到小于 0 立即回补并拒绝；桶间库存不均可用原子迁移回收或接受小误差。" }
      ],
      memory: "分布式限流 = 全城限购房：各售楼处（节点）不能各自记账（本地限流失效），必须连住建局中央系统（Redis），且「查资格 + 盖章」一个窗口一次办完（Lua 原子），不能查完回家想想再来（两步分开就被黄牛插队）。Redis 挂了按业务定生死：买房 fail-close 宁可错杀，进公园 fail-open 先放行。",
      tags: ["分布式限流", "Lua", "手写", "L3", "游戏实战"]
    },
    {
      id: "algorithm-18",
      level: 3,
      q: "现场编程综合题：设计一个「支持过期时间 + 容量淘汰」的本地缓存（TTL + LRU 双策略），说说数据结构设计和 get 的完整逻辑，并预估面试官会怎么追问。",
      a: "核心结论：双策略缓存 = LRU 双向链表管访问顺序 + 惰性过期检查管 TTL，get 时「先判过期再维护 LRU 顺序」，两个策略在 get/put 路径上各插入一个检查点。\n1. 数据结构设计：Node { key, value, expireAt, prev, next }，HashMap 定位 + 双向链表维护 LRU 顺序（同 LRU 题骨架）+ expireAt 字段承载 TTL。\n2. get 完整逻辑（顺序敏感，是这题的核心考点）：\n   - Map 查不到 → 返回 null；\n   - 查到但 expireAt <= now → 惰性过期：从 Map 和链表删除，返回 null（等价于不存在）；\n   - 未过期 → 移到链表头部（LRU 维护），返回 value。\n   注意顺序：过期判断必须在 LRU 维护之前，否则把死数据顶到头部、还把热数据挤掉。\n3. put 逻辑：已存在则更新 value + expireAt + 移头；不存在则新建插头；超容量先「顺手清理头部开始的过期节点」，再淘汰尾节点——过期数据优先于 LRU 淘汰。\n4. 追问清单（自己先供出来，面试反客为主）：\n   - 纯惰性过期的缺陷：冷数据永不被 get 就一直占内存 → 加定期清理（后台线程抽样扫描，Redis 的定期删除就是这个思路，或 TimerWheel）；\n   - 并发安全：整体 synchronized 最简单，生产换分段锁或直接用 Caffeine（它的 Window-TinyLFU 比 LRU 抗扫描污染）；\n   - 时钟源：System.currentTimeMillis 有时钟回拨风险，游戏服内可用逻辑时钟/单调时间；\n   - 穿透场景：get 未命中要不要缓存空值（防穿透）；\n   - 为什么要自己造：Caffeine/Guava 已有，自研只在「要定制淘汰策略（如按业务优先级）或极致性能」时合理。\n5. 收尾升维：游戏服里这类本地缓存的真实场景是「会话/配置热数据」，正确姿势是单玩家逻辑线程内私有缓存（无锁）+ Caffeine 兜底，而不是全局大缓存——把「数据结构题」答成「架构权衡题」就是 L3 的答法。",
      point: "考察双策略组合设计能力：get 路径上过期判断与 LRU 维护的顺序，及对缺陷的预判。",
      approach: "先给结构：LRU 骨架加 expireAt 字段。get 逻辑强调「先判过期再维护 LRU」这一顺序考点，put 先清过期再淘汰尾部。主动供出追问清单（定期清理、并发、时钟、穿透、自研合理性）反客为主，收尾升维到架构权衡。坑：顺序写反；忽略惰性过期冷数据占内存。",
      followups: [
        { q: "惰性过期 + 定期抽样的组合里，抽样策略怎么设计才高效？", a: "借鉴 Redis：每轮随机抽 N 个 key 检查过期，过期比例超过阈值（如 25%）就再抽一轮，否则停。用少量 CPU 换内存回收，避免全量扫描，频率按内存压力自适应调节。" },
        { q: "LRU 在「批量扫描型访问」下为什么会失效（缓存污染）？LFU/TinyLFU 怎么解决？", a: "一次性全表扫描把冷数据刷成最近使用，顶掉真正的热数据。TinyLFU 用 Count-Min Sketch 统计频率，新数据先和淘汰候选比频率，频率不够直接拒收，偶发扫描进不了缓存主区。" },
        { q: "如果过期事件要触发回调（如会话过期要通知玩家下线），数据结构要加什么？", a: "加一个按 expireAt 组织的最小堆或时间轮（如 Netty 的 HashedWheelTimer），后台线程到点弹出触发回调。注意与惰性过期同步：惰性删除时要把堆里的对应事件作废（惰性删堆项或事件带版本号）。" }
      ],
      memory: "TTL + LRU 缓存 = 便利店货架：LRU 是理货员把常被拿的商品摆最顺手位（链表头），卖不动的扔仓库（尾部淘汰）；TTL 是每晚检查保质期（get 时先看 expireAt），过期食品哪怕再畅销也立即下架（先判过期再管 LRU）。惰性过期 = 顾客拿到手才发现过期才扔，所以还要定期巡店（后台抽样清理）。",
      tags: ["L3", "综合设计", "LRU", "TTL", "游戏实战"]
    }
  ]
};
