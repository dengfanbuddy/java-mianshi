window.QB = window.QB || {};
window.QB["java-concurrent"] = {
  id: "java-concurrent",
  name: "Java 并发编程",
  icon: "🧵",
  desc: "JUC 与并发是游戏服开发的主战场：10 年 Netty/Disruptor/多线程经验的老手，重点复习锁升级、JMM、AQS 原理、线程池调优，并能结合游戏服线程模型与玩家数据并发写入讲清设计取舍。",
  questions: [
    {
      id: "java-concurrent-01",
      level: 1,
      q: "Java 线程有哪几种状态？状态之间如何转换？排查线上问题时这些状态怎么看？",
      a: "核心结论：Thread.State 定义了 6 种状态——NEW、RUNNABLE、BLOCKED、WAITING、TIMED_WAITING、TERMINATED。\n1. NEW：new Thread() 后未 start()。\n2. RUNNABLE：调了 start()，包括就绪（等 CPU）和运行中，JVM 不区分。\n3. BLOCKED：等 synchronized  Monitor 锁，抢不到就停在这。\n4. WAITING：无限期等待，由 wait()/join()/LockSupport.park() 进入，需要别人 notify/unpark 唤醒。\n5. TIMED_WAITING：带超时的等待，sleep(ms)/wait(ms)/join(ms)/parkNanos()。\n6. TERMINATED：run() 执行完毕。\n转换关键点：BLOCKED 只与 synchronized 抢锁有关；ReentrantLock 抢不到锁进的是 WAITING（park）。\n实战排查：jstack 抓线程快照，大量 BLOCKED 说明有锁竞争热点（比如游戏服里全局锁住了玩家数据表）；大量 TIMED_WAITING 在队列 take/poll 上是正常的消费者等待。",
      point: "考察对线程六状态的准确理解，以及用 jstack 定位线上问题的实战能力",
      approach: "先背出六状态，强调 BLOCKED 只与 synchronized 相关这一易错点 → 按状态逐个说触发方式与转换条件 → 落到实战：jstack 快照里什么状态分布说明什么问题。别只背定义不结合排查场景，也别把 ReentrantLock 排队说成 BLOCKED。",
      followups: [{q:"BLOCKED 和 WAITING 的本质区别是什么？分别由什么触发？", a:"BLOCKED 是抢 synchronized Monitor 失败在锁池排队，锁释放即可参与竞争；WAITING 是主动放弃执行权（wait/park/join），必须靠 notify/unpark 等外部信号唤醒。一个等锁，一个等信号。"}, {q:"你们线上线程 CPU 飙高怎么排查？", a:"top 找进程 → top -H -p pid 找高 CPU 线程 → printf %x 把 tid 转十六进制 → jstack pid 按 nid 定位线程栈，看是死循环、频繁 GC 还是锁自旋，针对性处理。"}, {q:"RUNNABLE 状态的线程一定在消耗 CPU 吗？", a:"不一定。JVM 把就绪和运行合并为 RUNNABLE，且线程阻塞在内核态 IO（如 socket read）时 Java 层仍显示 RUNNABLE，实际不耗 CPU，要结合系统态线程信息判断。"}],
      memory: "口诀：「新就阻等超时终」——NEW、RUNNABLE、BLOCKED、WAITING、TIMED_WAITING、TERMINATED；记住「BLOCKED 只认 synchronized，Lock 排队是 WAITING」。",
      tags: ["线程状态", "jstack", "线上排查"]
    },
    {
      id: "java-concurrent-02",
      level: 1,
      q: "创建线程有哪几种方式？为什么游戏服务器里严禁直接 new Thread()？",
      a: "核心结论：创建方式本质是「定义任务 + 交给执行体」，生产环境必须用线程池统一管理。\n创建方式：\n1. 继承 Thread 重写 run()。\n2. 实现 Runnable，传给 Thread（推荐，任务与执行解耦）。\n3. 实现 Callable + FutureTask，能拿返回值和异常。\n4. 线程池 submit/execute——生产唯一正确姿势。\n游戏服为什么禁止 new Thread()：\n1. 资源不可控：玩家一个操作 new 一个线程，突发流量直接把机器打爆（线程栈默认 1MB，几千个线程就是几个 G 内存）。\n2. 无法复用：线程创建销毁开销大，游戏服消息是高频小任务。\n3. 无法监控/优雅停机：池子里的线程可以统一命名（如 game-logic-1）、统一 shutdown 等业务落盘完再退出；裸线程谁也管不住。\n4. 破坏线程模型：游戏服通常要求「同一玩家的消息在同一线程串行执行」，乱起线程会直接破坏这个并发约束，造成数据错乱。",
      point: "考察是否理解线程必须由池统一管理，以及游戏服线程模型的硬约束",
      approach: "先给结论「定义任务 + 交给执行体，生产只用线程池」→ 四种创建方式一句话带过 → 重点展开游戏服禁 new Thread 的四条工程理由（资源、复用、监控、线程模型）。重心在工程取舍而非罗列 API，主动落到自己项目的线程划分。",
      followups: [{q:"Callable 和 Runnable 的区别？异常怎么拿到？", a:"Runnable 无返回值、异常只能内部消化；Callable 的 call() 有返回值且可抛异常，通过 Future.get() 取结果，异常被包装成 ExecutionException 在 get 时抛出。"}, {q:"你们游戏服的业务线程池是怎么划分的？", a:"Netty IO 线程只做收发编解码；业务逻辑经 Disruptor 投递到按玩家哈希绑定的消费者线程，单玩家串行；DB 落盘、日志走独立异步线程池，各池独立命名、独立监控。"}],
      memory: "类比：new Thread 像每次来客人现招一个临时工，线程池是养一支固定编制的前台团队——游戏服要的是「编制 + 排班」，不是打零工。",
      tags: ["线程创建", "线程池", "游戏服线程模型"]
    },
    {
      id: "java-concurrent-03",
      level: 1,
      q: "synchronized 有哪几种用法？分别锁的是什么对象？",
      a: "核心结论：synchronized 锁的是「对象头里的 Monitor」，锁谁取决于写法。\n1. 修饰实例方法：锁 this（当前实例）。\n2. 修饰静态方法：锁 类.class（Class 对象，全局唯一）。\n3. 修饰代码块 synchronized(obj)：锁括号里指定的对象，粒度最细，最常用。\n关键理解：\n1. 锁的是对象不是代码——两个线程用同一个 obj 才互斥，各 new 一个锁对象等于没锁。\n2. 可重入：同一线程可重复获取同一把锁（游戏服里玩家消息处理里调多个加锁方法不会自己锁死自己）。\n3. 锁字符串常量要格外小心：synchronized(playerId) 因为字符串驻留池，可能你以为锁的是单个玩家，实际不同玩家同一字符串互相阻塞——应锁一个与玩家绑定的专用锁对象或直接用线程模型规避。",
      point: "考察对「锁的是对象而非代码」的本质理解及锁对象选择的坑",
      approach: "先用口诀给出三种用法各自锁的对象 → 强调「锁的是对象不是代码」这一核心 → 主动讲可重入和 String 锁对象的坑展示深度。别把三种用法背完就停，面试官在等你讲锁对象选错的真实案例。",
      followups: [{q:"synchronized 锁方法和锁代码块哪个好？", a:"代码块更好：锁粒度细，只包住真正的临界区，持锁时间短、竞争少；锁方法会把整个方法体纳入临界区，无谓拉低并发度。"}, {q:"锁对象可以是 null 吗？", a:"不行。进入 synchronized 时会检查锁对象，为 null 直接抛 NullPointerException。"}, {q:"为什么不要用 String/Integer 当锁对象？", a:"String 有驻留池、Integer 有 -128~127 缓存，不同业务可能引用到同一对象，导致看似无关的代码互相抢锁。应 new 一个专用锁对象，职责清晰。"}],
      memory: "口诀：「实例锁 this，静态锁 class，代码块锁括号」；再加一句「锁的是对象，不是代码」。",
      tags: ["synchronized", "Monitor", "锁对象"]
    },
    {
      id: "java-concurrent-04",
      level: 1,
      q: "volatile 有什么作用？能保证原子性吗？举个你们游戏项目里用 volatile 的例子。",
      a: "核心结论：volatile 保证「可见性 + 有序性（禁止指令重排序）」，但不保证原子性。\n1. 可见性：写 volatile 变量会立刻刷回主存，读会强制从主存读，一个线程改了其他线程立即可见。\n2. 有序性：通过内存屏障禁止 volatile 读写与周围指令重排序（DCL 单例里防止拿到半初始化对象）。\n3. 不保证原子性：i++ 是读-改-写三步，两个线程同时自增会丢更新，count++ 用 volatile 是经典错误。\n游戏项目典型用法：\n1. 停服/维护标志位：private volatile boolean shutdown；GM 后台下发停服指令，Netty IO 线程和业务线程立刻看到，开始拒绝新请求、走关服流程。\n2. 配置热更新版本号：导表工具刷新配置后更新 volatile long configVersion，业务线程读取时比对版本决定是否重载。\n3. DCL 单例：private static volatile Instance instance，防止重排序导致拿到未构造完的对象。",
      point: "考察 volatile 两大语义的边界（不保证原子性）及真实项目应用",
      approach: "六字结论先行「可见有序不原子」→ 分别讲可见性和有序性（内存屏障）→ 用 i++ 反例坐实不保证原子性 → 举项目例子（停服标志、配置版本、DCL）。坑：别说 volatile 能保证线程安全，要主动给反例划清边界。",
      followups: [{q:"DCL 单例为什么必须加 volatile？", a:"对象创建分分配内存、初始化、引用赋值三步，后两步可能重排；不加 volatile，其他线程可能看到引用非空但字段未初始化的半成品对象。volatile 禁止该重排。"}, {q:"volatile 和 synchronized 的区别？什么时候用 volatile 就够了？", a:"volatile 只有可见性/有序性、无互斥；单变量读写、状态标志位场景够用且轻量。复合操作（i++、check-then-act）必须 synchronized 或 CAS 保证原子性。"}, {q:"volatile 写为什么比读贵？", a:"写后插入 StoreLoad 屏障，要求写缓冲刷回主存并使其他核对应缓存行失效，跨核通信成本高；读只需普通加载加屏障约束，便宜得多。"}],
      memory: "口诀：「可见有序不原子」——六字记住 volatile 全部特性；场景记「停服开关 + 双检锁」两个经典。",
      tags: ["volatile", "可见性", "DCL", "停服标志"]
    },
    {
      id: "java-concurrent-05",
      level: 1,
      q: "sleep() 和 wait() 有什么区别？interrupted 异常该怎么处理？",
      a: "核心结论：sleep 是 Thread 的静态方法不释放锁，wait 是 Object 的方法必须先持有锁且调用后释放锁。\n1. 所属类：sleep 在 Thread，wait 在 Object。\n2. 锁行为：sleep 抱着锁睡觉；wait 释放 Monitor，进等待队列，需要 notify/notifyAll 或超时唤醒，且唤醒后要重新抢锁才能继续。\n3. 使用位置：sleep 任何地方可用；wait 必须在 synchronized 块内（否则 IllegalMonitorStateException）。\n4. 醒来后状态：sleep 结束直接进 RUNNABLE；wait 被唤醒先抢锁。\n5. wait 必须放 while 循环里判断条件，防虚假唤醒。\ninterrupt 处理规范：\n1. InterruptedException 被抛出时中断标志已被清除，catch 里要么向上抛，要么 Thread.currentThread().interrupt() 恢复标志，绝不能吞掉。\n2. 游戏服关服时用 interrupt + 标志位优雅停掉业务线程的阻塞循环，让它们把未落盘数据写完再退出。",
      point: "考察 sleep/wait 锁行为差异及 InterruptedException 处理规范",
      approach: "先给核心差异「sleep 抱锁、wait 放锁」→ 从所属类、锁行为、使用位置、唤醒路径四个维度对比 → 补 while 判条件防虚假唤醒 → interrupt 处理两条铁律收尾。别漏「wait 必须在 synchronized 内」和「catch 后不许吞异常」两个得分点。",
      followups: [{q:"为什么 wait 必须在循环里调用？", a:"一防虚假唤醒（JVM 允许无 notify 时自发醒来），二防 notifyAll 后条件被先醒线程破坏；醒后必须 while 复查条件，用 if 会带着失效条件继续往下跑。"}, {q:"notify 和 notifyAll 怎么选？", a:"只有一个等待条件且叫醒任意一个都行时用 notify，效率高；多条件或不确定时用 notifyAll，防止信号丢失导致部分线程永远睡着。"}, {q:"join() 底层是什么实现的？", a:"就是 synchronized + wait 循环：while (isAlive()) wait(0)；线程退出时 JVM 内部自动 notifyAll，唤醒所有在 join 上等待的线程。"}],
      memory: "口诀：「sleep 抱锁睡，wait 放锁等」；interrupt 记「捕获后要么抛、要么补 interrupt 标志，不许吞」。",
      tags: ["sleep", "wait", "interrupt", "优雅停机"]
    },
    {
      id: "java-concurrent-06",
      level: 1,
      q: "什么是 CAS？它相比加锁有什么优缺点？AtomicInteger 是怎么实现的？",
      a: "核心结论：CAS（Compare-And-Swap）是一条 CPU 原子指令：内存值 == 期望值才更新为新值，否则失败重试，属于无锁乐观并发。\n实现原理：AtomicInteger 内部用 Unsafe.compareAndSwapInt(this, valueOffset, expect, update)，JDK9+ 底层是 VarHandle；value 用 volatile 修饰保证可见性，自旋重试直到成功。\n优点：\n1. 无锁：没有线程阻塞/唤醒、上下文切换开销，低竞争下远快于 synchronized。\n2. 不会死锁。\n缺点：\n1. ABA 问题（值被改走又改回来察觉不到）。\n2. 高竞争下自旋空转烧 CPU——这也是 LongAdder 分段思想出现的原因。\n3. 只能保证单个变量的原子性，多变量要靠 AtomicReference 包对象或加锁。\n游戏服用法：在线人数计数、全局唯一 ID 发号（AtomicLong）、道具日志计数等「单变量、低冲突」场景；玩家金币扣减不能裸用 CAS——要结合版本号或放到玩家绑定线程里串行处理。",
      point: "考察 CAS 原理、三大缺点及适用边界的完整认识",
      approach: "先一句话定义 CAS 并定位「无锁乐观并发」→ 讲 AtomicInteger 实现（Unsafe/VarHandle + volatile + 自旋）→ 优缺点对半讲，缺点（ABA、自旋烧 CPU、单变量）要主动给 → 落到游戏服适用与禁忌场景。讲缺点体现你知道边界，比分高。",
      followups: [{q:"CAS 自旋太久怎么优化？", a:"核心是降竞争：LongAdder 把热点拆到多个 Cell 分段累加；或限流削峰；竞争太激烈时退回加锁反而更省 CPU。按竞争强度选方案。"}, {q:"Unsafe 是什么？为什么业务代码不直接用？", a:"JDK 内部类，提供 CAS、直接内存操作、park 等底层能力，绕过 Java 安全检查；业务禁用因易破坏内存安全且非公开 API，JDK9+ 以 VarHandle 作为替代方案。"}, {q:"ABA 问题举个现实例子，怎么解决？", a:"无锁栈中节点 A 被弹出又压回，CAS 判断头还是 A 但实际链已变，导致节点丢失。解法：AtomicStampedReference 把值 + 版本号打包整体比较，每次修改版本递增。"}],
      memory: "类比：CAS 像「乐观改稿」——先记住看到的版本，提交时发现没变才覆盖，变了就重新看一遍再改；加锁则是「先抢钥匙再进门」。",
      tags: ["CAS", "AtomicInteger", "无锁", "乐观锁"]
    },
    {
      id: "java-concurrent-07",
      level: 1,
      q: "线程池的核心参数有哪些？提交一个任务后的执行流程是什么？",
      a: "核心结论：七大参数 + 一条「先核心、再队列、后最大、最后拒绝」的提交流程。\n七大参数：\n1. corePoolSize：核心线程数，常驻（默认 allowCoreThreadTimeOut=false）。\n2. maximumPoolSize：最大线程数 = 核心 + 救急线程。\n3. keepAliveTime + unit：救急线程空闲多久回收。\n4. workQueue：任务队列（ArrayBlockingQueue/LinkedBlockingQueue/SynchronousQueue 等）。\n5. threadFactory：自定义线程名、守护属性——游戏服必配，排查问题靠名字认线程。\n6. handler：拒绝策略。\n提交流程（execute）：\n1. 线程数 < corePoolSize → 直接创建核心线程执行。\n2. 否则 → 尝试入 workQueue 排队。\n3. 队列满且线程数 < maxPoolSize → 创建救急线程执行。\n4. 达到 max 且队列满 → 走拒绝策略。\n易错点：核心线程不是预热创建的，是任务来了才逐个创建；救急线程只在队列满之后才出现。Executors 默认工厂方法（newFixedThreadPool 无界队列、newCachedThreadPool 无线程上限）都有 OOM 风险，阿里规范禁止，必须 new ThreadPoolExecutor 显式传参。",
      point: "考察线程池七参数与提交流程的准确记忆及生产配置意识",
      approach: "先给「核心 → 队列 → 最大 → 拒绝」流程主线 → 七参数逐个一句话 → 点破两个易错点（核心线程不预热、救急线程在队列满后才出现）→ 收尾批 Executors 默认工厂的 OOM 风险。提交流程必须按顺序讲清，这是硬考点。",
      followups: [{q:"为什么队列满了才创建救急线程？这个设计有什么问题？", a:"设计意图是优先排队削峰、避免无控扩线程；副作用是突发流量先排队导致响应慢，救急线程来得太晚。所以队列类型和容量必须按流量模型认真选。"}, {q:"游戏服的 DB 异步落盘线程池参数你会怎么配？", a:"线程数结合 DB 连接池上限取 16~32，队列必须有界防 OOM，拒绝策略自定义：失败任务落本地文件/Kafka + 告警，玩家数据绝不能静默丢弃。"}, {q:"shutdown 和 shutdownNow 的区别？", a:"shutdown 停收新任务、等队列和运行中任务执行完，优雅；shutdownNow 尝试 interrupt 运行中线程并返回队列中未执行任务列表，用于快速停机。"}],
      memory: "口诀：「核心 → 队列 → 最大 → 拒绝」，像公司接活：正式工先上 → 活堆在前台 → 招临时工 → 实在接不了按规矩拒单。",
      tags: ["线程池参数", "execute流程", "ThreadPoolExecutor"]
    },
    {
      id: "java-concurrent-08",
      level: 1,
      q: "线程池有哪几种拒绝策略？游戏服务器场景下你会怎么选？",
      a: "核心结论：JDK 内置 4 种，生产环境通常要自定义。\n1. AbortPolicy（默认）：直接抛 RejectedExecutionException。\n2. CallerRunsPolicy：让提交任务的线程自己跑——天然反压，减慢上游提交速度。\n3. DiscardPolicy：静默丢弃，丢了都不知道。\n4. DiscardOldestPolicy：丢队列里最老的任务，塞新的。\n游戏服选型：\n1. 玩家操作类任务：不能丢！用 CallerRunsPolicy 反压 + 监控告警，或者自定义策略：先尝试短暂 offer 等待，再降级记录 + 告警。\n2. 日志/埋点类任务：可丢，用 DiscardPolicy 但配计数器监控丢弃率，丢了不影响业务。\n3. 数据落盘任务：绝不能丢，拒绝时必须落本地文件或 Kafka 重试，否则玩家数据丢失是线上事故。\n4. 自定义示例：实现 RejectedExecutionHandler，把拒绝的任务写进 Kafka 死信队列 + 钉钉告警，运维介入扩容。",
      point: "考察四种拒绝策略语义及按业务可丢弃性选型的工程判断",
      approach: "先列四种内置策略各一句话 → 按「能不能丢」给游戏服三类任务选型（玩家操作反压、日志可丢、落盘绝不丢）→ 引出自定义策略示例（死信队列 + 告警）。亮点在 CallerRuns 的反压机制和自定义策略，主动展示。",
      followups: [{q:"CallerRunsPolicy 有什么副作用？", a:"若提交方是 Netty IO 线程，被拒后 IO 线程亲自跑业务会阻塞网络读写，拖累该线程上所有连接。只在提交方允许被拖慢的链路用，并配合拒绝率监控。"}, {q:"如果队列用无界队列，拒绝策略还有意义吗？", a:"没有意义——队列永远不满，救急线程和拒绝策略永远不会触发，任务无限堆积最终 OOM。这正是阿里规范禁止无界队列的原因。"}, {q:"怎么监控线程池健康状况？", a:"定时采集 activeCount、poolSize、queueSize、completedTaskCount、rejectCount 上报 Prometheus/BI，配队列水位和拒绝率告警，先于玩家反馈发现瓶颈。"}],
      memory: "口诀：「抛、回、丢、丢老」——Abort 抛异常、CallerRuns 回提交者、Discard 丢新的、DiscardOldest 丢老的；游戏服铁律「玩家操作不能丢，日志可以丢」。",
      tags: ["拒绝策略", "反压", "线程池调优"]
    },
    {
      id: "java-concurrent-09",
      level: 1,
      q: "常见的 BlockingQueue 有哪些？游戏服里该选哪种？",
      a: "核心结论：按「有界/无界/不存元素/按优先级/延迟」区分 5 种常用实现。\n1. ArrayBlockingQueue：数组有界，一把锁，公平锁可选。\n2. LinkedBlockingQueue：链表默认 Integer.MAX_VALUE（近似无界），存取两把锁吞吐量略高。\n3. SynchronousQueue：不存元素，put 必须等 take（CachedThreadPool 用它）。\n4. PriorityBlockingQueue：按优先级出队，无界。\n5. DelayQueue：到期才能取（定时任务 ScheduledThreadPool 用它）。\n6. LinkedTransferQueue：transfer() 等消费者接手才返回。\n游戏服选型思路：\n1. 业务线程池队列：必须有界（如 ArrayBlockingQueue 几千容量）+ 合理拒绝策略，防止消息洪峰 OOM。\n2. 日志异步写：有界 LinkedBlockingQueue，满了降级丢弃 + 计数。\n3. 但真正的游戏服核心消息队列往往不用 BlockingQueue，而是 Disruptor 的 RingBuffer——数组预分配无 GC、无锁 CAS、顺序消费，这才撑得起单服几十万 QPS。BlockingQueue 只用在非核心异步链路。",
      point: "考察阻塞队列特性区分与有界选型意识，延伸到 Disruptor 认知",
      approach: "按「有界/无界/不存货/优先/延迟」分类快速过五种队列 → 重点讲选型思路：生产必须有界防 OOM → 拔高：核心链路上 Disruptor 的四点理由。别只背特性，面试官想听你怎么选、为什么这么选。",
      followups: [{q:"LinkedBlockingQueue 默认容量有什么坑？", a:"默认 Integer.MAX_VALUE 近似无界，任务洪峰时队列无限堆积导致 OOM，且救急线程永不触发。生产必须显式传容量。"}, {q:"DelayQueue 能用来做什么？", a:"元素到期才能取，适合轻量定时场景：buff 到期结算、定时活动开启。量大时单队列性能不够，改用时间轮（HashedWheelTimer）方案。"}, {q:"为什么 Disruptor 比 BlockingQueue 快？", a:"预分配环形数组零 GC、序号 CAS 只有一处竞争、缓存行填充防伪共享、消费者批量 drain 摊薄调度；BlockingQueue 有锁、节点对象有 GC 压力。"}],
      memory: "口诀：「数组有界一把锁，链表两锁近无界，同步队列不存货，优先延迟各管各」；游戏服核心链路记「上 Disruptor，不上 BlockingQueue」。",
      tags: ["BlockingQueue", "队列选型", "Disruptor"]
    },
    {
      id: "java-concurrent-10",
      level: 1,
      q: "ThreadLocal 是什么？工作中典型的使用场景有哪些？",
      a: "核心结论：ThreadLocal 为每个线程提供变量的独立副本，实现「线程封闭」，用空间换无锁。\n原理：每个 Thread 内部有个 ThreadLocalMap，key 是 ThreadLocal 对象（弱引用），value 是存的值。set/get 都是操作当前线程自己的 Map，天然无线程竞争。\n典型场景：\n1. 请求上下文传递：玩家消息处理链路上，把 playerId、traceId、协议流水号放进 ThreadLocal，日志输出/权限校验随时取，不用层层传参。\n2. SimpleDateFormat 等线程不安全工具的线程封闭。\n3. 数据库连接/事务上下文的线程绑定（Spring 的 TransactionSynchronizationManager 就是这么做的）。\n使用铁律：\n1. 用完必须 remove()，尤其在游戏服/Netty 这种线程复用的环境——线程不会销毁，脏数据会被下一个消息读到（玩家 A 的上下文串到玩家 B，是严重 bug）。\n2. 尽量声明为 static final，避免每个实例都建 ThreadLocal 造成 Entry 膨胀。",
      point: "考察 ThreadLocal 原理及线程复用环境下的使用规范",
      approach: "先给「线程封闭、空间换无锁」定位 → 讲 ThreadLocalMap 结构（key 弱引用）→ 三个典型场景 → 铁律收尾：用完 remove + static final。重点预警池化线程脏数据串号，这是游戏服真实事故点，要当重心讲。",
      followups: [{q:"线程池环境下用 ThreadLocal 有什么坑？", a:"线程复用不销毁，上次 set 的值残留被下个任务读到，轻则脏数据重则玩家串号。必须 finally 里 remove()，或封装 try-with-resources 自动清理。"}, {q:"InheritableThreadLocal 和 TransmittableThreadLocal 解决什么问题？", a:"前者在线程创建时把父线程值拷贝给子线程；但线程池复用线程使创建时拷贝失效，后者通过包装任务在提交时传递上下文，解决池化异步场景的跨线程传递。"}, {q:"ThreadLocal 为什么 key 用弱引用还会内存泄漏？", a:"key 被回收后 Entry 的 value 仍被强引用链持有：Thread → ThreadLocalMap → Entry → value，池化线程不死 value 永远释放不掉，只能靠 remove 清理。"}],
      memory: "类比：ThreadLocal 是「每个员工的私人抽屉」，钥匙（弱引用）丢了抽屉里的东西还锁在里面——所以用完要主动清空，别把上个客人的东西留给下个客人。",
      tags: ["ThreadLocal", "上下文传递", "线程封闭"]
    },
    {
      id: "java-concurrent-11",
      level: 2,
      q: "synchronized 的锁升级过程是怎样的？为什么要设计升级而不是直接用重量级锁？",
      a: "核心结论：JDK6 后 synchronized 走「无锁 → 偏向锁 → 轻量级锁 → 重量级锁」的升级路线（JDK15 起偏向锁默认废弃），只升不降，目的是按竞争强度逐级付出对应成本。\n1. 无锁：对象刚创建，Mark Word 存 hashcode 等。\n2. 偏向锁：Mark Word 记录线程 ID，同一线程再次进入只需比对 ID，零 CAS 开销——假设大多数锁只有一个线程用。高并发下撤销成本高，JDK15 默认关闭。\n3. 轻量级锁：出现竞争，线程在栈上建 Lock Record，CAS 把 Mark Word 指向它；失败者自旋（自适应自旋）等待，不阻塞。\n4. 重量级锁：自旋失败/竞争激烈，Mark Word 指向 ObjectMonitor，抢不到的线程进队列 BLOCKED，靠操作系统 mutex 挂起唤醒。\n为什么这样设计：\n1. 无竞争时：加锁开销 ≈ 一次 Mark Word CAS，几乎免费。\n2. 轻竞争时：自旋几十纳秒就能拿到锁，比线程挂起/唤醒（微秒级上下文切换）便宜得多。\n3. 重竞争时才付出内核态切换成本。\n实战结论：别再说「synchronized 很重」——JDK6 优化后轻竞争下性能与 ReentrantLock 相当；游戏服里低竞争的短临界区（如读配置）用 synchronized 简单可靠。",
      point: "考察锁升级四阶段细节与按竞争强度分层成本的设计思想",
      approach: "先给升级路线和「只升不降」结论 → 四阶段各说清 Mark Word 怎么变、成本多少 → 用成本分层逻辑回答「为什么不直接重量级」→ 收尾纠偏「synchronized 很重」的过时认知。提一句 JDK15 偏向锁废弃，显得跟得上版本演进。",
      followups: [{q:"锁消除和锁粗化是什么？", a:"锁消除：逃逸分析发现锁对象不逃出线程，JIT 直接去掉锁；锁粗化：循环内反复对同一对象加解锁，合并成循环外一把锁。都是 JIT 编译期自动优化。"}, {q:"自旋锁和自适应自旋的区别？", a:"自旋是抢锁失败后空转等待不放弃 CPU；自适应自旋由 JVM 根据该锁历史上的自旋成功率动态调整次数，上次成功率高就多转几圈，避免固定次数的浪费。"}, {q:"JDK15 废弃偏向锁的原因？", a:"高竞争下撤销偏向需要全局安全点，成本高；现代应用多线程是常态，偏向收益低；实现维护复杂，故默认关闭并后续移除。"}],
      memory: "口诀：「偏轻重量，只升不降」；类比小区门禁：认识你的脸直接放行（偏向）→ 排队刷卡稍等（轻量自旋）→ 人太多进等候室叫号（重量级）。",
      tags: ["锁升级", "偏向锁", "Mark Word", "自适应自旋"]
    },
    {
      id: "java-concurrent-12",
      level: 2,
      q: "讲讲 JMM 和 happens-before 规则。为什么需要 happens-before？",
      a: "核心结论：JMM 抽象出「主内存 + 工作内存」模型屏蔽硬件差异；happens-before 是 JMM 给程序员的可见性承诺——A happens-before B，则 A 的结果对 B 可见且 A 排在 B 之前。\nJMM 解决三大问题：可见性（缓存导致）、有序性（重排序导致）、原子性。\nhappens-before 核心规则：\n1. 程序次序规则：单线程内书写顺序在前 happens-before 在后。\n2. 锁规则：unlock happens-before 后续对同一锁的 lock。\n3. volatile 规则：对 volatile 的写 happens-before 后续对它的读。\n4. 传递性：A hb B，B hb C，则 A hb C。\n5. 线程启动规则：Thread.start() happens-before 线程内任何动作。\n6. 线程终止规则：线程内所有动作 happens-before join() 返回。\n7. 中断规则：interrupt() happens-before 检测到中断。\n8. 对象终结规则：构造完成 happens-before finalize()。\n为什么需要它：重排序在单线程语义不变的前提下是合法的，但多线程下会暴露（DCL 半初始化、标志位先于数据被读到）。happens-before 让你不用研究每种 CPU 的内存模型，只要遵守规则，JMM 保证正确性。\n游戏服例子：GM 线程修改配置表后 set volatile version 加 1，业务线程读 version 变了再读配置——volatile 规则保证读到的是完整新配置而不是半更新状态。",
      point: "考察 JMM 模型与 happens-before 规则的掌握及实际应用",
      approach: "先定义 JMM（主内存 + 工作内存）及它解决的三大问题 → 八条 hb 规则用口诀带过，重点讲透锁、volatile、传递三条 → 回答「为什么需要」时讲重排序边界 → 项目例子收尾。八条不必逐条展开，挑重点讲深。",
      followups: [{q:"as-if-serial 和 happens-before 的关系？", a:"as-if-serial 保证单线程内重排序不改变执行结果，happens-before 管多线程间的可见性承诺；两者共同划定重排序的合法边界，一个对内一个对外。"}, {q:"final 域的语义是什么？", a:"构造函数内对 final 字段的写入 happens-before 后续对该对象引用的读取，禁止 final 写与引用逸出的重排，保证其他线程看到构造完成的对象，即安全发布。"}, {q:"DCL 单例违反了哪条规则才出问题？", a:"非 volatile 时 instance 赋值与字段初始化之间没有 hb 约束，重排后其他线程读到引用非空但未初始化的对象——缺的正是 volatile 规则的保护。"}],
      memory: "口诀：「程锁 volatile 传，启动终止中断终」——程序次序、锁、volatile、传递、start、join、interrupt、finalize 八条规则；hb = 「先发生的，后到的必须看得见」。",
      tags: ["JMM", "happens-before", "可见性", "重排序"]
    },
    {
      id: "java-concurrent-13",
      level: 2,
      q: "什么是 ABA 问题？怎么解决？ AtomicStampedReference 的原理是什么？",
      a: "核心结论：ABA 是 CAS 检查「值没变」时无法感知「值被改走又改回来」的过程，需要给值加版本号解决。\nABA 场景：线程 1 读到 A，期间线程 2 把 A→B→A，线程 1 CAS(A→C) 成功——值虽然还是 A，但中间状态变化可能带来隐患。经典案例是无锁栈 pop 时的节点复用问题。\n什么时候真的有问题：\n1. 纯数值增减（金额、计数）：ABA 通常无害，结果一样。\n2. 涉及对象状态/链表节点复用：ABA 会引入逻辑错误，必须解决。\n解决方案：\n1. AtomicStampedReference：值 + int 版本号打包成 Pair，CAS 同时比较值和版本号，每次修改版本 +1。\n2. AtomicMarkableReference：值 + boolean 标记，只关心「是否被改过」时用，更轻。\n3. 数据库层面等价物：乐观锁 version 字段（MyBatis-Plus @Version），玩家数据 UPDATE ... WHERE version = oldVersion，也是同一思想的落库版本。\n游戏服结合：玩家数据并发修改用「版本号乐观锁」本质上就是带 stamp 的 CAS——从 DB 读出 version=5，修改后 UPDATE 条件带 version=5，影响行数 0 说明被别人改过，重读重试。",
      point: "考察 ABA 本质、何时真正有害及版本号解决方案的迁移能力",
      approach: "先定义 ABA → 给关键洞察：纯数值场景无害、对象/链表场景致命 → 解决方案三件套（Stamped/Markable/DB version）→ 落到项目乐观锁实践。「区分何时真的有问题」比只背定义高一个档次，务必讲。",
      followups: [{q:"AtomicStampedReference 是怎么把引用和版本绑在一起的？", a:"把引用和 int 版本号封装成不可变 Pair 对象，CAS 整体替换 Pair 引用，比较时值和版本同时校验；每次修改生成新版本 Pair，改走又改回版本也不同。"}, {q:"无锁数据结构里 ABA 为什么会致命？举一个链表/栈的例子。", a:"无锁栈 pop：线程读到 head=A、next=B，期间 A 被弹出、C/D 压入后 A 又压回栈顶，CAS(head A→B) 成功，但 B 早已不在栈中，C、D 永久丢失。"}, {q:"你们项目里玩家数据落库有没有用乐观锁版本号？冲突了怎么处理？", a:"玩家表带 version 字段，UPDATE 时 WHERE version=旧值，影响行数 0 即冲突；冲突后重读合并重试并告警，排查是否有 GM 直改库等旁路写入。"}],
      memory: "类比：ABA 像「杯子被人拿走又放回原位」，看起来没动过；加版本号等于杯底刻编号，每次动过编号 +1，一眼识破。",
      tags: ["ABA", "AtomicStampedReference", "乐观锁", "版本号"]
    },
    {
      id: "java-concurrent-14",
      level: 2,
      q: "讲讲 AQS 的核心原理。为什么说它是 JUC 的基石？",
      a: "核心结论：AQS = 一个 volatile int state + 一个 CLH 变体的 FIFO 同步队列 + 模板方法模式，JUC 里 ReentrantLock、Semaphore、CountDownLatch 等都靠它实现。\n核心结构：\n1. state：volatile int，表示同步状态（锁的重入次数/剩余许可数/倒计数），子类自定义语义。\n2. 同步队列：双向链表，抢锁失败的线程包装成 Node 入队，park 挂起；前驱释放锁后 unpark 唤醒。\n3. 模板方法：tryAcquire/tryRelease/tryAcquireShared 由子类实现，AQS 管排队、挂起、唤醒的通用骨架。\n获取锁流程（独占）：\n1. tryAcquire CAS 抢 state，成功就完事。\n2. 失败 → 构造 Node 自旋 CAS 入队尾。\n3. 自旋检查前驱是 head 就再试一次抢锁（给一次快速路径）。\n4. 否则把前驱状态置 SIGNAL，LockSupport.park 挂起（状态 WAITING）。\n释放流程：tryRelease 减 state 归零后，unpark 后继节点。\n为什么是基石：它把「排队阻塞唤醒」这个并发框架里最脏最通用的部分沉淀成模板，JUC 组件只需要定义 state 语义。ReentrantLock 的 Sync、Semaphore 的 Sync、CountDownLatch 的 Sync、ReentrantReadWriteLock 的 Sync 全是 AQS 子类。\n拓展：Condition（await/signal）也基于 AQS，每个 Condition 一个等待队列，signal 时从等待队列搬进同步队列。",
      point: "考察 AQS 三大核心结构与独占模式获取/释放全流程",
      approach: "先给「state + CLH 队列 + 模板方法」总纲 → 独占获取四步讲清（tryAcquire、入队、查前驱、park）→ 释放与唤醒 → 用 JUC 组件全是 AQS 子类论证「基石」。能现场画出 Node 排队与 SIGNAL 语义就是高分答案。",
      followups: [{q:"CLH 队列为什么前驱释放后才唤醒后继？", a:"Node 的 waitStatus=SIGNAL 表示「后继需要我唤醒」；线程 park 前把前驱置 SIGNAL，前驱释放锁时发现有责任就 unpark 后继，形成可靠唤醒链，避免信号丢失。"}, {q:"共享模式和独占模式的区别？", a:"独占一次只放行一个线程（锁）；共享 acquire 成功后会把唤醒传播给后继共享节点（setHeadAndPropagate），如 CountDownLatch 倒数到 0 时一串等待者级联全醒。"}, {q:"AQS 为什么用 park/unpark 而不是 wait/notify？", a:"LockSupport 无需先持锁，permit 语义允许 unpark 先于 park 调用（许可先存着），天然免疫 wait/notify「先 notify 后 wait 信号丢失」的时序竞态。"}, {q:"让你用 AQS 实现一个「最多 N 个玩家同时进入战斗服」的同步器，state 怎么设计？", a:"继承 AQS，state 初始化为 N 表示剩余名额；tryAcquireShared 里 CAS 减 1，≥0 成功，不足返回负数排队；玩家退出 releaseShared 加回名额并唤醒后继。"}],
      memory: "口诀：「一个 state 一条队，模板方法子类配」；类比：state 是「号」，队列是「办事大厅等候区」，AQS 是发号叫号系统，Lock/Semaphore 只是不同窗口。",
      tags: ["AQS", "CLH队列", "JUC", "模板方法"]
    },
    {
      id: "java-concurrent-15",
      level: 2,
      q: "ReentrantLock 和 synchronized 有什么区别？公平锁和非公平锁怎么选？",
      a: "核心结论：功能上都是可重入互斥锁，ReentrantLock 胜在灵活（可中断、超时、公平、多 Condition），synchronized 胜在简单不易错。\n区别对比：\n1. 实现层次：synchronized 是 JVM 内置（monitorenter/exit），ReentrantLock 是 JDK 类（基于 AQS）。\n2. 锁获取：ReentrantLock 支持 lockInterruptibly()（等锁时可响应中断）、tryLock(timeout)（超时放弃防死锁）——游戏服里跨资源加锁时超时获取很重要。\n3. 公平性：synchronized 只能非公平；ReentrantLock 可选公平（按排队顺序，吞吐低）/非公平（默认，允许插队，吞吐高）。\n4. 条件变量：ReentrantLock 可绑多个 Condition 精确唤醒；synchronized 只有一个 wait set，只能 notifyAll 全叫醒。\n5. 释放方式：synchronized 自动释放（编译器保证 finally monitorexit）；ReentrantLock 必须手动 unlock，忘了就锁泄漏——必须 finally。\n6. 性能：JDK6 优化后低竞争相当；极端高竞争 ReentrantLock 略稳。\n选型建议：\n1. 默认 synchronized：简单场景少犯错。\n2. 需要超时/可中断/多条件变量/公平性时才上 ReentrantLock。\n3. 游戏服经验：玩家数据这类高并发场景最好别靠锁——用「单线程化处理」（玩家绑定固定业务线程）从根上消灭竞争，锁只是兜底。",
      point: "考察两种锁的全维度对比与按需选型的判断力",
      approach: "先给「自动挡 vs 手动挡」定位 → 按实现层次、可中断/超时、公平性、Condition、释放方式、性能六维度对比 → 给选型建议 → 拔高：游戏服靠线程模型从根上消灭锁。六维度别漏「必须 finally unlock」这个易错点。",
      followups: [{q:"非公平锁为什么吞吐更高？", a:"锁刚释放时新来线程可直接插队抢到，趁队列线程还没被唤醒完成交接，减少 park/unpark 次数；代价是被唤醒线程发现锁又被占继续睡，可能饥饿。"}, {q:"Condition 相比 wait/notify 好在哪？", a:"一把锁可挂多个等待队列，生产者消费者分别 await/signal，精确唤醒该醒的类别；wait/notify 只有一个等待集合，notifyAll 全叫醒白抢锁，notify 可能叫错人丢信号。"}, {q:"ReentrantLock 可重入是怎么实现的？", a:"AQS 的 state 记录重入次数，exclusiveOwnerThread 记录持有线程；同线程再次 lock 时 state+1，unlock 减到 0 才真正释放并清空持有者。"}],
      memory: "口诀：「synchronized 自动挡，Lock 手动挡」——自动挡省心，手动挡能超时、能中断、能公平、能多车道（Condition），但忘了挂 P 挡（unlock）就出事。",
      tags: ["ReentrantLock", "公平锁", "Condition", "锁选型"]
    },
    {
      id: "java-concurrent-16",
      level: 2,
      q: "ReentrantReadWriteLock 的原理和使用场景？读锁和写锁的 state 是怎么存的？",
      a: "核心结论：读写锁把互斥拆成「读读共享、读写/写写互斥」，用一个 int state 的高 16 位存读锁计数、低 16 位存写锁重入次数，适合读多写少。\nstate 拆分（AQS 的经典复用）：\n1. 高 16 位：读锁持有总数（共享模式，多个线程同时持有）。\n2. 低 16 位：写锁重入次数（独占模式）。\n3. 每个线程的读锁计数存在 HoldCounter/ThreadLocal 里。\n锁降级与升级：\n1. 支持锁降级：持有写锁时可再获取读锁，然后释放写锁（保证修改后立刻读到自己的数据）。\n2. 不支持锁升级：持读锁直接抢写锁会死锁（两个读者互相等对方放读锁），必须先释放读锁。\n使用场景（游戏服非常典型）：\n1. 配置表/导表数据：策划导表生成全服只读配置，99.99% 读，热更新时才写——ReadWriteLock 或干脆 volatile 引用 + 不可变对象整体替换（更推荐，读侧完全无锁）。\n2. 排行榜缓存：读远多于写时可用读写锁保护本地缓存结构。\n3. 注意事项：写线程多时读写锁反而比互斥锁慢；读锁持有时写线程排队，要避免写饥饿（可用公平模式或 StampedLock 乐观读）。\n拓展：JDK8 StampedLock 提供乐观读（tryOptimisticRead 无锁读 + 校验戳），读多写少场景性能更高，但不可重入。",
      point: "考察读写锁 state 高低位拆分设计、降级规则与场景判断",
      approach: "先给「读读共享、读写互斥」语义 → state 高 16 读低 16 写的经典复用设计 → 锁降级支持、升级禁止及死锁原因 → 游戏服读多写少场景 → 提 StampedLock 乐观读作延伸。state 拆分是硬知识点，必须说准。",
      followups: [{q:"为什么不能锁升级？用代码推演一下死锁过程。", a:"线程 A、B 各持读锁，都申请写锁：A 等 B 放读锁，B 等 A 放读锁，循环等待死锁。要升级必须先放读锁再抢写锁，中间数据可能被改，需重新校验。"}, {q:"StampedLock 的乐观读怎么校验数据有效性？", a:"读前 tryOptimisticRead 拿 stamp → 把共享变量拷到局部 → validate(stamp) 检查期间有无写锁被获取；有效直接用，失败降级 readLock 悲观重读。"}, {q:"配置表热更新用读写锁和用「不可变对象 + volatile 引用整体替换」各有什么优劣？", a:"读写锁读侧仍有 CAS 开销、写时阻塞读；不可变整体替换读侧完全无锁无阻塞，但写时要全量构建新对象。配置读极多写极少，整体替换更优。"}],
      memory: "口诀：「高 16 读低 16 写，读读不互斥」；类比图书馆：很多人同时看书（读锁共享），有人要修订书目时（写锁）得清场，且只能「先改完再放手让人看」（只降级不升级）。",
      tags: ["读写锁", "ReentrantReadWriteLock", "StampedLock", "配置热更新"]
    },
    {
      id: "java-concurrent-17",
      level: 2,
      q: "CountDownLatch、CyclicBarrier、Semaphore 有什么区别？各举一个游戏服务器的使用场景。",
      a: "核心结论：三者都是 AQS 共享模式的封装——CountDownLatch 是「一次性倒数门闩」，CyclicBarrier 是「可重复集合点」，Semaphore 是「限流许可池」。\n1. CountDownLatch：构造给 N，countDown() 减一，await() 阻塞到 0；不可复用。\n游戏场景：服务器启动时等待多个初始化模块（配置加载、DB 连接、缓存预热）全部 ready 才对外开放端口；战斗结算时等所有玩家数据落库完成再广播结果。\n2. CyclicBarrier：N 个线程都到齐（await）才一起放行，到齐后自动重置可循环用；可传 barrierAction 在放行前跑一段汇总逻辑。\n游戏场景：回合制战斗里等 N 个玩家的操作指令都收集齐，再统一进入结算阶段（barrierAction 做汇总）；压测时让 N 个模拟客户端同时开跑。\n3. Semaphore：acquire() 拿许可，release() 还许可，控制并发数量。\n游戏场景：限制同时进入跨服战斗的玩家并发数；限制 DB 落盘线程池对数据库的最大并发连接；GM 批量操作限流保护下游。\n记忆区分：\n1. Latch 像「等大家都上车才发车」，一次性的。\n2. Barrier 像「每局大家都准备好才开局」，一局一局循环。\n3. Semaphore 像「停车场车位」，有空位才能进。",
      point: "考察三个 AQS 共享模式同步器的语义区分与场景匹配",
      approach: "先一句话各定位（一次性倒数门闩/循环集合点/限流许可池）→ 逐个讲构造参数与核心方法 → 各配一个游戏场景 → 用口诀收束。关键是场景要具体，说清为什么这个场景用它而不是另两个。",
      followups: [{q:"CountDownLatch 用完后能 reset 吗？为什么 CyclicBarrier 可以？", a:"Latch 的 state 到 0 后无重置逻辑，一次性设计；CyclicBarrier 有 generation 概念，到齐后换代重置 count，broken 时换代让所有等待者抛 BrokenBarrierException。"}, {q:"CyclicBarrier 中一个线程 await 时被中断会怎样？", a:"栅栏被打破（breakBarrier）：该线程抛 InterruptedException，其他等待线程收到 BrokenBarrierException，barrier 进入 broken 状态换代。"}, {q:"Semaphore 的公平/非公平有什么区别？", a:"公平模式许可按 FIFO 排队发放，新线程先看队列有没有人，防饥饿但吞吐低；非公平允许插队抢许可，吞吐高，是默认模式。"}],
      memory: "口诀：「闩倒数、栅集合、量限流」——Latch 数到 0 开门（一次），Barrier 人到齐一起走（循环），Semaphore 按发牌放人（限流）。",
      tags: ["CountDownLatch", "CyclicBarrier", "Semaphore", "限流"]
    },
    {
      id: "java-concurrent-18",
      level: 2,
      q: "ThreadLocal 为什么会内存泄漏？key 已经是弱引用了为什么还不够？Netty/游戏服环境下怎么防？",
      a: "核心结论：泄漏的根源不是弱引用 key，而是「线程长命 + Entry 的 value 是强引用」——key 被 GC 后 value 成了没人能访问却永远释放不掉的孤儿。\n引用链分析：\n1. ThreadLocalMap 的 Entry：key 是弱引用，value 是强引用。\n2. 强引用链：Thread（线程池里的长命线程）→ threadLocals → Entry 数组 → value。\n3. ThreadLocal 对象没人用后 key 被回收，Entry 变成 key=null 的脏条目，但 value 仍被线程强引用，线程不死（池化线程永不死）value 永不释放 → 泄漏。\n4. JDK 的缓解：set/get/remove 时会顺手清理 key=null 的脏 Entry，但只有你再操作同一个 ThreadLocal 才触发，不可靠。\n正确用法：\n1. 用完 finally 里 remove()，这是唯一铁律。\n2. 声明 static final，全类共享一个 ThreadLocal 实例，key 少而稳定。\n3. 不要往 ThreadLocal 里塞大对象。\n游戏服/Netty 环境为什么高危：\n1. Netty IO 线程和 Disruptor 业务线程都是「永生的池化线程」，任何残留都是永久泄漏。\n2. 玩家上下文串号：上个消息的玩家对象没 remove，下个消息读到上个玩家的数据——不只是泄漏，是数据错乱事故。\n3. 规范：协议解码后放入上下文 → 业务处理 → finally remove，或者封装成 try-with-resources 的 ContextScope。",
      point: "考察 ThreadLocal 泄漏的完整引用链分析及高危环境防护",
      approach: "先给结论「根因是线程长命 + value 强引用，不是弱引用 key」→ 画完整引用链 Thread→Map→Entry→value → 讲 JDK 缓解机制为何不可靠 → 正确用法 → 结合 Netty/游戏服永生线程讲串号危害。引用链必须讲完整，这是核心得分点。",
      followups: [{q:"既然会泄漏，为什么 key 还要设计成弱引用而不是强引用？", a:"若 key 强引用，ThreadLocal 被业务弃用后也无法回收，key 和 value 双泄漏；弱引用至少让 key 可回收、Entry 变脏可被探查清理，两害相权取其轻。"}, {q:"怎么排查 ThreadLocal 泄漏？", a:"jmap dump 堆 → MAT 直方图看 ThreadLocalMap$Entry 数量异常 → 支配树追 value 引用链定位到具体 ThreadLocal 和持有线程；也可用 OQL 筛 key=null 的脏 Entry。"}, {q:"你们的玩家上下文在 Disruptor 消费者线程里是怎么传递和清理的？", a:"解码层把上下文塞进 ThreadLocal，业务处理完在 finally remove；或封装 ContextScope 实现 AutoCloseable，用 try-with-resources 保证清理，杜绝跨消息残留。"}],
      memory: "口诀：「key 弱 value 强，线程不死 value 亡不了」；类比：更衣室柜子钥匙（弱引用）扔了，柜子里东西（value）还在，柜子（线程）又被下一个客人用——所以用完必须自己清空柜子。",
      tags: ["ThreadLocal", "内存泄漏", "弱引用", "Netty"]
    },
    {
      id: "java-concurrent-19",
      level: 2,
      q: "游戏服务器的线程模型一般怎么设计？为什么「一个玩家的消息在同一个线程串行处理」？你们项目用 Disruptor 解决了什么问题？",
      a: "核心结论：游戏服主流是「IO 线程与业务线程分离 + 按玩家 ID 哈希绑定业务线程」，用线程模型消灭锁，而不是用锁解决竞争。\n典型三层线程模型：\n1. Netty boss/worker IO 线程：只做收发包、编解码，绝不做业务，绝不阻塞。\n2. 业务逻辑线程（常通过 Disruptor 投递）：同一玩家 ID 哈希到固定线程，保证该玩家所有消息严格串行——玩家自己的数据操作天然无竞争，不需要任何锁。\n3. 异步线程池：DB 落盘、日志、BI 上报等慢 IO，业务线程只发事件不等待。\n为什么玩家串行：\n1. 玩家数据（背包、金币、任务）是高频读写热点，如果多线程并发改，要么加锁（性能差、易死锁），要么出脏数据。\n2. 哈希绑定后，玩家 A 的「扣金币买道具」和「领任务奖励」排队执行，逻辑简单、无锁、可复现。\n3. 跨玩家交互（交易、组队、战斗）才需要小心：要么消息转发到目标玩家线程，要么战斗中数据拷贝进战斗上下文。\nDisruptor 解决什么：\n1. Netty IO 线程到业务线程的投递需要队列，LinkedBlockingQueue 有锁、有 GC 压力；Disruptor 的 RingBuffer 预分配数组 + CAS 序号 + 单消费者，无锁无 GC，单服几十万消息/秒。\n2. 多消费者按玩家哈希分工（每个消费者认领一批玩家），仍保持单玩家串行。\n3. 批处理能力：消费者一次 drain 多条消息，降低调度开销。",
      point: "考察游戏服三层线程模型设计与用串行化消灭锁的架构思想",
      approach: "先给核心思想「用线程模型消灭锁，而不是用锁解决竞争」→ 三层模型逐个讲职责与禁区 → 玩家串行的三条理由 → Disruptor 解决的具体问题。这是经历题，多讲项目取舍和量级数字，少背概念。",
      followups: [{q:"跨服战斗涉及多个服的玩家，线程模型怎么破？", a:"战斗独立成战斗服：玩家进战斗时把所需数据拷贝进战斗上下文，战斗内闭环结算；结束后结果异步回写原服，避免跨服共享对象的并发问题。"}, {q:"业务线程里绝对不能做什么操作？", a:"禁止一切阻塞调用：同步 DB、HTTP、长时间持锁。该线程绑着成百上千玩家，一次阻塞全体卡顿；慢操作必须投递异步线程池，业务线程只发事件。"}, {q:"玩家哈希绑线程，某玩家下线后线程负载不均怎么办？", a:"按玩家 ID 取模天然近均匀；个别超活跃玩家可用一致性哈希或按线程负载动态迁移绑定；通常接受小概率不均，监控各线程队列水位预警。"}],
      memory: "口诀：「IO 不沾业务，玩家认死线程，慢活异步去」；类比：银行一个柜员固定服务一批 VIP 客户（玩家哈希绑定），VIP 的所有业务都在这个柜员这儿排队办，永远不会两个人同时改同一本账。",
      tags: ["游戏服线程模型", "Disruptor", "Netty", "玩家串行化"]
    },
    {
      id: "java-concurrent-20",
      level: 2,
      q: "CompletableFuture 相比 Future 解决了什么问题？登录流程中怎么用它做异步编排？",
      a: "核心结论：Future 只能 get() 阻塞等结果，CompletableFuture 支持回调编排（thenApply/thenCompose/thenCombine/allOf），把异步任务变成声明式流水线。\nFuture 的三大痛点：\n1. 只能阻塞 get() 或轮询 isDone()。\n2. 无法表达「A 完成后自动执行 B」的依赖关系。\n3. 多个 Future 的组合（全部完成/任一完成）要自己写样板代码。\nCompletableFuture 核心 API：\n1. supplyAsync/runAsync：异步启动任务（默认 ForkJoinPool，生产必须传自定义线程池！）。\n2. thenApply（转换）/thenAccept（消费）/thenRun（触发）：串行编排。\n3. thenCompose：结果作为下一个异步任务的输入（拍平 Future 嵌套，等价 flatMap）。\n4. thenCombine：两个独立任务都完成后合并结果。\n5. allOf/anyOf：多任务聚合。\n6. exceptionally/handle：异常处理，必须配，否则异常静默吞掉。\n游戏服登录流程实战：玩家登录要并行加载「背包、任务、好友、邮件」四份数据（各自查库/缓存 50ms），串行要 200ms，用 CompletableFuture.allOf(背包, 任务, 好友, 邮件).thenApply(组装角色快照) 并行只需 50ms+，且每路异常可独立降级（邮件加载失败给空列表不影响登录）。\n注意坑：\n1. 默认 ForkJoinPool 是公共池，游戏服必须传专属线程池并命名。\n2. 回调默认在「完成任务的线程」执行，带 Async 后缀才切到指定池——注意别在 Netty IO 线程的回调里跑重业务。\n3. 超时控制用 orTimeout/completeOnTimeout（JDK9+）。",
      point: "考察 CF 异步编排 API 体系及登录并行加载的实战应用",
      approach: "先讲 Future 三痛点 → CF 核心 API 分组讲（启动/串行/组合/聚合/异常）→ 登录四路并行实战（200ms→50ms + 单路降级）→ 三个坑收尾（默认池、回调线程、超时）。登录案例是记忆点，务必讲出降级细节和数字对比。",
      followups: [{q:"thenApply 和 thenCompose 的区别？", a:"thenApply 的函数返回普通值 T，结果包成 CF<T>；thenCompose 的函数返回 CF<T>，拍平嵌套避免 CF<CF<T>>，等价 flatMap，用于串起两个异步调用。"}, {q:"allOf 里一个任务失败了，其他任务结果怎么拿到？", a:"allOf 整体以第一个异常完成，其他结果拿不到。做法：每个子任务先各自 exceptionally 兜底返回降级值，再 allOf，保证整体不因单路失败而失败。"}, {q:"CompletableFuture 的默认线程池有什么坑？", a:"默认用 ForkJoinPool.commonPool，全 JVM 共享且只有核数-1 个线程；阻塞任务占满公共池会连累所有 parallelStream 和默认 CF 任务。生产必须传自定义命名线程池。"}],
      memory: "口诀：「Future 是取餐牌只能干等，CompletableFuture 是流水线」；登录记「四路并发 allOf，一路挂了 exceptionally 降级」——200ms 变 50ms。",
      tags: ["CompletableFuture", "异步编排", "登录流程", "并行加载"]
    },
    {
      id: "java-concurrent-21",
      level: 3,
      q: "什么是伪共享（False Sharing）？Disruptor 是怎么解决的？JDK8 提供了什么注解？",
      a: "核心结论：伪共享是多个线程修改「同一缓存行里不同变量」导致的缓存行在 CPU 核间无效化乒乓，让本应无竞争的并发大幅变慢；解法是让热点变量独占缓存行。\n原理：\n1. CPU 缓存以缓存行（通常 64 字节）为单位加载/失效。\n2. 变量 X、Y 落在同一缓存行，核 1 改 X、核 2 改 Y——虽然逻辑上互不相关，但任何一方修改都会让整个缓存行在另一核失效（MESI 协议），强制重新加载，性能雪崩。\n解决手段：\n1. 手工填充：在热点字段前后塞 7 个 long（56 字节）撑满缓存行——Disruptor 早期版本和 LongAdder 的 Cell 就是这么干的。\n2. JDK8 的 @Contended（jdk.internal.vm.annotation.Contended）：JVM 自动填充，需 -XX:-RestrictContended 才允许业务代码使用；LongAdder 的 Cell 类就标了它。\n3. JDK9+ 可用 jdk.internal 或 VarHandle 配合，JDK17+ 移到 java.lang.invoke 内部包，业务慎用。\n与 Disruptor 的关系：Disruptor 的 RingBuffer 里 cursor 序号、消费者序号都是多线程高频写的 long，如果不填充，序号 CAS 时会互相踩缓存行——这正是 Disruptor 精心做缓存行填充的原因，也是它敢称「无锁高性能」的底层细节之一。\n排查工具：JOL 看对象布局，perf 看缓存行失效（cache-misses）。",
      point: "考察缓存行机制、伪共享成因及填充方案的底层功底",
      approach: "先定义伪共享（一行两变量、缓存行乒乓失效）→ 讲 MESI 失效机制 → 两种解法（手工填充 7 个 long、@Contended）→ 落到 Disruptor 序号字段的填充实践。缓存行 64 字节、7 个 long 这些数字要说准，这是底层功底题。",
      followups: [{q:"为什么不直接把每个变量都放独立缓存行？代价是什么？", a:"代价是内存膨胀和缓存容量浪费：每个变量独占 64 字节，缓存能装的变量变少、命中率下降。只给跨线程高频写的少数字段做填充才划算。"}, {q:"LongAdder 的 Cell 和 @Contended 一起解决了什么问题？", a:"Cell 分段把计数分散降低 CAS 冲突，但多个 Cell 若挤在同一缓存行又会伪共享；@Contended 让每个 Cell 独占缓存行，分段 + 防乒乓双管齐下。"}, {q:"你们项目压测时有没有定位过缓存层面的性能问题？用了什么工具？", a:"压测中用 perf stat 看 cache-misses、LLC 失效率异常，JOL 打印对象布局确认热点字段是否同行，再对比填充前后 TPS 验证是否伪共享。"}],
      memory: "类比：两个厨师共用一块砧板（缓存行），哪怕各切各的菜（不同变量），拿放砧板也要互相等——伪共享的解法就是「一人一块砧板」（缓存行填充）。",
      tags: ["伪共享", "缓存行", "Disruptor", "@Contended"]
    },
    {
      id: "java-concurrent-22",
      level: 3,
      q: "Disruptor 为什么快？RingBuffer、序号机制和等待策略分别讲讲。它和游戏服业务线程模型怎么配合？",
      a: "核心结论：Disruptor 快在四点——环形数组预分配零 GC、CAS 序号无锁、缓存行填充防伪共享、批量消费。\n核心机制：\n1. RingBuffer：定长环形数组，启动时预分配所有 Event 对象，之后只做「覆盖写」不做对象创建——GC 压力为零，天然适合高频小消息。\n2. 序号机制：生产者用 CAS 竞争一个递增序号（cursor），消费者各自维护消费序号，通过 SequenceBarrier 等待可用数据；只有「申请序号」一处 CAS 竞争，真正的写数据无锁。\n3. 序号取模定位：index = sequence % capacity（capacity 取 2 的幂，位运算取模），producer 覆盖写前检查最慢消费者序号防环绕覆盖。\n4. 缓存行填充：cursor、消费者 sequence 等高频写字段填充 7 个 long 防伪共享。\n5. 等待策略（消费者侧）：BlockingWaitStrategy（锁+Condition，省 CPU）、SleepingWaitStrategy（自旋+让出+睡眠，均衡，适合异步日志）、BusySpinWaitStrategy（纯自旋，极致低延迟但烧 CPU）、YieldingWaitStrategy（游戏服低延迟常用）。\n6. 批量消费：消费者一次把 RingBuffer 里积压的消息 drain 一批处理，摊薄调度成本。\n游戏服配合：\n1. Netty IO 线程解码后 publishEvent 进 RingBuffer，立刻返回，不阻塞网络线程。\n2. 多个消费者按「玩家 ID % 消费者数」分工认领消息，保证单玩家串行。\n3. 慢操作（DB/日志）由业务线程二次投递到独立 Disruptor 或线程池，主链路永不阻塞。",
      point: "考察 Disruptor 四大提速机制及与业务线程模型的配合",
      approach: "先给四快总纲（预分配、序号、填充、批量）→ RingBuffer 与序号机制讲细（2 的幂取模、防覆盖）→ 等待策略按延迟/CPU 取舍给选型 → 游戏服三段配合讲清。等待策略选型最能体现真实使用经验。",
      followups: [{q:"RingBuffer 满了生产者怎么办？", a:"生产者申请序号时检查最慢消费者序号，满了默认自旋等待；应配置超时或降级策略，并监控 RingBuffer 水位做背压告警，防止生产端无限空转。"}, {q:"多生产者模式下 Disruptor 怎么保证不丢不重？", a:"多生产者 CAS 竞争 cursor 拿全局唯一序号，写入后发布时更新对应槽位的可用标记，消费者等标记置位才读；序号唯一保证不丢不重。"}, {q:"游戏服消费者线程挂了消息会丢吗？怎么做持久化兜底？", a:"RingBuffer 是纯内存队列，宕机即丢。关键消息先写 Kafka/WAL 日志再进 RingBuffer，重启后回放恢复；监控消费者心跳，挂了自动拉起并从未消费序号续跑。"}],
      memory: "口诀：「环数组、抢序号、填缓存、批量吃」；类比：环形旋转寿司带——盘子（数组）固定循环用，厨师（生产者）往空位放，客人（消费者）按号牌顺序取，盘子数永远是 2 的幂方便定位。",
      tags: ["Disruptor", "RingBuffer", "无锁队列", "等待策略"]
    },
    {
      id: "java-concurrent-23",
      level: 3,
      q: "玩家数据的高并发写入怎么设计？假设 5000 玩家同时在线，金币扣减、背包变更、数据落盘全链路你会怎么做并发控制？",
      a: "核心结论：分层防御——内存层靠「玩家绑定线程串行化」消灭竞争，落盘层靠「批量合并 + 乐观锁版本号」兜底一致性。\n内存层（热路径）：\n1. 玩家上线加载数据到内存，玩家 ID 哈希绑定固定业务线程，该玩家所有写操作串行执行——单玩家视角无并发，不需要锁。\n2. 金币扣减等校验在串行上下文里直接做余额判断，天然原子。\n3. 跨玩家操作（交易、赠送）：把请求作为消息投递到目标玩家的线程，严禁跨线程直接改别的玩家对象；双方互操作（交易锁物品）设计明确的时序协议防死锁。\n落盘层（持久化）：\n1. 不实时写库：内存标记脏位，定时（如 5 分钟）+ 关键节点（下线、关服）批量落盘，大幅降 DB 压力。\n2. 落盘在独立异步线程池，业务线程只提交事件——DB 慢不影响战斗帧率。\n3. UPDATE 带 version 乐观锁：UPDATE player SET gold=?, version=version+1 WHERE id=? AND version=?；影响行数为 0 说明数据被外部（GM 后台、其他服）改过，告警 + 重读合并。\n4. 全量快照 + 增量日志：BI/审计需要变更流水，操作先写 Kafka 日志服再落库，崩了可回放。\n容错与关服：\n1. 关服流程：volatile 停服标志 → 拒绝新登录 → 踢玩家下线触发落盘 → 等落盘线程池 drain → JVM 退出。\n2. 宕机兜底：增量操作日志在 Kafka，重启后按日志重放恢复到最近一致点。\n架构级取舍：宁可「单线程串行 + 异步批量落盘」也不要「多线程 + 锁 + 实时写库」——前者单服轻松撑几千在线，后者在锁竞争和 DB IO 上双双爆炸。",
      point: "考察内存串行化 + 批量落盘 + 乐观锁的全链路并发设计",
      approach: "架构综合题，按分层答：内存层玩家绑线程串行化消灭竞争 → 落盘层批量合并 + version 乐观锁 → 容错关服与日志回放 → 最后给取舍结论。面试官看体系化思路，每层给一个关键机制讲透即可，别陷细节。",
      followups: [{q:"批量落盘间隙宕机丢 5 分钟数据，怎么跟老板交代？", a:"关键操作（充值、交易）实时落盘不走批量；普通操作先写 Kafka 操作日志，宕机后按日志回放恢复到最近一致点；残余分钟级损失属可接受风险并事先明确。"}, {q:"跨服场景两个服同时改同一玩家数据怎么办？", a:"约束玩家同时只在一个服活跃；跨服用迁移协议：源服冻结数据转只读 → 数据迁到目标服 → 目标服接管写，杜绝两服同时写内存。"}, {q:"GM 后台直接改库怎么和游戏服内存数据保持一致？", a:"严禁绕过游戏服直改库。GM 操作发消息到游戏服，路由到玩家绑定线程执行内存修改并走正常落盘流程，保证内存与库单一写入路径。"}],
      memory: "口诀：「内存串行免锁，落盘批量带版本，操作日志先行」；类比：记账——柜员（业务线程）只在自己账本上顺序记，每天打烊后统一誊到总账（批量落盘），誊写时核对页码（version）防别人动过。",
      tags: ["玩家数据", "并发写入", "乐观锁", "批量落盘", "游戏服架构"]
    },
    {
      id: "java-concurrent-24",
      level: 3,
      q: "高并发计数场景下 AtomicLong 和 LongAdder 怎么选？LongAdder 为什么快？游戏服在线人数/日志统计该用哪个？",
      a: "核心结论：低竞争两者相当，高竞争下 LongAdder 用「分段 CAS + 汇总」碾压 AtomicLong 的「单点自旋」，以空间换时间。\nAtomicLong 的瓶颈：\n1. 所有线程 CAS 同一个变量，N 个线程竞争时 N-1 个自旋失败重试，CPU 空转且缓存行在核间疯狂乒乓。\n2. 写越多越慢，是单点瓶颈。\nLongAdder 的设计：\n1. 分段思想：内部一个 base 值 + Cell[] 数组，线程哈希到不同 Cell 上各自 CAS 累加，竞争被分散到多个「槽位」。\n2. 冲突时扩容：Cell 数组按需扩容（2 的幂），自动降低冲突。\n3. sum() 时把 base + 所有 Cell 汇总——注意 sum 不是原子快照，统计期间有并发写会读到近似值。\n4. Cell 标 @Contended 防伪共享（与 java-concurrent-21 呼应）。\n选型标准：\n1. 高频写、低频读（日志计数、QPS 统计、命中数）：LongAdder。\n2. 需要精确的原子返回值（incrementAndGet 的结果立刻要用，如发号器、库存扣减判断）：AtomicLong，因为 LongAdder 的 sum() 不适合做强一致判断。\n游戏服场景：\n1. 全服消息 QPS、日志写入量、协议错误计数 → LongAdder，压测下单服几十万 TPS 时 AtomicLong 会明显拖后腿。\n2. 全局唯一 ID 发号 → AtomicLong 或号段模式，因为要拿到精确的递增值。\n3. 在线人数：登录/登出增减要拿准确值做判断（如满员拒绝），用 AtomicInteger；纯监控展示可 LongAdder。",
      point: "考察 LongAdder 分段思想及按一致性要求选型的判断",
      approach: "先给选型结论 → 分析 AtomicLong 单点瓶颈（自旋 + 缓存行乒乓）→ LongAdder 三板斧（Cell 分段、冲突扩容、sum 汇总）→ 按「要不要精确值」给游戏服场景分流。关键洞察是 sum() 非原子快照，这是选型分水岭。",
      followups: [{q:"LongAdder 的 sum() 为什么不准确？什么场景能接受？", a:"sum 无锁遍历 base + Cell 求和，期间并发修改不阻塞也不重读，结果是近似值。监控统计类可接受；扣费、满员判断等强一致场景必须用 AtomicLong 精确原子读。"}, {q:"Striped64 的 cellsBusy 是什么作用？", a:"自旋锁标志，仅在对 Cell 数组初始化/扩容时 CAS 加锁，保护数组结构变更；正常累加路径不走它，几乎零开销。"}, {q:"JDK 还有没有类似「空间换并发」的设计？", a:"ConcurrentHashMap 的 size 统计用同样套路：baseCount + CounterCell 分段计数，汇总时求和。思路都是热点分散、读时汇总。"}],
      memory: "口诀：「单点抢不如分头记」——AtomicLong 是全公司在一个 Excel 格子抢编辑，LongAdder 是每人一个 sheet 各记各的，月底（sum）再汇总；但要拿「现在确切总数」就别用它。",
      tags: ["LongAdder", "AtomicLong", "高并发计数", "分段锁"]
    },
    {
      id: "java-concurrent-25",
      level: 3,
      q: "JDK21 虚拟线程（Virtual Thread）是什么原理？对游戏服务器意味着什么？你们的 Netty + Disruptor 架构还有必要吗？",
      a: "核心结论：虚拟线程是 JVM 调度的轻量级线程（M:N 映射到少量平台线程），让「一个请求一个线程」的阻塞式写法重新可行，但它解决的是「阻塞式高并发 IO」，不替代游戏服的「串行化消息模型」。\n原理：\n1. 平台线程（Platform Thread）1:1 对应 OS 线程，栈 ~1MB，创建/切换贵；虚拟线程由 JVM 调度，栈是堆上可伸缩的 continuation，创建成本接近零，可开百万级。\n2. 载体线程（Carrier）是 ForkJoinPool 里的平台线程；虚拟线程执行到阻塞 IO（socket read 等）时 JVM 自动卸载（unmount）让出载体，IO 就绪后再挂载（mount）恢复。\n3. 写法和普通线程一样：Thread.ofVirtual().start(task) 或 Executors.newVirtualThreadPerTaskExecutor()，同步代码获得异步的伸缩性。\n对游戏服的意义：\n1. 利好：登录服的阻塞式第三方调用（渠道 SDK 验证、支付回调）、GM 后台的 HTTP 接口、日志/BI 上报的同步写法，用虚拟线程后不用再硬拆回调/CompletableFuture，代码大幅简化。\n2. 不适合替代的部分：玩家消息串行模型（绑定业务线程）是「并发正确性」设计，不是线程成本问题；虚拟线程不解决共享数据竞争。战斗服 tick 循环这种 CPU 密集长任务用虚拟线程毫无意义（它还占着载体线程）。\n3. 两个大坑：\n   a. synchronized pinning：虚拟线程在 synchronized 块内阻塞时无法卸载，会钉住载体线程——老代码里的 synchronized 要评估换 ReentrantLock。\n   b. ThreadLocal 失控：百万虚拟线程 × ThreadLocal 大对象 = 内存爆炸，ThreadLocal 习惯要收敛（JDK21 有 ScopedValue 替代）。\n4. 结论话术：虚拟线程让「外围异步链路」回归同步写法，但 Netty IO 线程模型、Disruptor 投递、玩家绑定线程这套核心架构依然成立——面试时要能讲清「它优化什么、不优化什么」。",
      point: "考察虚拟线程原理边界及对游戏服架构影响的判断力",
      approach: "先给定位「解决阻塞式高并发 IO，不替代串行模型」→ 原理（M:N、mount/unmount、堆上 continuation）→ 利好与不适合场景分开讲 → 两大坑（pinning、ThreadLocal）→ 总结话术。核心是说清「它优化什么、不优化什么」。",
      followups: [{q:"虚拟线程在 synchronized 里阻塞为什么钉住载体线程？ReentrantLock 为什么没事？", a:"JDK21 里 synchronized 的 Monitor 进入路径不支持卸载，虚拟线程在其中阻塞时载体被一起钉住；LockSupport.park 做了卸载支持所以 ReentrantLock 没事。JDK24 已解决。"}, {q:"虚拟线程适合跑游戏服的战斗 tick 循环吗？为什么？", a:"不适合。tick 是 CPU 密集长任务几乎不阻塞，卸载机制用不上还白占载体线程；CPU 密集任务就该用贴核数的固定平台线程跑。"}, {q:"线程池限流还有意义吗？", a:"有，而且更重要。虚拟线程能开百万个，但下游 DB/第三方接口承载力没变，仍要 Semaphore/连接池限流；它解决线程成本，不解决下游容量。"}],
      memory: "口诀：「虚拟线程省线程，不省锁」——它让开线程像开协程一样便宜，但共享数据的并发约束一个不少；游戏服记「外围链路受益，串行模型不动」。",
      tags: ["虚拟线程", "JDK21", "Project Loom", "游戏服架构"]
    },
    {
      id: "java-concurrent-26",
      level: 2,
      q: "ForkJoinPool 的设计思想和普通 ThreadPoolExecutor 有什么不同？什么是工作窃取？并行流和 CompletableFuture 默认线程池有什么坑？",
      a: "核心结论：ForkJoinPool 是为「可分解的 CPU 密集递归任务」设计的分治线程池，靠「每线程一个双端队列 + 工作窃取」最大化 CPU 利用率；ThreadPoolExecutor 是「单队列 + 固定工人」的通用任务执行器。\n核心机制：\n1. 分治：ForkJoinTask（RecursiveTask 有返回值/RecursiveAction 无返回值）把大任务 fork 成小任务，join 等子任务结果合并，如归并排序、大批量数据并行处理。\n2. 工作窃取（Work Stealing）：每个工作线程有自己的双端队列（deque），自己从头部 LIFO 取任务；空闲线程从别的线程队列尾部 FIFO「偷」任务——减少竞争、热点任务天然被拆分。\n3. 对比 ThreadPoolExecutor：单阻塞队列所有线程争抢，适合「互相独立、可能阻塞」的任务；ForkJoinPool 的任务约定「不阻塞、纯计算」，一阻塞整个池就瘫。\n默认公共池的坑（面试高频）：\n1. parallelStream 和 CompletableFuture（不传 executor）默认共用 ForkJoinPool.commonPool()，大小 = CPU 核数 - 1，全 JVM 共享。\n2. 游戏服里在 commonPool 里跑阻塞 IO（查库、调渠道接口）会把公共池占满，连累同进程所有 parallelStream 和默认 CF 任务——必须传自定义线程池。\n3. 公共池线程是 daemon，且线程名不可控，出问题难排查。\n游戏服可用场景：\n1. 适合：离线批量计算（导表工具解析全量配置、日志批量聚合）、AOI 九宫格大规模实体遍历这类纯 CPU 可分治任务。\n2. 不适合：任何带 DB/网络阻塞的任务、玩家消息处理（有顺序性要求，ForkJoin 不保证顺序）。",
      point: "考察工作窃取机制、适用边界及 commonPool 共享池陷阱",
      approach: "先给分治定位并与 ThreadPoolExecutor 对比 → 工作窃取讲细（双端队列、头取尾偷）→ 适用/不适用边界 → commonPool 三大坑收尾。坑是高频考点，「公共池里别阻塞」要说成事故级警告。",
      followups: [{q:"工作窃取为什么自己从头取、偷从尾取？", a:"自己从头部 LIFO 取局部性好（缓存热）；窃取者从尾部 FIFO 拿，两端操作减少竞争；且尾部通常是先 fork 的大任务，偷走后拆分收益最大。"}, {q:"ForkJoinPool 里任务阻塞了会怎样？", a:"工作线程被阻塞占住无法参与窃取，任务堆积吞吐雪崩；managedBlock 可临时补偿增线程但有限。所以约定池内任务纯计算、不阻塞。"}, {q:"游戏服里什么场景你会主动用 parallelStream？", a:"仅限离线/启动期纯 CPU 批量计算，如导表解析、日志聚合；线上热路径禁用——公共池共享、线程不可控、阻塞会连累全局，不如自建池。"}],
      memory: "口诀：「ThreadPool 一个窗口大家排，ForkJoin 人手一摞活、干完去别人那儿偷」；坑记一句「commonPool 是公共厕所，别在里面睡觉（阻塞）」。",
      tags: ["ForkJoinPool", "工作窃取", "parallelStream", "commonPool"]
    },
    {
      id: "java-concurrent-27",
      level: 2,
      q: "JUC 原子类全家桶分几类？AtomicIntegerFieldUpdater 和原子数组类分别解决什么问题？游戏服里你会怎么用？",
      a: "核心结论：原子类分四类——基本类型、数组、字段更新器、引用（含版本号），共性是 volatile + CAS 无锁更新，选型看「要原子操作的目标是什么形态」。\n四大类：\n1. 基本类型：AtomicInteger/AtomicLong/AtomicBoolean——独立变量的原子读写与增减。\n2. 数组：AtomicIntegerArray/AtomicLongArray/AtomicReferenceArray——对数组指定下标元素做原子更新，不锁整个数组。\n3. 引用：AtomicReference（CAS 换整个对象引用）、AtomicStampedReference（带版本号防 ABA）、AtomicMarkableReference（带布尔标记）。\n4. 字段更新器：AtomicIntegerFieldUpdater/AtomicLongFieldUpdater/AtomicReferenceFieldUpdater——以反射方式对「已有类的 volatile 字段」做原子更新，前提是字段必须 volatile 且非 static。\nFieldUpdater 为什么存在：\n1. 对象数量巨大时，给每个对象配一个 AtomicInteger 实例太浪费（每个原子类对象有额外对象头开销）；FieldUpdater 一个静态实例服务所有对象的字段。\n2. 典型：Netty 内部大量使用（如 Channel 的状态位）；游戏服里玩家实体若要给某个 int 状态加原子操作又不想加锁，可用 FieldUpdater。\n游戏服用法盘点：\n1. AtomicBoolean：一次性开关（停服流程只执行一次 compareAndSet(false,true)）。\n2. AtomicReference：配置表「不可变对象整体替换」——热更新时 new 一份新配置，AtomicReference.set 切换，读侧完全无锁。\n3. AtomicLongArray：按槽位统计各协议号的调用量。\n4. 加分器：LongAdder/LongAccumulator（自定义聚合函数）高并发计数见 java-concurrent-24。\n边界提醒：原子类只保证「单变量」原子性，多变量一致性要靠 AtomicReference 包不可变对象、版本号、或回到线程串行模型。",
      point: "考察原子类四分类体系及 FieldUpdater 的内存节省动机",
      approach: "先给四分类总览（基本/数组/引用/字段更新器）→ 每类一句解决的问题 → FieldUpdater 的存在意义单独讲透（大户对象省内存）→ 游戏服用法盘点 → 边界提醒收尾。FieldUpdater 动机是差异化亮点。",
      followups: [{q:"FieldUpdater 对目标字段有什么要求？", a:"目标字段必须是 volatile（保证可见性）、非 static、非 final，类型与 Updater 匹配且可访问；内部靠反射拿偏移量 + CAS 操作。"}, {q:"AtomicReference 换配置表和 ReadWriteLock 保护配置表，优劣对比？", a:"AtomicReference 整体替换读侧完全无锁无阻塞，写侧需构建新对象；读写锁读侧有 CAS 开销、写时阻塞读。配置读极多写极少，AtomicReference 更优。"}, {q:"为什么原子类在高竞争下要升级成 LongAdder 思路？", a:"单变量 CAS 高竞争时 N-1 个线程自旋失败，CPU 空转且缓存行乒乓；LongAdder 把竞争分散到多个 Cell，冲突率指数下降，空间换时间。"}],
      memory: "口诀：「基本数组引用字段，全都 volatile + CAS」；记场景一句「开关用 Boolean，整体换配置用 Reference，大户对象省内存用 FieldUpdater」。",
      tags: ["原子类", "FieldUpdater", "AtomicReference", "CAS"]
    },
    {
      id: "java-concurrent-28",
      level: 3,
      q: "锁消除和锁粗化是什么？JIT 依据什么做这些优化？写代码时有什么启示？",
      a: "核心结论：锁消除和锁粗化是 JIT 编译期对 synchronized 的自动优化——依据是逃逸分析和代码形状，结论是「别为了想象中的性能手动微优化锁，JIT 比你想象的聪明」。\n锁消除（Lock Elimination）：\n1. JIT 通过逃逸分析判定锁对象不会逃出当前线程（别的线程永远访问不到），这把锁无意义，直接去掉。\n2. 经典例子：局部变量 StringBuffer sb = new StringBuffer()，append 是 synchronized 的，但 sb 只在方法内使用、不逃逸，JIT 把锁全消掉——所以单线程用 StringBuffer 并没有想象中慢。\n3. 条件：锁对象必须「不逃逸出方法/线程」（标量替换常伴随发生）；可用 -XX:+EliminateLocks 控制（默认开），-XX:+PrintEliminateLocks 观察。\n锁粗化（Lock Coarsening）：\n1. 如果 JIT 发现一串相邻操作反复对同一对象加锁解锁（典型：循环体内 synchronized 同一个对象），会把锁范围合并扩大，一次加锁覆盖整段，省去反复的 lock/unlock 开销。\n2. 例子：for 循环里 sb.append(...) 每次进出一个锁，JIT 粗化成循环外一把锁。\n对写代码的启示：\n1. 不必手动把循环里的 synchronized 挪出去「优化」——JIT 会做；但反过来，如果临界区逻辑上必须多次进入（中间要让出锁），别指望粗化，要考虑设计是否合理。\n2. 真正要手动控制粒度的是「业务层面」：游戏服里别把整段玩家处理包在一把大锁里——那是架构问题（该用线程串行模型），JIT 救不了你。\n3. 面试加分点：锁消除依赖逃逸分析，所以「方法内 new 的同步容器」不等于性能差；评估性能要用 JMH 实测，不靠直觉。\n关联考点：偏向锁（JDK15 废弃）也是「假设无竞争」的优化，与锁消除同属「按竞争强度省成本」家族。",
      point: "考察 JIT 锁优化原理及「勿手动微优化」的工程启示",
      approach: "先定调「JIT 比你想象的聪明」→ 锁消除（逃逸分析 + StringBuffer 例子）→ 锁粗化（循环内加锁合并到循环外）→ 三点启示（不用手动挪锁、业务粒度靠架构、性能用 JMH 实测）。StringBuffer 例子最直观好记。",
      followups: [{q:"逃逸分析除了锁消除还带来什么优化？", a:"标量替换：不逃逸对象拆散成成员标量分配在栈/寄存器，连堆分配都省；栈上分配：对象直接分配在栈帧，随方法返回自动回收，零 GC 压力。"}, {q:"锁粗化会不会把「本该细粒度」的锁变粗影响并发？", a:"只在相邻、同一锁对象的加解锁之间合并，语义不变、不影响正确性；真正的粒度问题出在业务设计（大锁包长流程），那不是 JIT 能救的。"}, {q:"怎么验证你的代码发生了锁消除？", a:"用 -XX:+UnlockDiagnosticVMOptions -XX:+PrintEliminateLocks 看编译日志；更实用的做法是用 JMH 对比开/关 EliminateLocks 的吞吐差异。"}],
      memory: "口诀：「逃不出去锁就消，连着加锁就合并」；类比：你一个人在书房（不逃逸）不用锁门（锁消除）；进出一栋楼十次都刷同一张卡，保安干脆给你开个常开权限（锁粗化）。",
      tags: ["锁消除", "锁粗化", "逃逸分析", "JIT优化"]
    },
    {
      id: "java-concurrent-29",
      level: 1,
      q: "Thread 的中断机制到底是怎么回事？interrupt()、isInterrupted()、interrupted() 有什么区别？为什么 stop() 被废弃？",
      a: "核心结论：Java 中断是「协作式」的——interrupt() 只设置一个标志位（或打断特定阻塞方法），停不停、怎么停由目标线程自己决定；stop() 之所以废弃，是因为它强制杀死线程会破坏数据一致性。\n三个方法的区别（高频混淆点）：\n1. interrupt()：实例方法，给目标线程设中断标志；若目标正阻塞在 sleep/wait/join/BlockingQueue.take 等，会抛出 InterruptedException 唤醒它（注意：抛出时标志被清除）。\n2. isInterrupted()：实例方法，查询标志位，不清除。\n3. interrupted()：静态方法，查询「当前线程」标志位，并清除——经典面试陷阱。\n协作式中断的正确姿势：\n1. 循环任务：while (!Thread.currentThread().isInterrupted()) { ... }，把标志位当停止信号。\n2. 捕获 InterruptedException 后：要么向上抛，要么 Thread.currentThread().interrupt() 恢复标志让上层感知——吞掉等于撕掉停战书。\n3. 正在阻塞在不可中断 IO（socket read、 synchronized 抢锁）时 interrupt 无效——这也是 NIO 用 Selector.wakeup 而 Lock 要提供 lockInterruptibly 的原因。\n为什么 stop() 废弃：\n1. stop 直接释放线程持有的所有锁并杀死它——如果线程改玩家数据改到一半，锁放了、数据是半成品，其他线程读到脏数据。\n2. 对比：interrupt 像「敲门提醒该收工了」，线程把手头的活收个尾再走；stop 像「直接拔电源」，账目必然错乱。\n游戏服应用：关服时对业务线程、异步落盘线程 interrupt + 标志位，让它们 drain 完队列、写完未落盘数据再退出——优雅停机的底层就是中断协作。",
      point: "考察协作式中断语义、三方法辨析及 stop 废弃原因",
      approach: "先给「协作式」定位 → 三方法逐个辨析，interrupted() 会清标志是经典陷阱 → 正确姿势三条（循环判标志、catch 恢复、认识不可中断 IO）→ stop 废弃用数据一致性讲 → 游戏服优雅停机收尾。清标志陷阱必提。",
      followups: [{q:"中断标志被 InterruptedException 清除后，为什么必须恢复？", a:"抛出异常时标志已清除；上层框架（线程池、shutdownNow）靠标志判断停止意图，吞掉不恢复，上层永远不知道要停，任务无法正常终止。"}, {q:"Future.cancel(true) 底层做了什么？", a:"对执行该任务的线程调 interrupt()；所以任务代码必须响应中断（检查标志或不吞异常）才停得掉，否则 cancel 只是设了个没人看的标志。"}, {q:"怎么停一个正在 socket.read() 上阻塞的线程？", a:"interrupt 对阻塞 IO 无效。方案：另一线程 close socket 触发 SocketException；或改用 NIO 用 Selector.wakeup；或给 read 设超时周期检查标志。"}],
      memory: "口诀：「interrupt 贴纸条，isInterrupted 只看，interrupted 看完就撕」；stop 记一句「拔电源的账务系统你敢用吗」。",
      tags: ["interrupt", "线程中断", "stop废弃", "优雅停机"]
    },
    {
      id: "java-concurrent-30",
      level: 2,
      q: "手写一个 wait/notify 版生产者消费者要注意哪些点？等待队列和同步队列（锁池）有什么区别？为什么推荐用 Condition？",
      a: "核心结论：wait/notify 的正确使用有四大铁律——synchronized 内调用、while 循环判条件、优先 notifyAll（或明确单条件用 notify）、理解「等待队列 ≠ 锁池」。\n四大铁律展开：\n1. 必须先持有锁：wait/notify 是 Object 的 Monitor 方法，不在 synchronized 内调抛 IllegalMonitorStateException。\n2. while 不是 if：被唤醒后要重新检查条件——防虚假唤醒（spurious wakeup）+ notifyAll 后条件可能被先醒的线程破坏。\n3. wait 释放锁进等待队列；notify 只叫醒一个（唤醒谁不保证），notifyAll 全叫醒（之后还要抢锁）。\n4. 共享缓冲用有界容器，满则 wait、空则 wait。\n两个队列的区别（面试官爱挖）：\n1. 等待队列（Wait Set）：调了 wait() 的线程在这，等的是「条件成立」的通知。\n2. 同步队列/锁池（Entry List）：想进 synchronized 没抢到锁的线程在这（BLOCKED），等的是「锁本身」。\n3. 流程：wait() 从持锁 → 等待队列；notify 从等待队列 → 锁池；抢到锁才真正继续执行。\n为什么推荐 Condition（ReentrantLock.newCondition）：\n1. 一把 Lock 可挂多个 Condition：缓冲区「满」和「空」各一个等待队列，生产者只 signal 消费者（notEmpty.signal()），不会像 notifyAll 那样把同类线程也叫醒白抢一轮。\n2. 支持超时 await、可中断 await。\n3. BlockingQueue 内部（如 ArrayBlockingQueue）就是 ReentrantLock + 两个 Condition 实现的——能复述这个实现就是高分答案。\n游戏服关联：手写生产者消费者是「简化版消息队列」，游戏服真实投递用 Disruptor（无锁化），但日志落盘等外围链路仍可能用 BlockingQueue——能讲清从 wait/notify 到 Condition 到 BlockingQueue 到 Disruptor 的演进线，就是你 10 年经验的体现。",
      point: "考察 wait/notify 四大铁律、两队列辨析及 Condition 演进理解",
      approach: "先给四大铁律框架 → 等待队列 vs 锁池讲清状态流转（wait→等待队列→notify→锁池→抢锁）→ Condition 的优势 → 用「能默写 ArrayBlockingQueue 实现」收尾拔高。两队列流转是核心加分点。",
      followups: [{q:"notify 之后线程立刻执行吗？", a:"不。被 notify 的线程从等待队列移到锁池，要等当前线程退出 synchronized 释放锁、并在锁池竞争中胜出，才真正继续执行。"}, {q:"用 notify 写一个双条件（满/空）缓冲为什么会死锁？", a:"满/空两个条件共用一个等待队列，notify 可能叫醒同类（生产者叫醒生产者），该醒的消费者永远睡着，最终全员等待即死锁。要么 notifyAll 要么分 Condition。"}, {q:"BlockingQueue.put/take 内部是怎么用两个 Condition 协作的？", a:"ArrayBlockingQueue 用一把 ReentrantLock + notFull/notEmpty 两个 Condition：put 满了 await notFull、成功后 signal notEmpty；take 对称。两类线程各睡各的队列，唤醒精确。"}],
      memory: "口诀：「持锁才喊人，while 防诈醒，满空分两队，signal 不 signal all」；类比：等待队列是「候车室等通知」，锁池是「检票口排队」——被通知了还得排队检票（抢锁）才能上车。",
      tags: ["wait/notify", "生产者消费者", "Condition", "阻塞队列原理"]
    },
    {
      id: "java-concurrent-31",
      level: 2,
      q: "StampedLock 相比 ReentrantReadWriteLock 改进在哪？乐观读的正确用法是什么？它有哪些坑？",
      a: "核心结论：StampedLock 在读写锁基础上加了「乐观读」——读不加锁、读完校验戳，读多写少场景把读路径成本降到接近零；代价是不可重入、API 复杂、容易用错。\n三种模式：\n1. 写锁 writeLock()：独占，语义同 ReentrantReadWriteLock 写锁。\n2. 悲观读锁 readLock()：与写互斥。\n3. 乐观读 tryOptimisticRead()：不加锁、不阻塞写，只拿一个 stamp；读完共享数据后 validate(stamp) 检查期间有没有写锁被获取过——有效则用读到的数据，失败则升级为悲观读重读。\n乐观读标准范式（必须背）：\n1. stamp = lock.tryOptimisticRead()。\n2. 把共享变量拷贝到局部变量（注意先读后校验，且字段拷贝顺序要小心，最好加 volatile 或依赖 final/不可变对象）。\n3. if (!lock.validate(stamp)) { stamp = lock.readLock(); 重读; 最后 unlockRead }。\n为什么比读写锁快：悲观读锁每次都要 CAS 更新 state（多核下缓存行竞争）；乐观读只是读一个 long 戳 + 内存屏障，零 CAS——读流量越大优势越明显。\n坑（面试官等你踩）：\n1. 不可重入：同线程重复获取读锁/写锁会死锁——StampedLock 不支持重入。\n2. 不支持 Condition。\n3. validate 失败必须重读，不能假装没事继续用。\n4. 写饥饿风险仍在：乐观读不阻塞写锁，但悲观读多了写线程照样排队；写多场景别用。\n游戏服场景：\n1. 坐标/位置查询：MMO 里玩家位置读远多于写（其他玩家 AOI 查询），官方示例 Point 类就是这个场景。\n2. 排行榜/在线列表这类「读极多、写可容忍读到旧值」的结构可用乐观读；但若数据本身能用「不可变对象 + volatile 引用整体替换」，那更简单，优先那个。",
      point: "考察乐观读范式、性能优势来源及不可重入等使用陷阱",
      approach: "先给「乐观读把读路径成本降到接近零」定位 → 三种模式 → 乐观读标准三步范式（拿戳、拷局部、validate）→ 为什么比读写锁快（零 CAS）→ 坑清单主动交代。范式要能默写，坑要主动说。",
      followups: [{q:"乐观读时如果写线程正在改数据，读线程会读到什么？", a:"可能读到改到一半的中间态，所以共享变量必须先拷到局部、validate 通过才使用；失败就升级悲观读重读，绝不能带脏数据继续跑。"}, {q:"StampedLock 为什么不可重入？", a:"设计取舍：stamp 只是个 long 戳，不记录持有线程和重入次数，换来极低操作开销；支持重入需要持有者簿记，会增加每次操作成本，故明确不支持。"}, {q:"悲观读锁和乐观读在内存语义上有什么区别？", a:"悲观读锁有完整 hb 保证；乐观读仅靠 stamp 的 volatile 读 + validate，使用不当（先校验后读、不拷局部变量）会看到撕裂数据，范式顺序不能错。"}],
      memory: "口诀：「乐观读先斩后奏——先看，看完对戳，戳不对加锁重来」；类比：偷拍公告栏（不登记直接看），看完发现栏上有「今日已更新」新戳，说明看的可能是旧内容，老老实实去登记室重看。",
      tags: ["StampedLock", "乐观读", "读写锁", "读多写少"]
    },
    {
      id: "java-concurrent-32",
      level: 2,
      q: "死锁产生的四个必要条件是什么？线上怎么排查死锁？你在游戏服项目里遇到过（或会怎么预防）死锁吗？",
      a: "核心结论：死锁四条件——互斥、占有且等待、不可剥夺、循环等待，缺一不可；破坏任意一个即可预防；排查靠 jstack/ThreadMXBean/Arthas 三板斧。\n四条件与对应的破坏手段：\n1. 互斥：资源独占——一般无法破坏（锁的意义就是互斥）。\n2. 占有且等待：拿着锁 A 等锁 B——破坏：一次性申请所有锁，或用 tryLock 拿不到就放弃已有锁重试。\n3. 不可剥夺：破坏：tryLock(timeout) 超时主动释放，或允许抢占。\n4. 循环等待：破坏：规定全局加锁顺序（所有线程按同一顺序拿锁）——工程上最常用、最有效。\n排查工具链（线上实战）：\n1. jstack <pid>：直接输出 Found one Java-level deadlock 并列出持有/等待链——JDK 自带首选。\n2. jconsole / VisualVM / JMC：线程页签有「检测死锁」按钮。\n3. ThreadMXBean.findDeadlockedThreads()：可写进游戏服监控线程，定时自检 + 告警（BI/GM 后台展示），这是你项目经历的加分点。\n4. Arthas：thread -b 直接找出阻塞其他线程的锁持有者，线上无侵入。\n游戏服死锁高危场景与预防：\n1. 跨玩家操作：交易时同时锁玩家 A 和 B——必须按 playerId 排序后依次加锁（破坏循环等待），或者更彻底：投递到目标玩家线程串行化，从架构上消灭加锁。\n2. 战斗服 + 功能服 RPC：线程持有战斗上下文锁同步等 RPC 返回，RPC 回调又要进同一锁——规则：持锁期间绝不做阻塞 RPC，RPC 一律异步回调。\n3. GM 后台直连改库 + 游戏服内存并发：GM 操作走消息到游戏服玩家线程执行，不要双写路径。\n4. 兜底：所有 ReentrantLock 跨资源场景用 tryLock(timeout)，超时记录告警重试。",
      point: "考察死锁四条件、破坏手段与线上排查工具链的完整实战",
      approach: "先背四条件并逐一给破坏手段（工程上规定锁序最实用）→ 排查三板斧（jstack/ThreadMXBean/Arthas）→ 游戏服高危场景与预防（跨玩家按 ID 排序加锁、持锁不做 RPC）→ tryLock 超时兜底。监控自检埋点是经历加分项。",
      followups: [{q:"活锁和死锁的区别？", a:"死锁是线程互相阻塞干等；活锁是线程都没阻塞，一直互相让步/重试却永远无进展，像两人在过道互让。解法：引入随机退避，打破同步重试节奏。"}, {q:"饥饿是什么？和死锁活锁怎么区分？", a:"饥饿是某线程长期抢不到资源（低优先级、非公平锁被插队），但系统整体还在推进；死锁是相关线程全部卡住。公平锁、优先级调整可治饥饿。"}, {q:"jstack 没看到 deadlock 但系统就是不响应，还可能是什么原因？", a:"可能是极端锁竞争：大量线程 BLOCKED 在同一把热锁；或条件等待信号丢失，全员 WAITING 等一个永远不会来的 notify——变相死锁，要查等待链与唤醒逻辑。"}],
      memory: "口诀：「互占有、等不抢、成环死」——互斥、占有且等待、不可剥夺、循环等待；排查记「jstack 一眼、ThreadMXBean 自检、Arthas 线上」；预防记「加锁定顺序，持锁不 RPC」。",
      tags: ["死锁", "jstack", "ThreadMXBean", "Arthas", "线上排查"]
    },
    {
      id: "java-concurrent-33",
      level: 3,
      q: "一次线程上下文切换的代价有多大？由哪几部分构成？高并发系统怎么减少上下文切换？",
      a: "核心结论：单次上下文切换是微秒级（直接成本 ~1-5μs），但真正的代价在缓存污染等间接成本，高并发下每秒几十万次切换能吃掉大量 CPU——减少切换的核心是「让线程数匹配 CPU、用无锁和批处理替代阻塞」。\n成本构成：\n1. 直接成本：保存/恢复寄存器、程序计数器、栈指针，内核态切换，调度器运行——单次约 1~5 微秒。\n2. 间接成本（更致命）：\n   a. CPU 缓存失效：新线程的数据不在 L1/L2/L3，要重新预热（缓存行冷启动）。\n   b. TLB 失效：页表缓存刷新。\n   c. 调度抖动：频繁切换让热点线程跑不满时间片。\n3. 量级感受：Linux 下 vmstat 1 看 cs 列，每秒几万正常，几十万以上就要警惕；perf 可细看。\n什么时候切换特别多：\n1. 锁竞争激烈：抢不到锁的线程 park/unpark，每次都是一次完整切换——所以高竞争下 synchronized 重量级锁比 CAS 慢。\n2. 线程数远超核数：调度器被迫频繁轮换。\n3. 高频阻塞 IO：线程在 read 上反复挂起唤醒。\n减少手段（结合游戏服）：\n1. 线程数贴近核数：Netty IO 线程默认 2×核数，业务线程按核数定，别动辄几百线程。\n2. 无锁化：CAS、Disruptor 序号机制，让线程不等锁就不切换。\n3. 批处理：Disruptor 消费者一次 drain 一批消息、Netty 批量 flush——摊薄每次切换的成本。\n4. IO 多路复用：Netty 用少量线程处理万级连接，避免「一连接一线程」的海量切换。\n5. 协程/虚拟线程：JDK21 虚拟线程的挂起是 JVM 级 continuation 切换，不进内核，比平台线程切换便宜——这是它对 IO 密集服务的核心价值（呼应 java-concurrent-25）。\n面试话术：能说出「直接成本微秒级、缓存污染才是大头、vmstat 看 cs、无锁+批量+IO 复用三板斧」就是完整答案。",
      point: "考察上下文切换成本构成认知与减切换的系统化手段",
      approach: "先给量级（直接成本微秒级，间接成本才是大头）→ 成本三块（寄存器保存/缓存 TLB 失效/调度抖动）→ 什么时候切换多 → 减切换五板斧结合项目讲 → vmstat 观测收尾。强调「缓存污染是大头」体现深度。",
      followups: [{q:"为什么说 CAS 失败自旋也比阻塞切换划算？这个结论什么条件下不成立？", a:"自旋是用户态纳秒级空转，切换是内核态微秒级；但临界区长、竞争激烈时自旋空转累积比切换更贵。自适应自旋正是 JVM 对此的动态权衡。"}, {q:"vmstat 里 cs 飙高你怎么定位是谁在切？", a:"pidstat -w -t -p pid 看各线程主动（cswch）/被动（nvcswch）切换；被动高说明被抢占多（线程超核数），配合 jstack 看是否大量 BLOCKED 或锁竞争。"}, {q:"游戏服 tick 循环用 sleep 控制帧率有什么问题？", a:"sleep 精度受 OS 调度粒度限制（常 1~15ms）帧率不稳，且每次挂起唤醒都是一次切换。高精度场景用 LockSupport.parkNanos 或忙等 + 让出混合策略。"}],
      memory: "类比：厨师换灶——放下锅铲、洗手、走到新灶台只要几秒（直接成本），但新灶台的锅是凉的、调料位置不熟（缓存失效）才是耽误上菜的大头；解法：一个厨师守一个灶，别来回换。",
      tags: ["上下文切换", "性能优化", "vmstat", "无锁化"]
    },
    {
      id: "java-concurrent-34",
      level: 2,
      q: "CopyOnWriteArrayList 和 ConcurrentLinkedQueue 的并发原理分别是什么？游戏服里哪些场景该用它们、哪些场景不能用？",
      a: "核心结论：CopyOnWriteArrayList 用「写时复制 + 读无锁」适配读极多写极少；ConcurrentLinkedQueue 用「CAS 无锁链表」适配高并发入队出队；两者都用「读侧无锁」换「写侧或一致性上的让步」。\nCopyOnWriteArrayList（COW）：\n1. 原理：底层是不可变数组快照；写操作（add/set/remove）加锁 + 复制整个数组 + 替换引用（volatile）；读操作直接读当前数组，无锁。\n2. 特性：迭代器遍历的是快照，不抛 ConcurrentModificationException，但也看不到最新修改（弱一致性）。\n3. 代价：写一次拷贝一次数组，O(n)，写频繁或大列表内存翻倍——只适合「读 : 写 = 99.99 : 0.01」。\n4. 游戏服场景：监听器/回调列表（玩家上线回调、停服钩子——启动注册一次，触发读千万次）；在线 GM 指令处理器注册表；配置相关的小规模只读视图。\n5. 禁忌：实时排行榜、高频增删的在线玩家列表——写放大 + 数组拷贝会雪崩。\nConcurrentLinkedQueue（CLQ）：\n1. 原理：单向链表 + CAS 更新 head/tail 引用，无锁；offer/poll 通过 CAS 重试完成（Michael-Scott 无锁队列算法变体）。\n2. 特性：无界、非阻塞；size() 是 O(n) 遍历且不精确——别拿它做容量判断。\n3. 与 BlockingQueue 的区别：CLQ 不阻塞（poll 空返回 null），适合「消费者轮询/事件驱动」；BlockingQueue 适合「消费者 park 等数据」。\n4. 游戏服场景：轻量事件队列（低频异步事件投递）、无界可接受的内部分发；但核心消息链路仍用 Disruptor（CLQ 有节点对象 GC，Disruptor 预分配零 GC）。\n对比记忆：\n1. COW：为「读」牺牲写——复制。\n2. CLQ：为「读写并发」牺牲精确 size 和阻塞语义——CAS。\n3. 共同思想：读侧无锁 + 不可变/CAS，和 AtomicReference 换配置表是同一哲学。",
      point: "考察两种读侧无锁容器的原理、代价与场景边界",
      approach: "先给共同哲学「读侧无锁换写侧/一致性让步」→ COW 原理 + 写放大代价 + 游戏服场景与禁忌 → CLQ 原理 + size 不准特性 → 对比收束。每种容器都要给「该用」和「不能用」两边，选型意识才是考点。",
      followups: [{q:"COW 的迭代器为什么是安全的？快照语义带来什么一致性问题？", a:"迭代器持有创建时的数组快照引用，写操作复制新数组不影响旧快照，故不抛 CME；代价是弱一致：遍历中看不到新增元素，事件通知场景需确认可接受。"}, {q:"ConcurrentLinkedQueue 的 size() 为什么不准且贵？", a:"size() 无锁遍历整条链表计数，O(n)；遍历期间并发增删无人暂停，结果只是近似值。拿它做容量控制必出 bug，应用有界结构或独立计数。"}, {q:"JDK8 后 COW 的加锁方式有变化吗？", a:"JDK8 及之前写路径用 ReentrantLock，之后改为 synchronized——侧面说明 JDK 对 synchronized 优化后的信心，也呼应锁升级考点。"}],
      memory: "口诀：「COW 写时抄家，CLQ 无锁抢链」；类比：COW 像改公告栏——摘下来整块重抄再挂回去，看的人永远看到完整一块；CLQ 像排队接龙——每个人 CAS 把手搭到队尾，谁也不拦谁。",
      tags: ["CopyOnWriteArrayList", "ConcurrentLinkedQueue", "并发集合", "无锁"]
    },
    {
      id: "java-concurrent-35",
      level: 3,
      q: "并发环境下的对象池怎么设计？Netty Recycler 的线程局部池是什么思路？你做导表/协议工具时对对象分配有什么心得？",
      a: "核心结论：对象池用「复用」换「分配 + GC」成本，但池本身是共享资源，并发安全是设计核心——主流解法是「线程局部缓存优先，共享池兜底」，Netty Recycler 是教科书级实现。\n为什么池化（游戏服动机）：\n1. 高频短生命周期对象（协议消息、战斗伤害数字、临时 DTO）制造大量年轻代 GC；池化后对象复用，GC 压力骤降。\n2. 但乱池化是反模式：分配本身很快（TLAB 指针碰撞），只有「构造贵、数量大、生命周期明确」的对象才值得池化——先 profiling 再池化。\n并发对象池的三种设计路线：\n1. 全局共享池 + 锁/CAS（如 Apache Commons Pool 的 LinkedBlockingDeque）：实现简单，但还/取都过共享结构，高并发下有竞争。\n2. 线程局部池（每个线程自己的小池）：同线程借还零竞争；缺点：A 线程创建的对象在 B 线程归还时回不到 A 的池。\n3. Netty Recycler 的混合设计（重点）：\n   a. 每个线程一个 Stack 本地池，同线程借还纯无锁。\n   b. 跨线程归还的对象进「创建线程」的 WeakOrderQueue（弱引用排序队列），由创建线程下次借出时收编回自己的 Stack——归还方只做一个无锁入队。\n   c. 结果：借出/归还全路径无锁，且对象最终回到「熟悉」它的线程（线程亲和，缓存友好）。\n池化对象的并发铁律：\n1. 对象归还前必须重置（reset），脏状态泄漏给下一个使用者是经典 bug（和 ThreadLocal 残留同理）。\n2. 池化对象绝不能同时被两个线程使用——借还协议要保证唯一所有权（可用 AtomicReference/状态位守护）。\n3. 池要有界：无界池 = 内存泄漏的温床。\n结合你的经历话术：\n1. 协议工具：解码出的消息对象如果走 Disruptor RingBuffer，Event 是预分配复用的——这本身就是对象池思想（覆盖写代替新建）。\n2. 导表工具：批量解析时的行 DTO/字符串可用池化 + 字符串去重（String.intern 或自维护 map）降内存峰值。\n3. Netty 的 ByteBuf 池化（PooledByteBufAllocator）就是 Recycler 的大规模应用——答题时把 Recycler 和 ByteBuf 串起来讲，直接呼应 Netty 经验。",
      point: "考察并发对象池的线程亲和设计及池化适用边界判断",
      approach: "先给池化动机与「先 profiling 再池化」的冷静边界 → 三种设计路线对比 → Netty Recycler 混合设计讲透（Stack + WeakOrderQueue）→ 池化铁律三条 → 结合 ByteBuf 与项目话术。Recycler 跨线程归还路径是核心，要讲清。",
      followups: [{q:"Recycler 跨线程归还为什么用 WeakOrderQueue 而不是直接锁原线程的池？", a:"直接锁有竞争；WeakOrderQueue 让归还方只做无锁入队，且弱引用指向创建线程的 Stack，创建线程死后队列可被 GC 回收，防悬挂泄漏。"}, {q:"对象池和 GC 的关系在什么情况下会反转（池化反而更差）？", a:"对象小、构造便宜时分配只是 TLAB 指针碰撞，池的借还/重置/同步成本反而更高；ZGC/Shenandoah 把停顿压到亚毫秒后，池化收益要重新 profiling 评估。"}, {q:"怎么防止池化对象被归还后还被旧引用使用？", a:"严格所有权移交：归还即失效，旧引用禁止再用；Netty ByteBuf 用引用计数 retain/release 管理，release 到 0 回池，再用抛 IllegalReferenceCountException。"}],
      memory: "口诀：「本地池优先，跨线归还寄回原主」；类比：共享雨伞架——自己工位（线程）放几把随取随还最快，别人用完的伞挂回「物主工位」的回收篮，物主上班时收回来，全程不用找管理员（无锁）。",
      tags: ["对象池", "Netty Recycler", "ByteBuf池化", "GC优化"]
    },
    {
      id: "java-concurrent-36",
      level: 2,
      q: "线程池大小到底怎么定？CPU 密集和 IO 密集分别怎么算？游戏服的各类线程池你会怎么配？",
      a: "核心结论：没有银弹公式，只有「估算起点 + 压测校准」——CPU 密集 ≈ 核数，IO 密集 ≈ 核数 × (1 + 等待时间/计算时间)，最终都靠监控和压测收敛。\n理论起点：\n1. CPU 密集（加密、协议编解码重计算、战斗数值结算）：N ≈ CPU 核数 或 核数 + 1——线程多于核数只增加切换不增加吞吐。\n2. IO 密集（DB 落盘、调渠道 SDK、Redis/Kafka 客户端）：N ≈ 核数 × (1 + W/C)，W 是等待时间，C 是计算时间；等待越久线程越多，让 CPU 在等 IO 时切去干别的活。\n3. 混合型：拆成两个池分别配，别揉一起——这也是游戏服天然分层的原因。\n游戏服各池配置思路（结合你的全链路经历）：\n1. Netty IO 线程（boss/worker）：默认 2 × 核数即可，规则是「只做编解码转发，绝不阻塞」——配再大也没用，瓶颈不在 IO 线程数。\n2. 业务逻辑线程（Disruptor 消费者）：≈ 核数，CPU 密集属性；玩家哈希绑定，靠无锁串行模型而非堆线程数。\n3. DB 异步落盘池：IO 密集，按 (1 + W/C) 估算起点（如 8 核机 DB 写均耗 5ms、业务组装 0.5ms → 8×(1+10)=88 太多，实际结合连接池上限取 16~32，且 ≤ DB 连接池大小，否则线程干等连接）。\n4. 日志/BI 上报池：可丢弃、低优先级，小池（4~8）+ 有界队列 + 丢弃计数。\n5. 登录/渠道验证池：调用第三方 HTTP 延迟不可控，池要独立 + 超时 + 熔断，绝不能共用业务线程池。\n校准与动态化：\n1. 压测观察：队列长度持续增长 = 池小或下游慢；CPU 打不满且队列空 = 池可减。\n2. 线上监控四指标：activeCount、queueSize、taskCount、rejectCount 上报 BI/Prometheus。\n3. 进阶话术：动态线程池（如美团 DynamicTp 思路）把 corePoolSize 等做成配置中心可调，配合告警——微服务项目里 Nacos 就能承载这个能力。",
      point: "考察线程池估算公式的应用及游戏服各池差异化配置",
      approach: "先泼冷水「没有银弹，估算起点 + 压测校准」→ 两个公式（CPU 密集贴核数、IO 密集乘等待比）→ 游戏服五类池逐个给配置思路 → 监控四指标与动态线程池收尾。具体数字与约束（≤ 连接池）体现实战感。",
      followups: [{q:"DB 线程池配得比连接池大很多会怎样？", a:"多余线程全阻塞在获取连接上，白占内存与调度资源，还可能在连接池内部锁上竞争；池大小必须 ≤ 连接池上限，瓶颈对齐才有意义。"}, {q:"一个 4 核容器跑着 200 线程的服务，你觉得正常吗？", a:"看构成：Netty IO、定时、虚拟线程多不算问题；但若有 200 个阻塞式业务线程在 4 核上跑，上下文切换必然爆炸，CPU 大量耗在调度而非业务。"}, {q:"突发流量时线程池怎么弹性应对？", a:"多层防御：有界队列缓冲削峰 → 救急线程扩到 max → 拒绝策略降级（反压/落 Kafka）→ 上游网关限流；配合水位监控做扩缩容决策。"}],
      memory: "口诀：「CPU 贴核数，IO 乘等待比，混合拆开算，最后压测拍板」；类比：厨师（线程）炒菜（CPU）一个灶台一个厨师刚好；等上菜（IO）的时间越长，一个厨师能看的灶台就越多。",
      tags: ["线程池调优", "CPU密集", "IO密集", "压测"]
    }
  ]
};
