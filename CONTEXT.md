# CONTEXT.md — Java 面试复习库领域术语表

> 本文件是「题库 + 知识库」系统的领域词汇表，只定义概念及其关系，不含实现细节。
> 架构决策见 `docs/adr/`。

## 领域对象

### 分类（Category）
题库的 18 个复习主题（如 Java 基础、Redis、Netty）。每个分类拥有三类资产：**题库**、**知识图谱**、**教程节点集**。其中「学习方法」「记忆方法」两个分类为方法论内容，不产教程。

### 题库（Question Bank, QB）
每分类一组面试题。每题五件套：考点 / 回答思路 / 参考答案 / 举一反三追问 / 记忆钩子，并有难度等级（基础/进阶/深挖）。题库是「检测」形态。

### 教程（Tutorial）
面向知识欠缺者的「补课」形态内容，与题库互补。每分类由若干**教程节点**组成，节点按**依赖**关系组织成**知识图谱**。

### 教程节点（Tutorial Node）
图谱上的一个节点，粒度=主题级（每分类 8~12 个）。一个节点 = 一篇八段结构教程：①一句话定位 → ②前置知识 → ③核心讲解 → ④代码示例 → ⑤SVG 图解 → ⑥常见坑 → ⑦关联题目 → ⑧小结。

### 知识图谱（Knowledge Graph）
每分类一张有向无环图（DAG）。节点=教程节点；边=前置依赖（「先学 A 再学 B」）。节点按 **学习层** 四层排布。

### 学习层（Learning Layer）
图谱的纵向分层：入门 → 核心 → 进阶 → 实战。体现该分类由浅入深的固定学习顺序。

### 依赖（Dependency）
「先学 A 再学 B」的前置关系。依赖未学完的节点**灰显**（提示性，不强制拦截）。

### 关联题目（Covered Questions）
教程节点显式声明的题库题目集合（题目 ID 列表）。用途：错题/不会题 → 补课直达；教程 → 学完自测。映射关系单向：教程 → 题目；题目侧不感知教程。

### 补课（Remediation）
用户做题遇阻后跳转教程补齐知识的学习行为。触发点全覆盖：题目卡片、自测「不会」、错题本、间隔重复答错。

### 学习状态（Learning State）
教程节点的掌握状态：**未学 / 学习中 / 已学完**。「已学完」由用户手动标记，附加自动记录最后阅读位置（续读）。

### 间隔重复（Spaced Repetition, SRS）
针对「掌握」题目的到期复习调度。当前为固定节奏 1/3/7/15 天，推进依据为「上次掌握/复习时间」。2026-08-08 审查确认存在机制缺口：复习动作无「答对/答错」反馈采集，答不出也照样推进，是「学了记不住」的主要成因。

### 复习反馈（Review Feedback）
SRS 复习时的掌握程度反馈（答对/答错，或四档评分）。当前缺失——「已复习」按钮不区分对错；自测模式的 1/2/3 评分也不回流 SRS 调度。规划方向：答错 → 重置间隔回 1 天重新爬 + 自动进错题本 + 触发补课；答对 → 正常推进。

### 掌握度（Mastery）
题目/节点的掌握程度度量。当前为二元态（已掌握/未掌握），无中间档。规划方向：与复习反馈、自测评分打通后形成多档掌握度，驱动更精确的复习调度。

## 交互形态

### 双视图（Dual View）
应用内两个平行视图：**刷题视图**（现有）与 **教程视图**（新增）。教程视图内含图谱视图与阅读视图。两视图状态各自保存，可互相跳转。

### 随堂小测（Quiz）
每篇教程末尾 3 道自测题。答错自动进入错题本，形成「学完即测」闭环。

### 教程笔记（Tutorial Note）
每篇教程可折叠的个人批注区，仅自己可见，复习时优先回看。

## 内容规范

### SVG 图解（SVG Diagram）
教程中的内联矢量图。标准：每篇 1~3 张；按内容匹配图型（原理架构图 / 流程时序图 / 对比图 / 数据结构图）；中文标注、字号 ≥12px、统一配色且跟随深色模式、自适应缩放。

### 存疑标注（Unverified Flag）
教程事实未经权威来源确认时，在内容处显式标注「存疑」，待人工确认，不静默混入正文。

## 非目标（Non-Goals）
- 教程不做成独立站/独立页面，不引入运行时依赖。
- 图谱不承载教程正文，节点只是入口。
- 「学习方法」「记忆方法」两分类不产教程。
